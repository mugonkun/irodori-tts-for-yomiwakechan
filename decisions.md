# decisions.md — 配布版 irodori-TTS（irodori-tts-for-yomiwakechan）の確定事項

> 本檔がこのリポの正典。依頼文（`research/`・yomiwakechan2/docs/irodori-ywk-repo-setup-brief-2026-09-04.md）は正典ではない。
> 用語＝**本体**＝yomiwakechan2／**配布版**＝このリポで作る Server＋ランチャ UI のアプリ／**上流**＝Aratako 氏の Irodori-TTS と Irodori-TTS-Server（無改変・submodule で pin）。
> 「司令官」＝利用者（mugonkun）。「席」＝この Claude Code セッション（Fable 5.1）。サブ席＝Workflow の opus 5。

## 設営時の確定事項 — 2026-09-04

### 司令官裁定（依頼文 §2 から持ち込み）

1. **別プロダクト・別リポ・本体に内包しない**。利用者は別途ダウンロードして導入。README 冒頭に「非公式・Aratako 氏とは無関係」。
2. **既定ポート 18088**・bind `127.0.0.1`・api_key なし（ループバック限定）。既存 8088（Irodori-TTS-Server 直結）と**併用可**。
3. **上流は無改変**＝submodule で pin（Irodori-TTS `8224dafb46d0aba89209a8f905f1cb7e3299d9c1`・Irodori-TTS-Server `841fb7c6ec57729c56b9b75c0ef2562249b13a10`）＋`patches/`（ビルド時適用）。本体との契約で足りない口は `server/ywk_server.py`（wrapper 1 檔・上流 app を import して路を足す）。
4. **CUDA 版＝既定 cu130・cu126 は利用者が選べる**。両版の取得台帳を持ち、検出ドライバが 580 未満なら cu126 を勧める（自動切替はしない）。cu126 が 2023 年秋ドライバに届くか（U-14）は RTX 機で実射して決着（便 B）。
5. **ROCm 未保障・Radeon 版は別リリース**（bf16 固定・起動時暖機）＝同じリポのビルド変種。保証文言は「gfx1151（Ryzen AI MAX+ 395／8060S）で確認済み」の事実だけ。
6. **起動主体＝配布版が常駐**（G-2）＝本体は `/health` で見つけるだけ。GPU 選択と精度は配布版の UI が env に載せる。
7. **既定値の焼き込み**＝`preload=true`・`empty_cache_interval=0`・精度は device 連動（GPU→bf16・CPU→fp32）・`allow_no_ref_voice=false`＋alias「デフォルト」・v4.1-Small 決め打ち・`voices_dir` 絶対パス・ready 待ち 120 s。

### 着任時に卓へ上げ、司令官が裁定した 5 点（2026-09-04・すべて管制推奨を採用）

8. **実行系（torch・依存 wheel）もモデルも初回取得**＋**第三者バイナリを配布物に入れない**（依頼文 §2-7 ⒜⒝）。Release 資産＝ランチャ＋埋め込み Python＋自作分（数十 MB）。torch は `download.pytorch.org/whl/cu1xx` の pin URL＋sha256、依存は PyPI、モデルは HF、vc_redist は Microsoft 公式 URL。`licenses/` は Irodori 系 MIT 3 本（全文）＋自作分。**オフライン導入は捨てる**。
9. **透かし（SilentCipher）は既定 ON**＝重み（≈65 MiB）を初回取得に含める。切る経路は持たない。
10. **既定 steps は 40 のまま**（上流既定）。UI のプリセットで 10 を選べる。本体の指定が来たら必ず勝つ（上流の優先順 `irodori.X`→トップレベル→env と一致）。
11. **参照潜在キャッシュ（ref_latent の事前計算 `.pt`）は持たない**。GPU 前提で節約は 40〜115 ms のみ。Radeon 版で必要になったら別途起票。

### 司令官の希望（2026-09-04・依頼文の外で追加）

12. Python を知らなくても導入できる。本体から独立した配布パッケージ＋UI を持つアプリ。単体 TTS として扱える。設定可能パラメータ一覧を本体が取得できる（`GET /params`）。
13. **CPU 合成は可だが遅さはケアしない**（UI にだけ露出・配信用途では非推奨と明記）。
14. **深い先読み機構は余力があれば**＝本体スケジューラはまだ対応しないが、使いやすい形で契約に準備する（設計は便 A の契約文書で案を出し、実装は後続便）。
15. **UI の目的**＝利用者が自環境で GPU・CUDA 版・パラメータ・参照ボイスを試す（配信外の読み上げテスト）。本体からの読み上げ司令は UI で指定した GPU・CUDA 版を**暗黙に**使う。パラメータは本体の指定で動作。
16. **話者＝参照ボイス檔＋名付けのペア**（本体からは話者として見える）。**参照なし＝話者名「デフォルト」**（alias `no_ref`）。
17. **プリセット話者 12 名**をリリース版のテンプレート参照ボイスとして同梱する＝琴葉茜（関西弁・VOICEROID2）・弦巻マキ英語（CeVIO AI・英語分解読み上げ）・吉田くん（VOICEROID2）・月読アイ・月読ショウタ・ちび式じい・もち子さん（VOICEVOX セクシー／あん子）・つくよみちゃん（COEIROINK）・KANA ないしょばなし（COEIROINK）・MANA いっしょうけんめい（COEIROINK）・おふとんP きざ（COEIROINK）・おふとんP のーまる v2（COEIROINK）。
    作り方＝各エンジンで一次 wav を生成 →「！」「？」絵文字などを付与した本文で既存 Irodori-TTS-Server（8088）を参照ボイスとして二次ボイスを生成 → 二次ボイスをテンプレート参照ボイスにする。
18. **権利**（司令官確認済み）＝上記エンジンの生成ボイスは個人利用・商用・有償でも再配布可（元々そういう目的の商品）。本ソフトは無償配布なので問題なし。※ 席は推測で断定しない＝各エンジンの規約原文の所在は `licenses/` の台帳に司令官の確認日付と共に記す。

### 機体・受け渡し・停止域（2026-09-04）

19. **Radeon 機（この席・gfx1151）は 2026-09-07（月）まで**。以後 RTX 3090 機へ環境移行。両機は作業完了まで司令官は使わない＝自由に使ってよい。稼働機の 8088 も落としてよい（設営時点で 8088／7861 は停止中）。
20. **RTX 3090 機**＝セットアップ直後・個人情報なし・**Windows ごと壊してよい**・ただし **D:・E:・F: ドライブは触らない**。遠隔席 `12900k-new-enchanted-metcalfe`（Opus 5・Remote Control）で操作。実測時の事実＝ドライバ 591.86・CUDA 13.1・Windows 10 Pro 26200・Python 3.12.14（キット）・torch 2.10.0+cu130。
21. **受け渡し**＝`N:/temp_for_claudecode_agents/`（両機から同じ N:）。キット・結果＝`N:/irodori-native-research/`（kit-cuda・kit-ssd・listening・results-ssd）＝「好きにしてよい」。
22. **停止域**（依頼文 §5）＝上流 clone・`C:/IrodoriTTS/`・`C:/irodori-TTS-server/` を書き換えない／外部公開なし（GitHub private・HF／PyPI へ push しない・Release は裁定まで作らない）／第三者バイナリをコミットしない／ライセンスの結論を推測で断定しない／yomiwakechan2 の檔を 1 行も変えない／RTX 機のドライバ入れ替え（U-14）は司令官の明示の一言を得てから。

### 設営の事実（2026-09-04）

23. GitHub private リポ `mugonkun/irodori-tts-for-yomiwakechan` を作成し origin にした。第一コミット＝`research/`（調査便のテキスト分 160 檔・2.3 MB）。
24. Radeon 稼働機の ROCm 構成（読むだけ）＝Python 3.12.14・`torch==2.13.0+rocm10.0.0`・`torchaudio==2.11.0.2+rocm10.0.0`・index `https://stable.repo.amd.com/rocm/whl-next/`・`torch[device-gfx1151]`・HIP 7.15。Radeon 変種の取得台帳の種。
25. この機体の道具＝.NET SDK 10.0.400・Inno Setup 6（`%LOCALAPPDATA%\Programs\Inno Setup 6\ISCC.exe`）・pwsh 7.6.5・Python 3.13.7・uv 0.12.7。

### プリセット話者の一次 wav（2026-09-04・司令官）

26. **一次 wav は存在しない＝席が生成する**。手段＝yomiwakechan2 を使うか、各エンジンを個別起動して生成（VOICEVOX／COEIROINK＝HTTP API・CeVIO AI＝COM・VOICEROID2＝本体アダプタの方式に倣う）。二次ボイスは既存 Irodori-TTS-Server（8088・Radeon 機）を参照ボイス経路で叩いて生成。この作業は Radeon 機が使える 2026-09-07 までに済ませる（便 C と同じ枠）。
27. **弦巻マキ（英語・CeVIO AI）の一次 wav**＝日本語読みができないため、席が詰まったら飛ばして進めてよい。後で司令官が生成して提供する（2026-09-04）。

### 着任読解（統合席）が挙げた追加の卓 7 件の裁定 — 2026-09-04

28. **libsndfile（soundfile wheel 同梱・LGPL-2.1）＝通知のみ**（司令官裁定）。裁定 8 により soundfile は配布物に入れず利用者機が PyPI から取得するので、§6 の義務は配布側に立たないと読む。初回取得の通知（`licenses/first-run-notices.md`・ランチャの初回取得 UI）に「LGPL-2.1 の libsndfile を含む soundfile を取得する」旨とソースの所在 URL を載せる。
29. **facebook/dacvae-watermarked のライセンス表示＝Apache-2.0 と読む（3 対 1）**（司令官裁定）。`licenses/dacvae/` には GitHub facebookresearch/dacvae の LICENSE（Apache-2.0 全文）を置き、README に「HF カード本文 46 行目のみ SAM License の記述が残る（GitHub 側は 2025-12-19 `34e0b0d` で Apache-2.0 に修正済み・LICENSE 実体は不変）」と原文の檔と行、および「当方の読みである」旨を併記する。
30. 〔設計席の判断・依頼文と整合〕**torchcodec は配布物から外す**＝`patches/0001`（`inference_runtime.py` の `_load_audio`／`save_wav`・`codec.py` の `encode_file` の `except RuntimeError` → `except (RuntimeError, ImportError)`）。理由＝FFmpeg（LGPL/GPL）を落とせ、日本語パスの参照 wav は soundfile 経路で実射済み。torchcodec 経路は未実射のまま使わない。
31. 〔設計席の判断・依頼文 §2-2〕**api_key は持たない**（bind 127.0.0.1 固定）。受け入れ条件「安全」行は「既定 bind 127.0.0.1・api_key なし・empty_cache_interval=0・preload=true を焼く」に書き換える（`docs/acceptance.md`）。
32. 〔設計席の判断・依頼文 §2-2〕**ポートは 18088 で確定**。本体側は D-9（`IrodoriConstants.cs:35` の固定値と `App.xaml.cs:131` の baseUrl 受け渡し）の改修が必ずセット＝便 F の票に明記（`docs/contract.md` §9）。
33. 〔設計席の判断〕**`/params` の `cfg_scale_caption` の既定は 3.0（API 実効値）**。gradio 初期値 4.0 は説明文の注記に残す。`IRODORI_DEFAULT_CFG_SCALE_TEXT` を動かすと caption 側の既定も連動する副作用を `/params` の note に書く。
34. 〔設計席の判断〕**GPU 同定は UUID**（`torch.cuda.get_device_properties(i).uuid`・`nvidia-smi -L` は NVIDIA 専用の高速路）。`CUDA_VISIBLE_DEVICES` に UUID 形式（`GPU-xxxx`）を渡す経路は調査に実射記録が無いので、便 B（RTX 実射）で確かめてから採否を決める。それまでランチャは UUID→index 解決＋`cuda:N` で指定する。

### 便 P（プリセット話者）の事実と判断 — 2026-09-05

35. **一次 wav は 12 名中 11 名を席が生成した**（VOICEVOX 2・COEIROINK 5・VOICEROID2 4）。VOICEROID2 は本体に wav 取り出しが無いため `tools/preset-voices/Vr2SaveTool/`（net48 x86・Codeer.Friendly・音声保存ボタン→WPF 設定窓→Win32 保存ダイアログ）を新規実装して通した。一次は再生成しても sha256 が一致（決定的）。弦巻マキ（英）は裁定 27 のとおり司令官提供待ち。
36. **二次 wav は私設サーバ（8090・`.venv-rocm` を読むだけ・ROCm bf16・v4.1-Small・seed 1234・40 steps）で生成**。稼働機の 8088 と C:/irodori-TTS-server/ は無改変（上流に .pyc 0 件を確認）。生成物は再現的（前席と 1 バイトも違わない）。
37. **`voices/presets/*.wav`（席が Irodori で生成した二次ボイス）はリポにコミットする**（設計席の判断）。裁定 22 の「第三者バイナリ」には当たらない＝自作の生成物・配布物の同梱資産（11 本・32 MB）。一次 wav（各エンジンの出力）は `N:/temp_for_claudecode_agents/irodori-ywk/preset-voices/primary/` に保全しリポには入れない。
38. **プリセットの音量は正規化しない**。参照用途では上流が参照クリップごとに −16 dB へ正規化する（`ref_normalize_db=-16.0`・サーバの info 行で確認）ので、檔の RMS のばらつき（−16.6〜−23.8 dBFS）は合成結果に効かない。UI の試聴再生でだけ差が出る＝ランチャ側で再生時に揃えるかは便 D で判断。
39. **末尾切れの是正**＝検分席の所見（採用 11 本中 7 本の末尾 50 ms の RMS が全体 RMS を超える＝語の途中で終端）。同じ本文・同じ参照で seed を変え、末尾が減衰する射を機械的に採る。参照は 30 s 版を既定とし、司令官の試聴（`docs/preset-voices-listening.md`）で話者ごとに 10 s 版へ差し替えてよい。
    **判定規則（2026-09-05・便 P（3）で確定）**＝`tools/preset-voices/verify_wavs.py` の 3 条件すべてで clean＝⒜ 末尾 50 ms の RMS − 全体 RMS ≤ −20 dB ⒝ 末尾 50 ms の RMS ≤ −40 dBFS（絶対値）⒞ 末尾 300 ms を 25 ms 刻みで見て最後の 100 ms が直前より +0.5 dB を超えて立ち上がらない（−60 dBFS 以下は無音として除く）。旧規則（⒜のみ）では月読アイが Δ −20.19 dB の縁で通過して実体の末尾切れを見逃した。掃引の結果＝もち子さん 1235・ちび式じい 1235・つくよみちゃん 1240・KANA 1235・おふとんP きざ 1239・おふとんP のーまるv2 1240・琴葉茜 1235・月読アイ 1238・他 3 名は 1234。`voices/presets.json` の secondary に md5・size_bytes・tail 節を必須にした。
40. 便 C の暖機設計に効く実測（二次席）＝私設サーバの読込 18.98 s・spawn→ready 21.1 s。**未見の参照ボイス形状に当たるたびに decode_latent が跳ねる**（VOICEVOX/COEIROINK 参照で 0.6〜1.0 s → 最初の VOICEROID2 参照で 14.8 s・MIOpen の workspace 不足警告と同時）。暖機は no_ref 短文だけでなく、利用者が実際に使う参照ボイスで 1 本ずつ撃つのが確実。146 字の本文は `chunk_min_chars=80` で 2 分割される。

### 便 A（骨格＋ビルド）の完了と設計席の裁定 — 2026-09-05

41. **便 A 完了**（コミット fa05f63）。受け入れ条件 A-1〜A-8 を Radeon 機で実走して全緑＝CPU 変種の組み立て（台帳 101 item・903 MB・git/pip/uv 不使用）・パッチ 3 ハンク適用（submodule 無傷）・`/params` 1〜4 ms・CPU fp32 実合成（参照ボイス経由の 2 発目でパッチ適用を証明）・契約テスト 175 本・licenses 突合・第三者バイナリ 0 件。敵対検分 3 席（契約・ビルド・ライセンス）の high 9 件・medium 15 件は是正済み。
42. **依存解決の override `protobuf>=5.29.0` を採る**。理由＝descript-audiotools が引く protobuf 3.19.6 は `-nspkg.pth` を要し、`import site` を書かない `._pth` 運用（裁定の実射正本）と両立しない。実解は 7.36.1（調査便の lab 箱と同版）。
43. **取得台帳の kind に `sdist` を足す**（argbind・randomname は PyPI に wheel が無い）。展開は tar.exe（Windows 10 1803 以降の標準）。ランチャ（便 D）は .NET の tar 実装で同じ台帳を読む。
44. **numba／llvmlite（約 130 MB）は当面残す**。調査便は「両コードベースに import 0 件」と記録しているが、外す証明（A-4 の実合成）は便 B の CUDA 実射と一緒に行う。
45. **`IRODORI_DEFAULT_VOICE=デフォルト` を wrapper が焼く**（setdefault）。`/params` の `request.voice` が既定「デフォルト」を名乗る以上、voice 省略が上流の 400 になるのは矛盾。
46. **`licenses/first-run-notices.md` は配布物に入れる**。設計書 §1 の「配布物には入れない」は第三者物のことであり通知文ではない＝表現の誤り。設計書・檔冒頭・licenses/README・assemble-app の除外・check-licenses の検査を「入れる」に揃える。ランチャは初回取得前にこの檔を表示する。
47. **SSE（`stream_format:"sse"`）は受ける**（上流の機能を殺さない）。本体は使わない。契約文書に枠の形（`event: audio_chunk`／`done`／`error`・取消不可）を書く。
48. **caption と seed の空文字は wrapper が「未指定」に畳む**（上流は空 caption を 400 にする）。本体の新アダプタ（便 F）は「空は欄ごと省略」の現行規則のままで通るが、契約の文言が変わったことを便 F の票に書く。
49. 台帳の運用注意＝torch の pin URL は `download-r2.pytorch.org`（index の href の実体）で `download.pytorch.org` を fallback_url に持つ／`models.json` は pin した revision の全檔を持つので既存キャッシュに対する `--check-only` は README 等を file_missing と報告する（異常ではない）／初回取得後に `refs/main` を書かないと `HF_HUB_OFFLINE=1` で上流の読み込みが落ちる（是正済み・テストで釘）。

### 便 B の停止域の解除 — 2026-09-05

50. **U-14（RTX 3090 機のドライバを 2023 年秋の版に入れ替えて cu126 を実射）を司令官が承諾**（「入れ替え承諾」・2026-09-05）。便 B の台本に B-7 として組み込む。対象は RTX 機の C: だけ（D:/E:/F: は触らない）。終わったら現行 591.86 に戻すかは任意（壊してよい機体）。

### 便 D（ランチャ）の着工前の裁定 — 2026-09-05

51. **ランチャの .NET ランタイムは SelfContained publish（1 exe）**（司令官裁定）。.NET ランタイム（MIT・再配布条項が明確）を自作分と一緒に焼く。裁定 8「第三者バイナリを配布物に入れない」の**例外**として記帳する＝法解釈が立つ第三者物（MSVC・LGPL・NVIDIA）を避けるという趣旨に反しない。`licenses/` に .NET のライセンス（MIT）と第三者通知を置く。
52. 〔設計席〕ポート 18088 が塞がっていたら**止まって告知**する（次を探さない＝本体の定数と食い違うため）。試し撃ちの再生は NAudio（MIT）で、再生時に −16 dBFS 相当へ揃える（裁定 38）。UI は日本語のみ。設計書＝`docs/design/ben-d-launcher.md`（草稿・便 B・C の結果で §3・§5 を確定してから着工）。

### RTX 機の疎通と事実 — 2026-09-05

53. **遠隔席 `12900k-new-enchanted-metcalfe` と双方向の疎通を確認**（SendMessage の往復＋N: への檔 `ping/12900k-new-20260905-025233.txt`）。RTX 機の事実＝hostname `12900k-new`・pwsh 7.6.5＋PS 5.1（26100.9168）・git 2.55.0・**Python 3.14.7**（配布版は使わない＝埋め込み 3.12.10 を台帳から組む）・**uv 0.12.9**（`build/dev-venv.ps1` は 0.12.7 以外で止まる＝持ち込みの `uv.exe` 0.12.7 を PATH 先頭で使わせる）・RTX 3090 `GPU-19adfe89-c9e0-df55-4a8a-e31798717a36`・ドライバ 591.86・C: 空き ≈3.5 TiB・N: 可視・ドキュメントが OneDrive 配下（台本は全部 `-NoProfile`）・PSModulePath に Desktop 版経路が混在（pwsh→PS 5.1 の罠が成立し得る）。遠隔席の作業先は `C:/ywk/` を指定する（現状は `C:/claudeprobe`）。
54. **U-8（vc_redist 欠落機）は RTX 機で素のまま再現できない**＝`C:/Windows/System32/msvcp140.dll` 14.42.34438.0 が既に在る。便 B の B-3 は「System32 の msvcp140.dll の有無と版を検出し、在れば vc_redist を飛ばす」判定の実射に読み替える。欠落状態の実挙動を撃つには System32 の同 DLL を一時的に退避する必要がある（TrustedInstaller 所有・他アプリに影響）＝台本の最終手順として任意に置き、実施は司令官の一言を得てから。

### 遠隔席への交付の作法 — 2026-09-05

55. **交付経路＝`SendMessage`（双方向・実測）**。本体 `docs/operations.md` §3「remote-control 経由の実装席」の正典は「走行中の bridge 席への伝達は Routine の即時発火（作って・撃って・消す）・雲席からの SendMessage は届かない」だが、本席はローカル CLI なので `SendMessage` が往復で通った（返信 3 通・N: の檔）。Routine 経路は使わない。倣う作法＝⑴ 交付は全文を投げず「在り処＋枷」（N: の持ち込み物 `rtx-handoff/runbook/` と bundle 内の `probe/rtx-remote-runbook.md` を指し、停止域・成功判定・命名・チェックポイントをメッセージに書く）⑵ MCP 前提の指示を出さない（席が持つのは gh と PowerShell）⑶ モデルは席の自己申告で照合（bridge の get_session は last_served_model を返さない）⑷ 全工程 `-NoProfile`。
56. **ドライバ入れ替え（U-14）の承諾は遠隔席のセッションで改めて取る**（遠隔席の規則＝セッション間の許可の持ち回りは不可）。台本ではドライバ手順を独立した最終ブロック（現行 591.86・投入版と NVIDIA 公式アーカイブ URL・戻し方・セーフモードで標準 VGA に戻す復旧経路）にし、cu130／cu126 の 591.86 での測定を N: に書き戻したチェックポイントの後にだけ入る。降格後に測るのは cu126 のみ・cu130 の落ち方は事実として 1 回記録。
57. **U-8 の扱いは司令官の指示待ち**（AskUserQuestion を却下）。台本には System32 の msvcp140.dll の退避手順を入れない。B-3 は「検出して飛ばす」判定の実射のみ。

### 便 C（Radeon／ROCm 版）の完了と実測 — 2026-09-05

58. **便 C 完了**。ROCm 変種の台帳 `ledger/runtime-rocm-gfx1151.json`（107 item＝cpu と共通 99・AMD 由来 8＝torch 2.13.0+rocm10.0.0・torchaudio 2.11.0.2+rocm10.0.0・amd-torch-device-gfx1151・**amd-torch-device-gfx115x**（torch[device-gfx1151] が 2 本引く）・rocm（sdist・実行時必須）・rocm-sdk-core／libraries／device-gfx1151。AMD 由来 1.274 GB・展開 4,347.7 MiB・26,523 檔）。AMD 索引は sha256 を publish しないので落として計算し、生成器に pin（8 件）を持たせて再生成で照合する。rocm-bootstrap は落とす（torch の .py に参照 0 件・実射で不要）。
59. **実測（gfx1151・bf16・cuda:0・40 steps）**＝起動（spawn→loaded）空 db 29.0 s／温 26.1 s。暖機 6 射（no_ref 4/8/12 s＋話者 3 名）空 db 51.8 s／温 12.6 s＝機体初回の合計 80.8 s・2 回目以降 38.7 s。暖機後の同一プロセスで暖機済み話者 1 発目 1.49 s・参照なし 1.05 s・未暖機話者 1 発目 2.5 s（空 db では 9.9 s）。MIOpen db は `YWK_DATA_DIR/miopen/` に 3 檔 826 KB で落ち、プロセスを跨いで効く（暖機なし新プロセスの 1 発目 4.0 s・空 db 5.4 s）。**3 発目の再発（19.4 s）は HTTP 経路でも出ない**（研究の未確認を閉じた）。
60. **C-5 の「本物が待たされるのは暖機 1 射分（≤ 10 s）」は文言を改める**（設計席）。機構は効く（本物は走行中の 1 射だけ待ち `shots_done` は進まない）が、既定段 12 s の射は空 db で 11.7 s・温 db で 10.9 s＝1 射が 10 s を超えうる。条件文＝「本物が待たされるのは走行中の暖機 1 射分（既定段では最大 ≈12 s・ランチャは `/ywk/status.warmup` を見せる）」。段の秒に上限は設けない（12 s 段は長文形状の db を育てるため）。
61. **C-8 の RTF < 0.5 は未達**（11 プリセット・定常 0.27〜1.02・単発なら 0.25〜0.35）。原因 2 つ＝⑴ 話者切替ごとの prepare_reference 0.95〜1.44 s ⑵ 連続運転で sample_rf が 618→1,400 ms（2.3 倍・decode は不変＝MIOpen ではない・原因はクロック／電力／温度と推定・測る道具なし）。⑴ は ref_latent（.pt 事前計算）で消える見込み＝裁定 11「持たない」を **Radeon 版だけ再考する材料**として卓へ（便 D の前に司令官に問う）。⑵ は未解決の事実として `docs/acceptance.md` §3 に記帳。
62. **`empty_cache_interval=0`（裁定 7）で Radeon の GPU メモリは 1 セッション 10〜13 GB まで単調増加**（OS の GPU Process Memory・torch allocator の値ではない）。この機体は共有 107 GB なので破綻しないが、VRAM の少ない Radeon での既定は卓へ（便 D の前に司令官に問う）。
63. wrapper の追加＝`YWK_VARIANT`（rocm-* で bf16 固定・fp32 は exit 2・MIOpen db を `YWK_DATA_DIR/miopen/` へ）・`POST /ywk/warmup`／`DELETE /ywk/warmup/{id}`／`/ywk/status.warmup`（state に cancelled を含む）・`YWK_WARMUP_ON_START`・SSE 中も本物のカウンタを最後のチャンクまで保持・暖機の射も本番と同じ話者解決（`resolve_default_voice`）・ROCm torch に `YWK_VARIANT=cuda` のままなら stderr に警告。`verify-runtime.ps1` は `-Variant`／`-Device`／`-Precision`／`-HfHome` を取り、`/ywk/status` の variant・actual・precision・name・hip・gcn_arch を検分（rocm 17 checks・cpu 15 checks）。`assemble-runtime -ExpectGpu` は gcnArchName を台帳の gfx と突合し bf16 の matmul を 1 回撃つ。
64. 未特定ライセンスの追加＝rocm 版 torch の `torch/lib/liblzma.dll`・`aotriton_v2.dll`（License-File 107 件に該当なし）＝`docs/acceptance.md` §3 11 番。cu の zlibwapi・cpu の libiomp5md と同型（配布物には入れない＝初回取得の通知のみ）。

65. **参照潜在キャッシュ（ref_latent の事前計算 .pt）は Radeon 版だけ持つ**（司令官裁定・2026-09-05）。裁定 11 の「持たない」は CUDA 版に限る。Radeon 版では話者登録時（プリセットの初回展開時と利用者の追加時）に wrapper が `.pt` を 1 回焼き、`voices.json` の alias を `ref_latent` に向ける（上流の VoiceSpec は `ref_latent` を受ける・同 stem の `.wav` が `.pt` に勝つ穴があるので `.pt` は別名で置く＝research 40 §6-2・handoff §1-7）。実装は便 D（ランチャの話者台帳）と wrapper の `POST /ywk/voices/{id}/precompute` 相当で行い、`empty_cache_interval` の扱いは別途裁定待ち。

66. **便 B のドライバ入れ替えで再起動が要る場合＝席が `claude -c` で復帰するよう仕込んでから、そのまま再起動してよい**（司令官裁定・2026-09-05）。仕込み＝遠隔席が自分を起こした元のコマンド行（`Get-CimInstance Win32_Process` で自プロセスの CommandLine を読む）に `-c`（continue）を付けたものを `HKCU:\Software\Microsoft\Windows\CurrentVersion\RunOnce` か「ログオン時」のタスクに登録し、作業ディレクトリ・-NoProfile を保ったまま `shutdown /r /t 15` を撃つ。復帰後は同じ会話の続き（関門⑴の確認返信と関門⑵の承諾が済んだ後の手順）から再開する。自動ログオンでなければログオンは司令官が行う。裁定 56 の「席は自分で shutdown を撃たない」はこれで上書き。

### 司令官の追加要件（便 D・Radeon 版）— 2026-09-05 就寝前

67. **Radeon 版のランチャ UI**＝⑴ 参照潜在キャッシュの ON／OFF の切り替え（既定 ON・裁定 65）⑵ 参照ボイスごとの消費メモリの概算表示（wav 参照＝+0.7 GB 級・潜在参照＝増えない・出力尺 1 フレーム ≈3.5 MB＝research 40 §1・§6 と便 C（2）の実測で係数を確定）⑶ GPU メモリの使用量と占有量（torch の allocated と reserved・OS 側の GPU Process Memory）の目視表示。実装＝wrapper の `/ywk/status` に `memory`（allocated／reserved／max・話者ごとの潜在サイズ）を足し、ランチャが常時表示する。CUDA 版でも同じ欄を出す（値の意味は同じ）。
68. 二次 wav（プリセット）の司令官の試聴は**後回し**（`docs/preset-voices-listening.md` は据え置き）。

69. **就寝前の 3 点は既定どおり**（司令官「規定通りでよい」・2026-09-05）＝⑴ U-8 は検出判定の実射のみ・System32 の DLL は退避しない ⑵ Radeon 版の `empty_cache_interval` はランチャの設定項目（初期値 0）にし、使用量表示（裁定 67）で利用者が判断できる形にする（便 C（2）の実測を docs に残す）⑶ 便 D はこの Radeon 機で着工し、UIA 検分など機体固有の段は RTX 移行後に回す。

70. **便 B のドライバ入れ替え＝最新版（591.86）への復旧は不要**（司令官・2026-09-05）。降格したままでよい。承諾なしの実行は司令官の希望だが、遠隔席の規則（承諾はそのセッションで取る＝裁定 56）は本席から上書きできない。**司令官が遠隔席の会話に先に承諾を書いておけば関門⑵は先に満たせる**（台本と交付文にその旨を書く）。

71. **遠隔席は再起動の自動実行と RunOnce の自己登録を本席の指示では受けない**（そのセッションの利用者が明示した場合のみ）。よって裁定 66 の仕込みは**司令官が遠隔席の会話に直接書いたときだけ**行われる。台本 B-7 は再起動の前後で独立して再開できる区切りにし（進捗を `C:\ywk\` に残す・降格前の全測定は N: に書き戻し済み）、再起動が要る地点では止まって報告する。降格は「恒久」（裁定 70）として承諾の文面に書く。

72. **司令官が遠隔席の会話にドライバ入れ替えの承諾（恒久降格・戻し不要・RunOnce＋`claude -c` の自動再起動可）を直接書いた**（2026-09-05「承認を通知した」）。関門⑵は先に満たされた。残る関門は⑴＝降格前の測定完了の報告に対する本席の確認返信。

73. **便 B 着手**（2026-09-05 04:0x）＝遠隔席（セッション名は「Windows sandbox environment」に改名・同一席）が N: の台本を読み B-0（持ち込み・sha256 突合・bundle からの clone・submodule pin 確認・uv 0.12.7）を完走。bundle の HEAD＝`dc6525e4cacef1fc6ebb9e592ee0fa672fb8b4f2`（decisions 72）＝本席が最新と確認し B-1 へ進むよう返信。遠隔席の利用者は「この Windows 環境はサンドボックス・D:/E:/F: に触らなければ自由」と指示済み＝B-1 以降は連続で撃つ。関門⑴（降格前の測定完了の報告→本席の確認）は生きている。既知の不具合＝`build/make-handoff.ps1` が MANIFEST の head_line を CP932 で復号して uXXXX エスケープに落とす（檔は無傷・要修正）。

74. **便 B の再起動後のログオン**＝司令官の Chrome から対象機（12900k-new）へ Chrome Remote Desktop が繋がっており、再起動後は PIN 入力画面になる。PIN は司令官から本席に渡され、**席のローカルのメモリにだけ保持**（リポ・docs・台本・他セッションへのメッセージには書かない）。自動ログオンでなければ本席が Chrome の CRD 画面で PIN を入れ、遠隔席の RunOnce（`claude -c`）が復帰するのを待つ。

75. **便 B §2（降格前の測定）完了・関門⑴を本席が確認**（2026-09-05 04:3x・遠隔席の報告・repo HEAD dc6525e・結果は `N:\temp_for_claudecode_agents\irodori-ywk\rtx\`）＝⑴ cu130／cu126 とも台帳だけから組み上がり（torch と torchaudio の 2 檔だけ取得・他 99 件は持ち込みの cache hit・fallback_url は sha256 一致）⑵ モデル 22 檔 3,570,982,039 B を 68 s で取得・`--check-only` EXIT=0・refs/main 一致 ⑶ **U-8 は再現せず**（msvcp140.dll 14.42 が既在・裁定 54 どおり・検出判定のみ実射）⑷ GPU 実合成＝device.actual=cuda:0・RTX 3090・uuid `19adfe89-…`・pci_bus_id 1・bf16・cold cu130 1.363 s／cu126 1.258 s・warm の rtf_http（短文 40 参照なし 0.277／0.262・長文 40 0.101／0.096・短文 10 0.107／0.104・参照 30 s あり 0.309／0.313）＝kit の rtf_synth より HTTP 分だけ大きい（予算 0.25／0.10 を 10 % 台で超えるのは測定の定義差＝未達とは書かない）・VRAM はプロセス全体の占有で peak 7.8 GB（torch allocator の値ではない・idle 752 MiB に完全復帰）⑸ **CUDA では未見の参照形状の罰は無い**（11 プリセットの spread 1.12／1.30）⑹ cu126 と cu130 は 12 条件中 8 条件が ±5 % 以内・4 条件は cu126 が 5〜13 % 速い ⑺ **`CUDA_VISIBLE_DEVICES` に UUID（GPU-xxxx）を載せる経路は動く**（device.actual=cuda:0・2 射 200・不一致表示は接頭辞 `GPU-` の有無だけ＝比較を直す。1 枚機での確認）＝裁定 34 の暫定（UUID→index 解決）は不要にできる ⑻ 契約テスト 255 passed（新規 clone では `build/dev-venv.ps1` の smoke が transformers を要求して落ちる＝fallback では外す・要修正）⑼ 3 点セット（import torch 全文・is_available/version.cuda/device_count・nvidia-smi 全文）を両変種で採取済み。§3（ドライバ）は第 4 ラウンドの台本の到着待ち。

76. **便 C（2）完了＝Radeon 版の参照潜在キャッシュ（裁定 65）を実装・実測**。wrapper に `POST /ywk/voices/precompute`（202・優先度は暖機と同じ・rocm-* は ready 直後に自動）・`DELETE /ywk/voices/precompute/{id}`・`DELETE /ywk/voices/{id}/latent`（alias を wav に戻して .pt と sidecar を消す）・`/ywk/status.precompute`・一覧の `latent`／`latent_stale`。潜在は `<voices_dir>/latents/<stem(話者 id の sha256 12 桁)>.pt`（fp32・(T,D)・上流が runtime dtype へ cast）＋sidecar `<stem>.json`（元 wav の sha256・符号化条件・checkpoint・latent_dim）。alias は `{"ref_latent":"latents/<stem>.pt"}` に置換（temp→os.replace）。**実測（gfx1151・bf16）**＝焼き 11 名 11〜16 s・.pt 11 檔 1.1 MB・prepare_reference は焼く前 0.95〜1.44 s → 焼いた後 1.0〜4.3 ms（プロセス初見 7.7〜16.7 ms）・**C-8 は 2 巡目で 11/11 が RTF < 0.5**（0.22〜0.40）。プロセス最初の 1 発だけ 0.73〜0.85（暖機の枠）。便 C の原因⑵（連続運転で sample_rf 2.3 倍）は 22 射・35 s では再現せず＝解消の証明ではない（未確認のまま）。焼く工程は GPU メモリを約 3.6 GB 残す（研究の「潜在は VRAM を増やさない」は射ごとのピークの話）。
77. **`empty_cache_interval` 0 と 10 の材料**（裁定 69＝設定項目・初期値 0）＝射の所要への増分は見えない（10 射目 18.6 ms・20 射目 6.4 ms＝ばらつきの中）・OS 側の GPU メモリは =10 でも下がる標本 0（標本間隔 1.4〜1.8 s の限界）・11〜21 射目で =10 は 8.54 GB で平ら・=0 は 8.54→9.16 GB（1 走ずつなので断定しない）。裁定は据え置き（設定項目・初期値 0）。
78. 便 D への申し送り＝⑴ ランチャは話者を voices.json に書いた後に明示的に `POST /ywk/voices/precompute {"all":true}` を叩く（起動時の自動 all は ready 時点の voices.json を見る）⑵ voices.json は「書くときに読み直す」（wrapper が alias 欄を書き換えるため）⑶ 話者の削除は `DELETE /ywk/voices/{id}/latent` → wav と台帳の順。便 F への申し送り＝焼いた後は要求時の `ref_normalize_db`／`ref_ensure_max` が効かない（契約 ⑷ 4-4）。

79. **便 B の持ち込みを HEAD 6916264（便 B 準備 1〜4 のコミット）で作り直し、遠隔席へ「差し替え完了・§3 に入ってよい」を送信**（2026-09-05 05:02）。関門⑴＝本席が §2 の測定を確認（裁定 75）・関門⑵＝遠隔席の会話に承諾あり（裁定 72）。§3 の手順＝復元ポイント→直リンク HEAD→取得と sha256→サイレント導入（昇格・UAC は本席が CRD から押す）→ nvidia-smi→再起動が要れば RunOnce＋`claude -c`（遠隔席の利用者の承諾済み）→ 復帰後 cu126 の 3 点セット＋1 射・cu130 の 3 点セットを記録→ `SUMMARY-after.md`。降格は恒久（裁定 70）。

80. **U-14 の裁定＝cu126 は古いドライバで実効する（実射で確定・2026-09-05 05:35）。** RTX 3090・ドライバ **537.58**（2023-10・`nvidia-smi` の CUDA Version 12.2）で `torch 2.10.0+cu126`＝`is_available=True`・`device_count=1`・`cuda-bench -Variant cu126 -Quick -Label cu126-olddriver`＝4 検査すべて PASS（`/health` 200 まで 19.2 s・`/ywk/status` device.actual=cuda:0 bf16・2 射とも 200 で wav）。cold 1 発目 1.776 s（591.86 では 1.258 s＝1 標本ずつなので差は断定しない）・warm 0.996 s（RTF_http 0.265）・VRAM ピーク 5,196 MiB（2 射のみ・10 条件走の 7,844 とは比べない）。**同じドライバで cu130 は `cudaGetDeviceCount() returned cudaErrorNotSupported`＝`is_available=False`・`device_count=0`（import は通り、落ちない）。** 根拠＝`N:\temp_for_claudecode_agents\irodori-ywk\rtx\logs\step-B7-3-8-cu126-3pointset.log`・`step-B7-3-9-cu130-olddriver.log`・`probe-log-cu126-olddriver\`。**これで裁定 4 の「cu126＝古いドライバ向けと謳わない」は解除**＝配布文言は「cu126 は 537.58 まで実測済み」。起動前検査の閾値＝**cu130 ≥ 580.xx／cu126 ≥ 528.33**（NVIDIA の CUDA 12.x minor version compatibility の Windows 下限・**未実測**）とし、**537.58 未満は「未実測の帯」と UI で告知して合成は許す**（司令官の「既定どおり」の範囲で本席が置いた初期値＝覆してよい）。ランチャの variant 選択は `import torch` の成否ではなく **`torch.cuda.is_available()` と `device_count`** で決める（cu130 は古いドライバでも import が通るため）。
81. **UAC と CRD の実射で判った運用（台本 §3-6 に反映）**＝⑴ 同意窓の「はい」は**左**（既定でハイライトされるのは右の「いいえ」）。1 回目の 1223 は本席の誤押下。⑵ **同意窓が出ている間、CRD 側はキー入力を送らない**（本席の Escape が同意窓に当たって 2 回目の即 1223）。⑶ 2 度 1223 なら遠隔席は撃ち直さず、本席が CRD で「管理者: Windows PowerShell」を開き（タスクバー右クリック→ターミナル（管理者）→はい）、`cd /`→`cd ywk`→相対パスで `run-b7-elevated.ps1` を撃つ（CRD の鍵盤は日本語配列に写り `:`・`\` が化ける）。⑷ この機体の UAC は `PromptOnSecureDesktop=1`・`ConsentPromptBehaviorAdmin=5` だったが **CRD から同意窓は見え、押せた**＝遠隔席の「原理的に押せない」は取り下げ済み。⑸ 昇格ブロックのログは `Tee-Object` だと UTF-16LE になる＝`Out-File -Encoding utf8` に揃える。
82. **§3 の事実の記録**＝⑴ 3-3 の復元ポイントは **1440 分規則で作られなかった**（直近は 2026-09-03 21:43 の「Windows モジュール インストーラー」）＝巻き戻しの保険なしで 3-6 に入った（台本 3-11 の復旧経路は生きている・機体は壊してよい＝裁定 20）。⑵ 3-6 `-s -noreboot -clean` は **173 秒で `EXIT=1`**・**再起動前の `nvidia-smi` がすでに 537.58**＝台本 3-7 の表どおり再起動せず 3-8 へ進み、3-8 が通った（EXIT=1 の意味は断定しない）。⑶ 降格は恒久（裁定 70）＝591.86 へ戻さない。⑷ `SUMMARY-after.md` は遠隔席が書く（N: の `rtx\`）。
83. **B-10（W-1＝CUDA wheel のまま GPU を隠して CPU 合成）の実射（537.58・降格後）**＝**cu126 は通る**（`CUDA_VISIBLE_DEVICES=-1`・`/ywk/status` device.actual=cpu fp32・2 射とも 200・first 13,580 ms／warm 12,846 ms＝GPU の約 13 倍・-Quick なので速度行には使わない）。**cu130 は落ちる**＝起動は健全に見え（`/health` 200 を 50.8 s で・`/params`・`/ywk/status` も 200・device.actual=cpu）、**最初の合成で `prepare_reference: 0.1 ms` の直後にプロセスが 0xC0000005（STATUS_ACCESS_VIOLATION・-1073741819）で消える**。Python 例外は出ず、サーバのログにも残らず、クライアントには `ConnectionResetError 10054` だけ。**限界**＝降格後に撃ったため「GPU を隠した」と「537.58 では cu130 が GPU を見られない」を分離できていない。**分離測定はしない**（591.86 へ戻すのは裁定 70 の恒久降格と衝突・本席の判断）。**配布への帰結**＝⑴ ランチャは **`is_available()=False` の機体で cu130 を起動しない**（cu130 の CPU 転落は禁止＝cpu か cu126 の変種に切り替える。裁定 80 の variant 選択規則を強める）⑵ 「起動して `/health` が 200 でも最初の合成でプロセスが消える」形があるので、**ランチャの死活監視は `/health` だけでなくプロセスの生存（exit code）を見て、消えたら理由 1 行で告知する**（便 D への申し送り）⑶ 本物の NVIDIA 無し機での W-1 は**未確認のまま**（acceptance §3 の 6 は cu126 について「代用で通る」に更新・cu130 は「代用で落ちる・分離未」）。根拠＝N: `rtx\probe-log-cu126-w1-nogpu\`・`probe-log-cu130-w1-nogpu\`・`SUMMARY-after.md`。
84. **B-11（A14・A15＝`torch/lib` の削れる DLL）の実射**＝台本の path `\Lib\site-packages` は誤り（埋め込み Python は `runtime-<v>\site-packages`）で直した。実測（削除・改名・移動はしていない）＝**zlibwapi.dll は両変種に在る（89,088 B）**・torch 配下に LICENSE/NOTICE は再帰で 1 檔も無い（＝「wheel の third_party に zlib の LICENSE が同梱」の裏付けはこの機体では取れない・A14 は未解決のまま）。nvperf_host.dll＝cu130 27.8 MB／cu126 15.4 MB・nvrtc-builtins64＝4.5／5.3 MB・**nvrtc64_*_0.dll と同名 `.alt.dll` がほぼ同サイズで並ぶ**＝cu130 91.0＋91.0 MB／cu126 45.9＋45.9 MB（5 檔合計 cu130 204 MiB／cu126 107 MiB）。torch\lib 全体＝cu130 53 檔 2.49 GB／cu126 53 檔 3.75 GB（cu126 の torch_cuda.dll 1.04 GB・cudnn_engines_precompiled64_9.dll 514 MB）。**削ってよいかの判断は別便**（削減余地は `.alt.dll` が zlibwapi より 3 桁大きい）。
85. **便 B 終了（2026-09-05 05:5x）**＝§2（591.86 の cu130/cu126 本測定・UUID 経路）・§3（降格・U-14）・B-10・B-11 まで完了。成果物は N: `rtx\`（SUMMARY.md 無傷・SUMMARY-after.md 35,845 B・6 走行の json・step-*.log 30 檔）。遠隔席は commit/push なし・D:E:F: 不可侵を最後まで維持・ドライバ 537.58 のまま（恒久）。B-10 の分離測定は要らない（本席の判断＝裁定 70 優先）。管理者窓は本席が CRD から閉じた。
86. **便 D（ランチャ MVP）を ae5e7a4 で取り込んだ（2026-09-05 06:4x）**＝WPF／.NET 10・SelfContained 1 exe（66.4 MiB・裁定 51）・骨組み→取得席／起動席／画面席の 3 並列→統合席（実 ROCm 実行系で無人検分 `probe/d-launch-probe.ps1` 27 段）→敵対検分 3 席（起動・取得・画面）→是正席（high/medium 18 件を直し・xUnit 401 本・検分 31/31・8088/7861 は無傷）。受け入れ条件の実測＝D-1 exit 2 を 1.65 s・D-2 列挙 2.22 s・D-3 追加 2 操作で再起動なし反映・D-4 ログ 4 行・D-5 取得→展開→`._pth`→`import torch` が実弾で exit 0（25,012 檔・903.3 MiB＝assemble-runtime と 1 檔も違わない）・D-6 起動→待機 27.5〜28.9 s・D-7 台本 31 段。**残る low 14 件と卓への票は 87〜88 の便 D（2）で処理**。実機で判った 2 つ（UI Automation は自プロセスの持ち窓と共通檔窓を列挙しないことがある＝Win32 `EnumWindows` を併用・PS 5.1 の `Get-Content` は `-Encoding UTF8` 必須）は便 E の検分台本の前提にする。
87. **便 D（2）の裁定＝⑴ `/ywk/status.memory` の形（裁定 67 ⑶・wrapper に足す）**＝`{"device":"cuda:0"|"cpu"|null,"allocated":int|null,"reserved":int|null,"max":int|null,"gpu_total":int|null,"gpu_free":int|null,"gpu_used":int|null,"latents":{"<話者 id>":bytes,…},"latents_total":int,"sampled_at":"<ISO 8601>"}`。単位はすべてバイト。`allocated`／`reserved`／`max` は torch の allocator（`memory_allocated`／`memory_reserved`／`max_memory_allocated`）、`gpu_total`／`gpu_free` は `torch.cuda.mem_get_info`（ROCm も同じ口）、`gpu_used = gpu_total − gpu_free`＝**カード全体の占有（他プロセス込み）**。device が cpu か未読込のときは数値欄を null にし `latents` だけ出す。`latents` は `latents/<stem>.pt` の実サイズを話者 id で引いた表（焼いていない話者は載せない）。ランチャの表示＝**使用量＝allocated・占有量＝reserved・GPU 全体＝gpu_used／gpu_total**、話者ごとの概算＝焼いてあれば `latents[id]`、無ければ wav 参照の係数（実測前の概算と明示）。**⑵ `device.pci_bus_id` は wrapper が `str()` を掛けて文字列で出す**（ROCm は int を返す・契約 ⑹ に `string|null` と書く）。**⑶ 契約の位置レコード 4 つ（`WarmupStartResult`・`PrecomputeStartResult`・`CancelResult`・`DropLatentResult`）に `[JsonPropertyName]` を書く**（SnakeCaseLower 頼みをやめる）。**⑷ vc_redist の起動引数は台帳（`ledger/vc_redist.json` の `silent_args`＝`/install /quiet /norestart`）が正**＝設計書 §6 の `/passive` を台帳に合わせて直す。`VcRedistAction.Unknown` は「入れる」に落とさず利用者に問う（low 7）。
88. **便 D（2）の裁定＝ランチャの変種の門と死活（裁定 80・83 の実装）**＝⑴ **起動前の門**＝GPU 変種（cu130／cu126／rocm）を起動する前に、その変種の `python.exe` で torch を検分し（既存の `TorchGpuProbe`）、`is_available()=False` か `device_count=0` なら**起動しない**で理由 1 行（例＝「cu130 はこの機体で GPU を見られません（ドライバ 537.58・CUDA 12.2）。cu126 か cpu の変種に切り替えてください」）と**勧める変種**（`nvidia-smi` の driver_version が ≥ 580 なら cu130・528.33 以上なら cu126・それ未満か NVIDIA 無しなら cpu）を出す。**cu130 の CPU 転落は禁止**（裁定 83＝最初の合成でプロセスが消える）。cpu 変種は門を通さない。⑵ **ドライバの閾値**＝cu130 ≥ 580.00／cu126 ≥ 528.33。cu126 で 528.33 ≤ v < 537.58 は「未実測の帯」と状態帯に 1 行出して合成は許す（裁定 80）。⑶ **死活**＝Ready 後にサーバの子が消えたら（exit code が取れた瞬間）状態を Failed に落とし、`ServerExitCodes.Describe`＋stderr の末尾 1 行で理由を出す。合成中の消失は「試す」画面にも「サーバが落ちました（exit −1073741819）」と出し、HTTP の timeout を待たない。見張りは `ServerProcess` の 1 本に寄せ、窓は最新の `StatusResponse` を読むだけにする（low 3）。⑷ **low 14 件は全部直す**（1〜9・11〜14 はランチャ・10 は `ledger/README.md` §7 を 5.26 GiB に）。⑸ **卓への票**＝`build/assemble-app.ps1` が `voices/presets/*.wav` と `voices/presets.json` を配布樹に写す行を足し、`build/verify-runtime.ps1` か check-tree 側に「配布樹にプリセット 11 檔＋presets.json が在る」検査を 1 本足す（便 A の欠け＝この便で埋める）。`CUDA_DEVICE_ORDER=PCI_BUS_ID` の 2 台構成の実射は便 E（RTX 機）へ。
89. **便 E（インストーラ）の設計を取り込んだ（2026-09-05 08:1x・`docs/design/ben-e-installer.md`・`docs/install.md` を確定文に・`installer/README.md` §3 を「決めたこと」に）**＝読み手 2 席→設計案 3 本（最小／寿命／検分）→審査 2 席→統合筆記。軸は**検分案**（この機体で Inno 6.7.3 を実射して 8 つの事実を逐語で取った唯一の案＝`User privileges: None`・`Administrative install mode: No`・`Install mode root key: HKEY_CURRENT_USER`・`Removed all? Yes`・抑止された問いは既定 No・`usPostUninstall returned 7`・起動した setup の pid には窓が無く子の `*.tmp` が持つ・Inno の `TNewButton` には UIA の InvokePattern が効かず `BM_CLICK` で押す）。寿命案から「2 つの樹の専管」（`{app}`＝インストーラ・`%LOCALAPPDATA%\irodori-tts-ywk\`＝ランチャ＝更新でモデルを再取得しない）・`[InstallDelete]` 4 行（server／ledger／licenses／voices\presets）・アンインストール 1 段目の種跨ぎ抑止・vc_redist の UAC 1 回の但し書きを、最小案から docs の明示列挙（acceptance／contract は配らない）・空き容量の門の 2 点是正（見るのは `{localappdata}` のドライブ・警告のみ）・公式版 Inno の導入を接いだ。退けた物＝歓迎頁の文言（`DisableWelcomePage` 既定 yes）・`[UninstallDelete]` に `{app}` 樹ごと・アンインストール 2 問目の削除・既存 probe ヘルパでの Inno 操作・8 段を今日全部・junction の実射・`AppMutex` の `Local\` の断定。
90. **便 E の裁定（卓への問い Q-E1〜Q-E3 への回答＝既定どおり）**＝**Q-E1＝版は 2 本**（`-cuda`／`-radeon`・AppId 別・導入先別・同居可。1 本では `ReleaseFlavor` の「runtime-rocm-* が 1 件でもあれば Radeon 版」により CUDA 機で cu130／cu126 が UI から消える）。`installer/README.md` §1 の「1 本」は「版ごとに 1 本」に読み替え。**AppId の GUID（永久・アンインストール鍵の名）＝CUDA `{F228543A-DCF9-45A3-8826-7485C81E1757}`／Radeon `{ECA98712-1574-4D2A-A1FE-0FF5347BF185}`**。**Q-E2＝`docs/acceptance.md` サイズ行の読み方**＝「実行系 ≤ 2.2 GiB・モデル ≤ 3.4 GiB」は**落とすバイト**で読む（cu130 台帳 1.90 GiB／モデル 3.33 GiB で満たす）。「導入後 ≤ 7.0 GB」は展開後の実測（runtime-rocm 4.32 GiB＝台帳の 3.02 倍・runtime-cpu 3.60 倍・`FetchPlanner.ExpansionFactor=3.3` と整合）から **Radeon 版は 8.3 GB で超える**ので、cu130 の展開後を RTX 機で 1 回測ってから条件値を裁定し直す（月曜以降）。実測を待たずに効く手当て＝**cache（cu130 で ≈1.93 GiB）は初回の合成が 200 で返った後にランチャが消す**（便 D（3）へ）。acceptance.md の行の書き換えは便 D（2）の着地後に本席が行う。**Q-E3＝この機体（Radeon・司令官の常用機・月曜に移行）では管理者昇格も再起動もしない**（司令官の「Windows ごと壊してよい」は RTX 機だけの裁定＝20）。よって⑴ 月曜までにこの機体で撃つ不可逆 2 件＝**Radeon 版 setup の実射（E-4〜E-7）**と**本物の NVIDIA 無し機での W-1＝cu126 の CUDA 版インストーラをこの機体に入れて初回取得（外部取得 2.57 GiB）→ CPU で 200 が返るか**（acceptance §3 の 6 を本物で埋める・cu130 は裁定 83 で既に材料あり）。⑵ **E-2（日本語と空白のユーザ名）は RTX 機で月曜以降**（ローカルユーザ「テスト 太郎」の作成＝壊してよい機体）。⑶ **E-1（vc_redist 欠落機）は司令官の裁定待ち**＝RTX 機の `msvcp140.dll` を退避する許可（U-8 の問いは「次の指示を待て」で閉じている）か、本物の欠落機の用意か。それまで E-1 は未確認のまま残す（`docs/acceptance.md` §3 の 3）。
91. **便 E の実装順（設計書 §10）**＝⑴ `installer/irodori-tts-ywk.iss`（UTF-8 BOM・CRLF・ISPP の /D 7 本＝AppVersion・AppVersionNumeric・Flavor・SrcApp・SrcExe・Repo・OutDir）⑵ `build/installer-build.ps1`（ASCII・CRLF・PS 5.1／7＝assemble-app → release-build -SkipZip → 門 A 6 本 → ISCC → 門 B 3 本 → sha256・2 版の初回ビルドで門 B-2 のサイズ帯を実測で確定・`AppDisplayVersion` の v 前置に注意）⑶ `probe/e-install-probe.ps1`（Inno 用の 4 関数は台本の中・`probe/common.ps1` は触らない・**この機体で段 1・2・3・5・6・7・8**）⑷ 実射の記帳を設計書の末尾に ⑸ RTX 機（月曜以降）＝Inno 6.7.3 を公式インストーラで導入（N: で写さない＝裁定 22）・cu130／cu126 で「導入→初回取得→起動」を時計付きで 1 周（≤ 10 分）・cu130 の展開後の実サイズ・`CUDA_DEVICE_ORDER=PCI_BUS_ID`。**便 D（3）への票**＝`settings.json` に `runtimeLedgerSha256` と `installedAppVersion` を焼き、起動時に配布樹の台帳と食い違ったら「実行系を組み直す」1 手を出す（`AppPaths` は python.exe の在否しか見ない）・cache の削除（90 Q-E2 ⑶）。**便 E の実装は便 D（2）の着地後**（build/out/launcher と build/ を共有するため）。
92. **便 D（2）を取り込んだ（2026-09-05 09:3x）**＝実装 4 席（wrapper／起動席／画面席／工具席）→統合席→敵対検分 3 席→是正席（high 1・medium 8 をすべて実機で再現してから直した・誤りだった所見 0）。本席の再検証＝`dotnet build` 警告 0・xUnit **499**・契約テスト **339**（316＋memory 22＋pid 1）・check-tree／check-licenses 緑・無人検分 `probe/d-launch-probe.ps1` **58/58**（窓と発行 exe の 2 回）・release-build exe 69,610,933 B。**入った物**＝⑴ `GET /ywk/status.memory`（裁定 87 ⑴ の 10 欄・失敗時のみ `error`・所要 median 2.0 ms・gfx1151 の実射＝`allocated` 1.85 GB／`reserved` 2.21 GB／`max` 4.00 GB／`gpu_total` 107.09 GB＝**共有プール**・`latents` は話者 id で引ける表）と **`pid` 欄**（見張りが自分の子か突合するため・欄が無い古い個体は突合しない）⑵ `pci_bus_id` は `str()`（実射 "197"）＝**torch は CUDA でも ROCm でも int を返す**ので契約 ⑹ の「CUDA は文字列」は撤回（設計書 §15-1 の 1 行は §20-1 ⑼ で訂正）⑶ **変種の門 `VariantGate`（純関数）**＝cu130 は `is_available=False`／検分が読めない・撃てない・落ちた、のどれでも起こさない（裁定 83）・畳んだ名 `cuda` には cu130 の閾 580.00 を掛ける・cu126／rocm は検分が読めなくても注意 1 行で起こす・cu126 の 528.33〜537.57 は「未実測の帯」の注意・組んでいない変種は勧めない・`StartAsync` は例外を投げない ⑷ **死活**＝`Process.Exited` で即 Failed・接続の期限 1.5 s・応答が消えた個体を降ろすまで 18.0 s → 10.5 s・他人の pid が同じポートで答えても採らない・合成中の消失は「試す」が timeout を待たずに告げる ⑸ 見張りは `ServerProcess` の 1 本（`LatestStatus`／`StatusSampled`）・窓の `DispatcherTimer` は消えた ⑹ 状態帯＝使用量＝allocated・占有量＝reserved・GPU 全体＝gpu_used／gpu_total（実射「使用量 2.21 GB／占有量 3.02 GB／GPU 全体 3.46 GB / 99.74 GB（最大 3.64 GB）」）・潜在キャッシュ ON/OFF と焼いた話者数・1 名あたりの概算が主役 ⑺ 取得＝Content-Range の突合・`..` の tar は例外・sha256 の無い item は取らない・無通信の期限 60 s・`WheelInstaller` は消す前に cache を一巡・`.data` の残骸は Fail・vc_redist の Unknown は窓で問う（既定＝入れない） ⑻ `[JsonPropertyName]` を契約の全レコードに ⑼ `build/assemble-app.ps1` が `voices/presets/`（11 wav 32,571,364 B）と `presets.json` を写す・`verify-runtime.ps1` に検査 ⑽ `ledger/README.md` §7 を 5.26 GiB に・設計書 §6 の vc_redist を `/quiet` に。**便 D（3）へ残す low**＝画面席の檔の 6 件（`UiText` の GB/GiB・`（最大 …）` の置き場と `memory.error` の文言・`MemoryStatus.Latents` の JSON null で NRE・`MainViewModel` が状態機械を通さず Failed を書く 2 箇所・「GPU メモリの欄」の適用が次回起動まで効かない・409 待ち行列の取りこぼし）＋ `d-launch-probe` の段 c／g の締め＋ **窓側の GPU 列挙が起こせない python.exe で 60 秒黙る**（模型側は 8 ms で Failed＝`AsyncRelayCommand` の全例外の受け口と `ProcessRunner` の「起こす」段の期限）＋ 裁定 91 の票（`runtimeLedgerSha256`／`installedAppVersion`・cache の削除）。**卓の裁定（残る 1 件）**＝`memory.latents` の鍵は話者 id を素通し（絶対パスの話者 id がそのまま出る）＝**据え置き**（契約 ⑹ 6-1「話者 id で引ける表」を優先。話者 id に絶対パスを入れるのは利用者側の誤用で、ランチャは id を表示名から作るので出ない）。
93. **便 E（2）を取り込んだ（2026-09-05 12:0x）＝インストーラの生産ライン・検分・本物の初回取得**＝⑴ `build/installer-build.ps1`（assemble-app → release-build -SkipZip → 門 A **7 本**（檔数と総バイトの一致・プリセット 11＋presets.json・種別の台帳の組と混入 0・拡張子の白名簿・1 檔 4 MiB 上限・発行 exe の版・名指しの 6 檔の存在）→ ISCC → 門 B 3 本（exit 0 と混入 0・サイズ帯 **77.0〜85.0 MiB**（実測 4 標本の ±5 %）・SHA256SUMS.txt）。exit＝落ちた門の数・例外死は **64**・落ちた setup は棚に残さない・PS 5.1／7 の両方で同一 sha256＝**Inno の出力は同じ入力でバイト再現する**）。本席の再走＝`DONE: 2 installer(s), 20 gate(s), 0 failed.`（cuda 84,986,833 B／radeon 85,001,303 B）。⑵ `probe/e-install-probe.ps1`＝段 1・2・3・5・6・7・8（**72 段**・利用者データ樹 16.99 GB を Rename-Item で退避し finally の先頭で復元＝檔数・バイト・名前と長さの差 0 を毎走検証・アンインストーラの `Log closed.` を待ってから判定＝待たないと問いの答えより先に読む競走が出る）。⑶ **本物の初回取得（E2E）**＝**Radeon 版**＝setup 2.71 s → ウィザード → 取得 16.45 s（新規 8 檔 1.37 GB・線速 83 MB/s）→ 展開 27.25 s（26,523 檔 4.25 GB）→ モデル 3.81 s（既存・再取得 0）→ 起動 39.26 s（ready・bf16・gfx1151）＝**導入〜完了 97.84 s**・「試す」1 射 200・状態帯「使用量 1.92 GB／占有量 2.36 GB／GPU 全体 2.96 GB / 99.74 GB」・潜在 ON（11 名 1.1 MB）。**CUDA 版（この NVIDIA 無し機で cu126）**＝setup 2.74 s → 通知 16,509 字が檔と一致 → 変種 3 択（cu130／cu126／cpu）・ドライバ「読めませんでした」の注意 → 取得 21.42 s（102 檔 2.77 GB・129 MB/s）→ 展開 36.31 s（25,049 檔 4.33 GB）→ モデル 5.67 s → **起動は門が拒否**（`cu126 はこの機体で GPU を見られません（is_available=False・device_count=0）。cpu の変種に切り替えてください。`＝裁定 88 ⑴ どおり・`firstRunCompleted` は焼かれない）。**W-1（acceptance §3 の 6）は本物で埋まった**＝展開した `torch 2.10.0+cu126` を `IRODORI_MODEL_DEVICE=cpu` で直に起こすと `runtime loaded in 10.36s`・`POST /v1/audio/speech` **200・303,404 B の RIFF/WAVE**（fp32・10 steps・RTF 1.44）。⑷ 2 版は AppId も導入先も別で同じ機体に順に入って干渉しない。導入・削除とも UAC 0 回。
94. **便 E（2）の実測が動かす受け入れ条件（`docs/acceptance.md` を本席が改訂）**＝⑴ **導入行「利用者操作 ≤ 6」は未達**＝ウィザードだけで **9 押下**（同意チェック・同意して次へ・変種・取得を始める・「次へ」4・試し撃ちへ）。原因＝`FirstRunViewModel.NextAsync` が取得→展開→モデル→起動の各段で「次へ」を要求する。**裁定＝成功した段は自動で次へ進み、失敗した段でだけ止まる**（便 D（3））＝押下 5 に落とす。数え方は押下のまま（緩めない）。⑵ **「オンライン導入 ≤ 10 分（100 Mbps 級）」は未判定**＝この機体の線速は 0.66〜1.04 Gbps で条件の 7〜13 倍。12.5 MB/s に引き直した算術＝cu126 ≈9.7 分・rocm ≈8.0 分（推測）。帯域を絞った走は RTX 機（月曜以降）。⑶ **サイズ行**＝「落とすバイト」で読む（裁定 90）＝cu126 2.57 GiB＋モデル 3.33 GiB＝5.9 GiB・rocm 4.79 GiB＝**満たす**。**展開後の占有は cu126 8.38 GB／rocm 8.29 GB（cache 抜き）で「導入後 ≤ 7.0 GB」を超える**＝条件を**「展開後（cache 削除後）≤ 9.0 GB」に改める**（実測に 0.6 GB の余裕・cu130 は未実測＝RTX 機で測ってから再裁定）。`FetchPlanner.ExpansionFactor=3.3` は cu126 で過大（実測 1.69・rocm 2.98）＝変種ごとの実測係数に（便 D（3））。cache（cu126 2.77 GB／rocm 1.37 GB）は初回の合成の後にランチャが消す（裁定 90・未実装＝便 D（3））。⑷ **§3 の 6（W-1）＝埋まった**（本物の NVIDIA 無し機・cu126・CPU で 200）。⑸ **§3 の 3（E-1・vc_redist 欠落機）＝未確認のまま**（司令官の裁定待ち＝RTX 機の `msvcp140.dll` 退避の可否・裁定 90 ⑶）。⑹ **未実射で残す**＝cpu 変種を実際に選ぶ初回取得・まっさらな機体でモデル 3.33 GiB を本当に落とす走（RTX 機・月曜以降）・対話での setup（3 クリック）・E-2（日本語＋空白のユーザ名・RTX 機）・cu130 の展開後サイズ。
95. **便 D（3）を取り込んだ（2026-09-05 15:1x）**＝実装 3 席（ランチャ／検分／便 F 申し送り）→統合席（実 ROCm で無人検分 88/88・e-install-probe 72/72・installer-build 20 門・**まっさらなデータ樹からの初回取得（CUDA 版 setup・cpu 変種・モデル 3.33 GiB を本物で取得＝48.8 MB/s）＝押下 5・働く段は自動進行・cache は最初の 200 の後に 0 B・全体 199.6 s**）。敵対検分 2 席は本席の 5 時間枠の上限で落ちた（15:00 JST に復帰）＝**再走して是正を通す**（96）。本席の再検証＝build 警告 0・xUnit **559**・契約テスト 339・check-tree／check-licenses 緑。**入った物**＝⑴ 初回取得の自動進行（裁定 94 ⑴＝押下 9→5・失敗段でだけ「もう一度」）⑵ cache の削除（裁定 90＝試し撃ち 200 の後に `<data>\cache` を置き場ごと空に・手動ボタン「取得キャッシュを消す（n GiB）」・関門＝python.exe の在否と `runtimeLedgerSha256` の一致。**「台帳の item の檔を消す」を「置き場ごと空にする」に読み替え＝追認**）⑶ `settings.json` の `runtimeLedgerSha256`／`installedAppVersion`（裁定 91・台帳が違えば「実行系を組み直す」1 手・焼き印の無い樹は起動時に焼く）⑷ 展開係数を変種ごとの実測に（cu126 1.69・rocm 2.98・cpu 3.60・cu130 は推定 3.3＝見積りに「実測／推定」を出し分け）⑸ 裁定 92 の low 6 件 ⑹ **窓が 60 秒黙る正体＝Windows のハードエラーの窓（「サポートされていない 16 ビット アプリケーション」）が `Process.Start` を止めていた**＝`SetThreadErrorMode(SEM_FAILCRITICALERRORS|SEM_NOOPENFILEERRORBOX)` を掛けて起こす（スレッド単位＝子に広げない）＋「起こす」段の期限 3 s＋`AsyncRelayCommand` の全例外の受け口 ⑺ `docs/ben-f-handoff.md`（本体席への索引＝正典との突合 8 群一致・食い違い 6 件は §10 に）。**受け入れ条件の更新（本席）**＝導入行「利用者操作 ≤ 6」＝**5 押下で満たす**（まっさらな機体・CUDA 版・cpu）・速度行「CPU RTF ≤ 4.0」＝製品の路・40 steps で **3.33**・サイズ行＝cpu の展開後 4.26 GiB。**票**＝`docs/contract.md` ⑻ に `runtimeLedgerSha256`／`installedAppVersion` の 2 行（本席が足す）・展開係数の分母はランチャが展開した樹で測り直す（cpu 3.23 対 3.60）・`ben-f-handoff.md` §10 の正典の綻び 6 件（`/ywk/status` に schema 無し・exposed は 10 件・prefetch.planned の値・抜粋の外 4 欄・`ywk_invalid_voice_id`・DELETE の `cancelling`）は便 D（4）で契約側を直す。
96. **E-1（vc_redist 欠落機）を RTX 機で実射した（2026-09-05 13:2x〜15:1x・司令官が `System32\msvcp140.dll` の退避を承諾）＝経路 ⑴ が本物で通った。** 手順＝遠隔席が CUDA 版 setup を無人導入（exit 0・UAC 0）→ `C:\ywk\models` をデータ樹へ写す → 本席が CRD の管理者窓で `msvcp140.dll`（14.42.34438.0・575,568 B・**NVIDIA のドライバインストーラ同梱の VC ランタイム**）を `.e1-bak` へ退避 → ランチャ起動 → ウィザード（通知 16,509 字が檔と一致・変種 cpu・見積り「取得 3.62 GiB・必要な空き 4.53 GiB」）→ **数秒で UAC「Microsoft Visual C++ 2015-2022 Redistributable (x64) - 14.44.35211」＝1 回・本席が「はい」** → Trail「Visual C++ 再頒布可能パッケージを入れた。」→ 取得 102 件 → 展開 25,012 檔 903.3 MB → モデル（既存・HF からの実体取得 0＝増えたのは `.agent_harnesses.json` 5,698 B だけ）→ **サーバ起動（CPU・`runtime loaded in 51.48s`）** → 「試す」**200・所要 6.38 秒／出力 4.32 秒／RTF 1.48**（10 steps）。vc_redist は `Apply complete, result: 0x0, restart: None`・既存 14.42 を **MajorUpgrade** して 14.44.35211.0（557,728 B）。**所見**＝⑴ **ランチャはログ檔を 1 つも書かない**（`<data>\logs\` は空＝Radeon 機でも同じ）＝画面を見ていない者には理由を後から復元できない＝**便 D（4）の票（ランチャと wrapper の stderr を `logs\*.log` へ）** ⑵ **ウィザード時の GPU 検出が失敗して `gpuUuid=null` で確定していた**（「ドライバの版が読めませんでした」）が、設定タブの「数え直す」は DLL の有無に依らず 93／91 ms で 1 台を返す＝**検出経路が別物か、ウィザードが列挙の完了を待っていない**＝原因未特定（ログが無い）＝**便 D（4）の票（初回ウィザードは変種の段の前に列挙を完了させ、失敗なら理由を Trail に）** ⑶ `nvidia-smi` は `msvcp140.dll` に依存しない（PE インポート＝KERNEL32・SHELL32 のみ・DLL 無しで exit 0／41 ms）＝本席の推測は撤回 ⑷ CRD の鍵盤は**大文字を小文字に写す**（`nvidia-smi -L` が `-l`＝ループになった）＝台本 §3-6 の注意に足す。**戻し**＝`.e1-bak2`（14.44）を `msvcp140.dll` に戻し、`.e1-bak`（14.42）は残置（版も実体も違う）。データ樹は残す（月曜の 1 周に使う）。**`docs/acceptance.md` §3 の 3＝埋まった**（欠落機で理由が読める形で止まる→自動で直る）。
97. **便 D（3）の敵対検分と是正を再走して取り込んだ（2026-09-05 16:2x）**＝検分 2 席（high 3・medium 7・low 11）→是正席（誤りだった所見 0＝10 件すべて実機で再現して直した）。本席の再検証＝build 警告 0・xUnit **581**・check-tree 緑・是正席の再走＝無人検分 **89/89**（窓と発行 exe）・e-install 72/72・installer-build 20 門（cuda 85,007,352 B／radeon 85,021,827 B）・利用者データ樹は前後で差 0。**直った物**＝⑴ **焼き印を変種ごとの表に**＝`settings.json` の `runtimeLedgerSha256`／`installedAppVersion`（1 組）を **`runtimeLedgers`／`installedAppVersions`（鍵＝変種名）** に置き換え（両方の実行系を組んだ機体で変種を切り替えると偽の「組み直してください」が出て健全な樹を消す穴＝裁定 91 の趣旨に反していた）。1 巡目の 2 欄は読まない（移行しない・焼き印を失った樹は次の起動で焼き直る）⑵ **設定の「適用」がウィザードの成果を巻き戻す穴**＝`CopyInto` から設定頁が編集しない欄（firstRunCompleted・同意 sha256・焼き印・voiceOrder・lastTest*）を外し、ウィザードの後に写しを取り直す ⑶ cache の掃除は**まだ検めていない変種だけが名指す原檔を残す**（cu126 専用の torch wheel 2.41 GiB が守られる・共有 wheel は消す＝置き場は両方通れば空）⑷ 掃除の札は関門を通った後にだけ立てる ⑸ 「版だけ違う」は焼き直して黙る（1 手を出さない）⑹ 焼き印は `*.dist-info` の件数が台帳と合う（展開が完走した）樹にだけ押す（実物 rocm 107/107・cu126 101/101）⑺ 「実行系を組み直す」に取消と進捗帯と代金の告知（「cache に n 件足りません＝x GiB を取り直します」）⑻ ウィザードの最後の段（起動の確認）でも中断が効く・落とし始める前に空き容量を確かめる・「戻る」は変種の段へ・Trail は同じ段を重ねない ⑼ 無人検分の段 h の空の釘（「取得」の 2 字が「取得（モデル）」にも当たっていた）を題の全一致＋段番号で締めた。**契約 ⑻ の 1 行は本席が新しい形に直した。便 D（4）へ残す**＝ランチャのログ檔（96 ⑴）・ウィザード時の GPU 検出失敗（96 ⑵）・展開係数の分母（ランチャが展開した樹＝cpu 3.23）・`ben-f-handoff.md` §10 の正典の綻び 6 件・cu130 の展開後サイズ（RTX）・E-2（RTX・ローカルユーザ＝遠隔席の権限の分岐を台本に先に書く）。
98. **Radeon 版を本席の機体（Radeon 8060S・gfx1151）に本導入した（2026-09-06 20:1x・司令官「Rocm版を今インストールしてほしい」）。** 使った setup＝`build/out/installer/irodori-tts-ywk-setup-v0.1.0-radeon.exe`（85,021,827 B・sha256 `c8111bf0…`＝便 D（3）是正後 09-05 16:13 の組み・`SHA256SUMS.txt` と一致）。無人導入（`/VERYSILENT /SUPPRESSMSGBOXES /NORESTART`）＝exit 0・`User privileges: None`・昇格 0・`%LOCALAPPDATA%\Programs\irodori-tts-ywk-radeon\`（112 檔＝ランチャ 69,624,712 B・`ledger\` 6 檔＝models／python-embed／runtime-cpu／runtime-rocm-gfx1151／vc_redist／README・`docs\` 3 檔・server／licenses／voices）・登録 `{ECA98712-…}_is1`「irodori-TTS for 読み分けちゃん（Radeon 版） v0.1.0」・スタートメニュー 1 本。**データ樹は 09-05 の検分の残り（`%LOCALAPPDATA%\irodori-tts-ywk`・rocm 実行系 107/107・モデル 3.4 GB・話者 12＝latent 22 檔）をそのまま使い、初回ウィザードは出ない**（`firstRunCompleted=true`）。**本席が `settings.json` の `port` を 18098（検分の私設ポート）→ 18088（製品既定・裁定 32）に書き換えた**（ランチャ停止中・原本は `settings.json.pre-install-2026-09-06.bak`・18088 の LISTEN は導入前 0）。起動で焼き印が入った＝`runtimeLedgers.rocm-gfx1151 = ceec8560…`（配布樹の台帳と一致）・`installedAppVersions.rocm-gfx1151 = v0.1.0`。UI Automation で「サーバ起動」→ **49.2 s で ready**（`device.actual=cuda:0`・bf16・HIP 7.15.26333・torch 2.13.0+rocm10.0.0・precompute reused 11/11）→ `/health`・`/params` 200・`/v1/audio/voices` 12 件・`POST /v1/audio/speech` 2 射 200（デフォルト＝出力 6.66 s／所要 10.33 s＝RTF 1.55＝初射の形状罰・つくよみちゃん＝8.45 s／8.73 s＝RTF 1.03）・memory＝allocated 1,970 MiB／reserved 2,340／max 3,418／gpu_used 2,985 MiB。**残し**＝ランチャは起動したまま（サーバ待機・18088）・データ樹の検分の残り＝`runtime\cu126`（展開係数の実測用・この機体では起こせない）と `cache\` 110 檔 3.9 GB（設定タブ「取得キャッシュを消す」で消せる）・`settings.json` の退避 1 檔。**所見**＝⑴ `logs\` は空のまま（96 ⑴＝便 D（4））⑵ `gpuUuid`／`gpuName` は null のまま（ウィザードを通っていない樹＝96 ⑵ と同根か・設定タブ「数え直す」は未押下＝便 D（4）で見る）。便 F（本体の 10 番目のエンジン）はこの導入を相手に撃てる。
99. **`cfg_scale_text` は本体の「感情表現」パラメータ（2026-09-06 20:4x・司令官「"cfg_scale_text" 本体は"感情表現"パラメータとして扱うようにdocを修正」）。** 本体（yomiwakechan2）は AivisSpeech の `intonationScale` を「感情表現の強さ」（0〜2・既定 1.0・`Engines/Aivisspeech/AivisspeechConstants.cs:138`・本体の裁定 10「抑揚と表示しない」）として出している。配布版の 10 番目のエンジンでは**この枠に `irodori.cfg_scale_text` を置く**（Number・0.0〜10.0・step 0.1・既定 3.0＝`/params` の値）。意味＝本文条件（挿した絵文字を含む本文そのもの）への追従の強さ（上流 `docs/parameters.md:129`）＝上げるほど絵文字で指定した表情も強く出る（README「Emoji-based Style Control」）。**直した物**＝⑴ `/params` の記述子（`server/ywk_params.py`）＝`label`「CFG 強度（本文）」→**「感情表現の強さ」**・`description` を感情表現の説明に・`note` に本体の枠を書く（本体は `label` をそのまま `DisplayName` にする＝ここを直さないと申し送りと画面が食い違う）⑵ `docs/contract.md` ⑸ 5-1 の抜粋（**`min` 1.0 は誤記＝実装・gradio とも 0.0**・`label`／`note` を足す）と 5-2 に 1 条 ⑶ `docs/ben-f-handoff.md` §2-6・§4-2・§6（「余力があれば」の 5 欄から `cfg_scale_text` を外して**必ず出す欄**に上げる・残り 4 欄は据え置き）。`caption`（演技指示）と `cfg_scale_caption` は別枠のまま（caption を使うときだけ意味がある）。9 番目のアダプタ（8088）は `cfg_scale_*` を送らない（本体 `IrodoriConstants.cs:343`）＝変えない。ランチャの「試す」タブの欄名 `cfg_scale_text（空＝既定）` は技術者向けのまま。**敵対検分 2 席（整合・意味＝Opus）→ high 3・medium 8・low 6 をすべて取り込んだ**＝⑷ `description` を上流の逐語に寄せた（初稿の「絵文字・記号・文脈で乗る表情の出方を決める」は過大＝上流 `docs/parameters.md:129` は「本文条件への追従の強さ」・絵文字は本文側の事実＝「本文条件（挿した絵文字を含む本文そのもの）への追従の強さ。上げるほど本文どおりに読み、絵文字で指定した表情も強く出る」）＋上流唯一の使いどころ「発音・滑舌が弱いときに少し上げる」を戻した ⑸ **`IsEmotion` は立てない**（→ **100 で覆った＝立てる**）（本体の「感情」見出しの帯は `ParamDescriptor.IsEmotion` で決まり、AivisSpeech の `intonationScale` は立てていない＝揃えるのは表示名だけ。「感情」帯に入れるなら本体席の裁定＋`IrodoriCapabilityBuilderTests.cs:222` の釘の差し替え）⑹ **表示名だけが同じで目盛りは互換でない**（Aivis 0〜2 中立 1.0／Irodori 0〜10 中立 3.0・本体にマスター感情の写像は無い）⑺ **0 は本文 CFG の枝そのものを外す**（上流 `rf.py:264` `has_text_cfg = cfg_scale_text > 0`＝表情が薄くなるのではなく本文への追従が弱る）＝`note`・契約・申し送りに警告＋実用下限 1.0 付近 ⑻ 既定 3.0 は焼かない（`IRODORI_DEFAULT_CFG_SCALE_TEXT` で動く＝本体は `/params` の `default` を読む）⑼ `group` は配布版自身の UI 用＝本体は帯の判断に使わない（`caption`・`cfg_scale_text`・`cfg_scale_caption`＋`max_caption_len` が `emotion`）・`description` は `NoteToolTip` に写してよい（ただし本体の Number の帯は現状 Note を出さない＝出したいなら本体側の 1 手）・`note` は写さない（実装席向け）⑽ **本体の裁定 6（露出 5 本・`cfg_scale_*` 非露出＝`Engines/Irodori/IrodoriConstants.cs:343`）はこの欄・`num_steps`・「余力があれば」行の `cfg_scale_caption`・`cfg_scale_speaker`・`t_schedule_mode` について改訂が要る**と申し送り §6 に明記（`sway_coeff` は 343 行の列挙に無い・10 番目のエンジンの話）⑾ 散文の「9 欄」を **8 欄** に直した（`contract.md` ⑸ 5-2・`acceptance.md` API 行・`ywk_params.py` の docstring＝申し送り §10 ⑵ の票は閉じた・便 D（4）の票から 1 件減）⑿ `docs/design/ben-a-skeleton-and-build.md` の抜粋の `min` 1.0 → 0.0（契約の誤記の出所）⒀ 契約テストに `label`「感情表現の強さ」と description の要点を固定する 1 本を足した＝**340**。**再検分 1 席（Opus）＝閉 16／開 1／新 7 を取り込んだ**＝⒁ `acceptance.md` API 行の「残り 35 欄」は 9→8 の直しで算数が崩れた（8＋35≠44）＝実装から数え直して **残り 36 欄＝`nullable: true` 18・実効既定 18** に ⒂ 本体の Number の帯は Note を出さない（`TextParamViewModel.cs:82-83`）を契約と申し送りに ⒃ 裁定 6 の改訂範囲を「余力があれば」行の 3 欄まで広げた ⒄ `emotion` 群は `max_caption_len` を含む 4 欄 ⒅ §10 の残票は 5 件 ⒆ 契約 5-2 に `description`／`note` の行き先を写した ⒇ 本裁定の頭の「意味」を上流の逐語に直した（初稿の語が残っていた）(21) `build/out` と導入済み Radeon 版の旧 label＝申し送り §6 に注意書き。**据え置き**＝ランチャの「試す」タブの欄名（技術者向け＝申し送りに明記）・`research/lab/notes/05-params-inference.md` の「追従強度（発音の明瞭さ）」は日付つきの記録で、直した description とも矛盾しない。**注意**＝この機体に入れた Radeon 版（98）と `build/out` の組み上げ済み樹の `/params` は旧 label「CFG 強度（本文）」のまま（次の `assemble-app.ps1`→組みで変わる・便 F の席が叩くなら組み直してから渡す）。
100. **`IsEmotion` を立てる（2026-09-06 21:4x・司令官「IsEmotionを立てる。」＝99 ⑸ を覆す）。** 本体は 10 番目のエンジンの `cfg_scale_text` の記述子に **`IsEmotion = true`** を置き、「感情」見出しの帯に出す（本体 `VoiceEditorViewModel.cs:230-232` の振り分け＝`IsEmotion ? EmotionParams : BasicParams`）。表示名「感情表現の強さ」は AivisSpeech の `intonationScale` と同じだが、帯は違う（Aivis は基本帯）。9 番目（8088）のアダプタと `IrodoriCapabilityBuilderTests.cs:222`（全欄 `IsEmotion=false`）は 9 番目の話＝10 番目が別 builder なら触らない・共有するなら釘を差し替える。**直した檔**＝`server/ywk_params.py` の `note`・`docs/contract.md` ⑸ 5-1 の抜粋と 5-2・`docs/ben-f-handoff.md` §2-6・§6・契約テストの固定語（「IsEmotion を立てて」）＝**340**。手続き（鍵 `cfg_scale_text`・値域・既定・`schema: 1`）は 99 と同じく不変＝変わるのは `/params` の表示用文字列だけ。
101. **10 番目は 9 番目（8088）とあくまで別物として扱う・本体の実装に備えて環境を更新した（2026-09-06 22:1x・司令官「あくまで別として扱う。本体の実装に備えて環境の更新まで委任する。」）。** ⑴ **別物**＝10 番目のエンジンは別 builder・別テスト＝9 番目の `IrodoriCapabilityBuilderTests.cs:222`（全欄 `IsEmotion=false`）には触らない（100 の「共有するなら差し替える」は消える）＝申し送り §6 に明記。⑵ **環境の更新**＝`build/installer-build.ps1 -All` を全段で再走（assemble 108 檔・publish・門 A 7＋門 B 3 ×2 種＝**20 門 0 失敗**・A-1 は総バイトの drift の WARN＝37,111,434 → **37,112,395 B**＝記述子の文言分。台本の `$ExpectedAppBytes` を新しい値に更新）→ **cuda 85,005,985 B sha256 `ffd35375…`／radeon 85,020,453 B sha256 `6a1d95f4…`**（22:10）。⑶ この機体の Radeon 版を**上書き導入**（無人・exit 0・昇格 0・旧の登録鍵を Inno が消して再登録・112 檔・`server\ywk_params.py` は新 label）→ ランチャ起動 → 「サーバ起動」**70.8 s で ready**（rocm-gfx1151・cuda:0）→ 実物の `/params`＝`label`「感情表現の強さ」・`note` に「IsEmotion を立てて」・`rules.exposed_to_ywk` 10 件・`POST /v1/audio/speech` 200（出力 6.49 s／所要 8.92 s＝RTF 1.38）。**ランチャは起動したまま（18088 で待機）＝本体席はこの実物を叩ける。** ⑷ **N: の RTX 向け配布物を差し替え**＝`N:\temp_for_claudecode_agents\irodori-ywk\rtx-handoff\installer\` に cuda・radeon の両 setup と `SHA256SUMS.txt`（sha 一致を確認・E-1 で使った旧 cuda `639e60ef…` は上書き＝E-1 の記録は 96 に残る）。⑸ 申し送り §6 の「組み直し前の樹は旧 label」の注意書きを、新しい組みの事実（時刻・サイズ・sha）に置き換えた。データ樹は 98 のまま（cache 3.9 GB・`runtime\cu126` は残置）。
102. **便 F（本体席 `yomiwakechan2-e5`）からの初回問い合わせと回答（2026-09-06 22:5x・席間メッセージ）。** 本体側の報告＝着手済み・`docs/contract.md`（ac7f5e3）を本体 `docs/irodori-ywk-contract.md` へ逐語で写し、18088 の実応答（`/params`・`/ywk/status`・`/ywk/voices`・`/v1/audio/voices`・`/health`・400 の body 9 種）を本体の契約テストの fixtures に採った。本体側の裁定 12 項（別物＝`Engines/IrodoriYwk`・ID `irodori-ywk`・表示名「irodori-TTS 配布版」・発見 4 段・ready 待ちのループ無し・`LaunchKind.None`・話者は `/ywk/voices` の data・パラメータは `/params` から動的・seed だけ Text・`cfg_scale_text` は label＋`IsEmotion=true`・音量は本体で wav に乗算・実効値を常に送る・`error.code` で分岐・pid 突合／SSE／暖機／事前計算／prefetch は叩かない）＝**正典と食い違い無し**。実射の事実（seed の `ywk_type_error`・`2147483648` は 200・`num_steps` 10.0 は type_error・none 別名と voice 省略は 200・全 4xx に code）＝契約テストと一致。**回答**＝⑴ **`schema`≠1 は「契約の版が違う」で発見を失敗させてよい**（契約 ⑻＝欄を消す・意味を変えるときだけ上げる。上げるときは配布版が正典・契約テストの題目・本体席への通知の 3 つを行う）。label・description・note は表示用＝schema の外＝fixtures では「データ」扱いを勧めた。便 F の間に `/params` を動かすときは事前に本体席へ知らせる（表示文字列も）。⑵ **無人でサーバを起こす口＝`settings.json` の `autoStartServer`（コード既定 true・設定タブ「起動時にサーバを起こす」）だけ・CLI 引数は無い**。true なら `IrodoriTtsYwk.Launcher.exe` を起こすだけでサーバが起きる（初回取得が未完ならウィザード）。単一起動＝Mutex `Local\irodori-tts-ywk-launcher`（2 本目は既存の窓を前に出して退く）。起動時に窓が出る。ポートはモデルを載せてから開く（preload=true・裁定 7）。**本体の元栓（一括起動）から配布版を起こす席は裁定 6（起動主体＝配布版・本体は `/health` で見つけるだけ）に触れる＝司令官の裁定待ち**（承認なら後便で CLI 旗 `--start --minimized` を足せる。今便は `LaunchKind.None` で正しい）。⑶ 注記＝読込中はポートが閉じているので発見は「未起動」で落ちる＝失敗を焼き付けず再検出できる形に。この機体の設定樹の `autoStartServer` は検分の名残で false。
103. **本体の一括起動（元栓）が配布版を起こす＝従来型 TTS と同じ扱い・配布版側は何も足さない（2026-09-07 00:0x・司令官「一括起動で起こす。従来型のTTSと同じ扱い。」→「本体が設定にしたがって一括起動する。それだけだ。ここは待機。」＝裁定 6 の「本体は `/health` で見つけるだけ」を覆す）。** 本体側＝`LaunchKind.ExePath`（VOICEVOX・COEIROINK と同じ＝パス欄＋参照・`Process.Start`・引数なし）。起こす exe＝`%LOCALAPPDATA%\Programs\irodori-tts-ywk-<radeon|cuda>\IrodoriTtsYwk.Launcher.exe`（HKCU Uninstall `{ECA98712-…}_is1`（radeon）／`{F228543A-…}_is1`（cuda）の `InstallLocation` でも引ける）。ランチャは自分の設定 `autoStartServer`（コード既定 true・設定タブ「起動時にサーバを起こす」）に従ってサーバを起こす。単一起動（Mutex）＝2 本目は既存の窓を前に出して退く＝重ねて起こしても害なし。ポートはモデルを載せてから開く（preload=true・裁定 7）＝本体の `/health` 待ちは 120 s 上限（Radeon 8060S 50〜70 s・3090 20〜28 s）。終了時の扱いは本体の従来型エンジンと同じ（配布版は問わない）。常駐と後始末（wrapper のツリー kill）は引き続きランチャ。**設計席が一度 CLI 旗（`--start`／`--minimized`／`--exit`＝便 G）を本体席へ打診したが、司令官が「何もしなくていい」と裁定＝取り消して本体席へ訂正を送った。** この機体の設定樹の `autoStartServer` を検分の名残の false から製品既定の true に戻す。102 の「裁定待ち」はこれで閉じる。**実射（この機体・Radeon 版・autoStartServer=true）**＝古い個体を片付けてから引数なしで exe を起こす→ **35.2 s で `/health` 200**・`/ywk/status.pid` の親はそのランチャ（wrapper 経由）。注意＝ランチャを強制終了（taskkill）すると後始末（wrapper のツリー kill）が走らず古いサーバが 18088 に残る＝終了はトレイの「終了」か窓の閉じ方に従うこと。配布版席は待機。
104. **便 F 完納の報と、契約文書への票 3 件の適用（2026-09-07 00:4x・本体席 `yomiwakechan2-e5` から）。** 本体側＝実装完納・実走 smoke 合格（18088 の実物でスキャン成功・3 発 Completed・停止時の失敗の顔＝2.1 s で確定（接続拒否）・「サーバ停止→サーバ起動」で `/health` 200 まで 32.6 s（温間）・元栓の重ね起動が無害）・本体の PR を提出する段。本体の写し `docs/irodori-ywk-contract.md` は原本の逐語＝本体側では直さない。**票の適用（本席）**＝⑴ 契約 ⑵ の 4 段目に「表示名と理由が要るなら `GET /ywk/voices` でもよい＝本体はこちら」を足した ⑵ 契約 ⑻ の「`schema` は `/params` と `/ywk/status` に載る」を「`/params` の応答に載る（`/ywk/status` には載らない）」に直した（申し送り §10 ⑴＝閉） ⑶ 契約 ⑶ 3-3 の code 表に `ywk_invalid_voice_id` を 1 行足した（上流の登録 API の札＝配布版は 3 口を外しているので本体には届かないはず・網羅表に載せるのは `default` 分岐に落とさないため＝申し送り §10 ⑸＝閉）。**あわせて裁定 103 を正典に写した**＝契約 ⑵「起動主体は配布版＝本体はサーバを起こさない」→「常駐と後始末はランチャ・本体の一括起動は引数なしで exe を起こしてよい」・申し送り §0 の 1 段目／§1-3／§7 の 5・README・`App.xaml.cs` の説明文（挙動は不変＝組み直し不要）。申し送り §10 の卓への票は残り 3 件（⑶ prefetch.planned の値・⑷ 抜粋の外 4 欄・⑹ DELETE の `cancelling`）。契約テストは触っていない（文書と説明文だけ）。本体席へ「契約 ⑵⑶⑻ が動いた＝写しを採り直すなら今」と知らせる。
105. **便 G＝ポート先行（bind してから裏でモデルを載せる）に変える（2026-09-07 01:5x・司令官「変えよう。」＝裁定 7 の `preload=true`「載せてから bind」を覆す）。** 由来＝本体席経由の司令官の意向「まだ新しい TTS は実績がないし、先にポートを開かせて疎通を確認した方がいい。モデルを載せるのを待つ段はスキャンで良い。そこで失敗を知れば本体は通過。」（本席が司令官に上げ、承認）。**設計**＝⑴ wrapper が焼く `IRODORI_PRELOAD` を `false` にし、`ywk_lifespan`（上流 startup の直後＝bind の前）で**読込の糸**を起こす＝上流 `runtime_manager.get()` を裏で呼ぶ。`/ywk/status.runtime`＝`loading=true → loaded=true`・失敗は **`error` に理由**（プロセスは生かす＝本体とランチャが理由を読める）。欄の形は不変＝**`schema: 1` のまま**。⑵ 事前計算（rocm の既定 ON）・暖機・`note_device_once` は**読込の成功後**に起こす（今は startup 直後＝載った前提）。⑶ 読込中に合成要求が来たら上流の遅延読込に合流して待つ（本体は `loaded` を見てから撃つ）。⑷ ランチャ＝Listening→Ready の昇格は今のまま `runtime.loaded` の標本・**`runtime.error` の標本で Failed（理由つき）**へ・「サーバ停止」は Failed でも効く（既存）。⑸ 本体の元栓＝完了検知は疎通（`/health` 200）だけ・スキャン＝`runtime.loaded` を 2 s 間隔で最大 120 s・`error` か時間切れで理由つき除外（本体席が実装中・現行の配布版でも無害）。**書き換える正典**＝契約 ⑵（ready 判定・:36・:54）・⑹ の pid の註（:498）・⑶ 3-4 の註（:571-575）・`acceptance.md` 安全行（`preload=true` を焼く→`false`＋裏読込）と起動行・`install.md` の起動行・申し送り §0 の 5 段目／§1-2／§1-4・設計書 `ben-d-launcher.md` §25・契約テスト（`test_env_and_devices` の `PRELOAD`・`test_status` の註・読込の糸のテスト）・無人検分の該当段。**受け入れ**＝引数なしで exe を起こして `/health` 200 まで **≤ 3 s**（bind 先行）・`runtime.loaded` まで従来どおり（Radeon 35〜70 s・3090 20〜28 s）・読込失敗の個体で `/health` 200＋`runtime.error` に理由＋ランチャの状態帯に同じ理由。組み直してこの機体に入れ直し、本体席へ知らせる。
106. **便 G（ポート先行）を取り込んだ（2026-09-07 03:3x）＝実装席 1（24 檔）→敵対検分 2 席（高 4・中 8・低 6＝18 件すべて再現して是正）→是正席→本席の是正 1 件。** 本席の再検証＝契約テスト **354**（+14＝`test_runtime_loader.py` ほか）・launcher build 警告 0・xUnit **592**（+11）・無人検分の台本は parse 0 エラー（全段は未走）。**入った物**＝⑴ wrapper＝`IRODORI_PRELOAD=false`＋`ywk_lifespan` の読込の糸（上流 `runtime_manager.get()`＝同じ錠に合流するので二重読込なし・成功後にだけ事前計算／暖機／device の 1 行・失敗は `_one_line_reason` で 1 行に畳み絶対パスを洗って `runtime.error` へ・プロセスは生きる・stderr 3 行「runtime load started／runtime loaded in／runtime load failed」）⑵ `_runtime_error` の遅延書き手を「載らなかった」だけに狭めた（合成 1 回の 5xx で健全な個体が「読込に失敗」を名乗る穴＝検分 high）＋パスを洗う ⑶ ランチャ＝`ReadinessSample.RuntimeError`・`ApplyReadiness` は `loaded=false`＋`error` で Failed（理由「モデルの読込に失敗＝…」・`loaded=true` の標本では絶対に落とさない二重の閂）・ready 待ちは失敗の標本で即返す ⑷ **本席の是正＝読込に失敗した個体は起動の待ちの最中でも殺さない**（是正席は「止めろと言われる前に止める」を掛けて子を落としていた＝本体には接続拒否しか届かず裁定 105 の眼目が消える）。`ServerProcess.IsAliveLoadFailure`（理由の接頭辞・終了コード無し・生存）で見分け、ポートを閉じるのは「サーバ停止」（Failed でも押せる）。「止めろと言われる前に止める」は答えない個体（期限切れ・bind 失敗）にだけ ⑸ 正典＝契約 ⑵／⑶ 3-4／⑹・受け入れ 起動行と安全行・`install.md`・申し送り §0／§1-2／§1-4・設計書 §25・`radeon.md` の註・README。**実射（この機体・Radeon 版・入れ直し後）**＝⒜ 引数なしで exe を起こす→ **`/health` 200 まで 8.9 s**（ランチャの門 5.5 s＋wrapper の bind 3.3 s＝torch の import の後）→ `loading=true` の窓を観測 → **`loaded=true` 34.5 s** → 状態帯「暖機中」（rocm の事前計算）→ speech 200（RTF 1.45）⒝ wrapper を私設ポート 18097 で直に起こし偽の checkpoint＋HF offline＝**3.6 s で `/health` 200 と同時に `runtime.error`**（`LocalEntryNotFoundError …`・絶対パス無し）・プロセスは生存・`/health` は 200 のまま・stderr 3 行のとおり。**105 の「≤ 3 s」は wrapper 自身の bind の見積り**＝物差しを「exe 起動から」に揃えて **≤ 10 s（実測 8.9 s）** に直した（契約・申し送り・`install.md`・受け入れ条件）。組み直し＝両 setup 03:13（cuda 85,009,462 B `2e7914de…`／radeon 85,024,071 B `9cc4d65a…`・A-1 の記録値 37,125,613）・この機体に上書き導入（exit 0）・N: の RTX 向け配布物を差し替え・ランチャは 18088 で待機のまま。`docs/irodori-ywk-contract.md`（本体側の写し）が本リポに未追跡で在ったので消した＝写しは本体リポの物。本体席へ「契約 ⑵・⑶ 3-4・⑹ の文言が動いた＝写しを採り直すなら今」と知らせる。
107. **便 F（本体側）クローズの報（2026-09-07 04:0x・本体席 `yomiwakechan2-e5`）。** 提出済み＝本体 PR **#512**（10 番目のエンジン `irodori-ywk`・便 G の実射込み）→ **#513**（スキャン進捗の小窓＝配布版の「モデル読込中（N 秒）」を行に出す）→ **#514**（元栓の席の並び＝Irodori-TTS→配布版→COEIROINK 優先）の stacked 3 本。本体の契約の写しは **98d8888 版**（便 G 反映）。以後の契約変更は本体側の次の席へ知らせる（宛名は同じ `yomiwakechan2-e5`）。配布版側の残り＝便 D（4）の票（ランチャのログ檔・ウィザード時の GPU 検出・展開係数の分母・申し送り §10 の残票 3）・月曜の RTX の 1 周（cu130／cu126 の導入時間・cu130 の展開後サイズ・`CUDA_DEVICE_ORDER`・E-2）・プリセット二次 wav の試聴（司令官が延期）。
108. **プリセット話者「もち子さん」を「もち子さん（セクシー／あん子）」に改名（司令官の指示・2026-09-07 16:0x）。** 動くのは `display_name` だけ＝`engine_speaker`（VOICEVOX の話者名「もち子さん」）・style（セクシー／あん子・styleId 66）・id `vv_mochiko_sexy`・二次 wav（md5 `0d2e0175…`・2,953,004 B）は据え置き。**表示名はそのまま話者 id である**（裁定 17・契約 ⑷ 4-1）ので、本体に見える話者 id の変更でもある。**引き継ぎが要る理由**＝正本 `voices/presets.json` だけを直しても既存の利用者の台帳（`voices.ywk.json`）は旧い id を持ち続け（`InstallIfFirstRun` は `presets_installed` の印で 0 件）、「入れ直す」は新しい名を旧い名の隣に足して同じ wav を指す 2 名になる。**入れた物**＝⑴ ランチャ `PresetVoices.MigrateRenamed`（規則＝正本 ⑴ `presets.json` から読めたときだけ〔⑵ 台帳・⑶ 檔名走査の id では改名しない〕・台帳のプリセット行〔`preset`／`origin=preset`〕で檔名を持ち id が正本に無い行を、檔名が一致する正本の行の新 id〔台帳に未在〕へ改める・同じ檔を指す 2 行は序数で先頭の 1 つだけ・利用者の行と空白の鍵は触らない。持ち越す＝檔・caption・既定・preset・origin・added_at。捨てる＝`ref_latent` と旧 id の `latents/<stem>.pt`＋sidecar〔`VoiceStore.DeleteLatentFiles`＝削除と同じ 1 つの規則〕。**参照 wav は消さない**。順は台帳の保存→潜在の削除〔逆だと保存が落ちた回に `ref_embed` 持ちの行が残って二度と焼き直らない〕）⑵ 走る場所＝起動の `PrepareVoices`（移送→**改名**→初回展開→`voices.json`）と「入れ直す」（改名を通してから `Restore`・文言に「名を改めた N 名（旧→新）」）⑶ 設定の `voiceOrder`／`warmupVoices`／`lastTestVoice` の旧 id も同じ回で改めて `settings.json` に落とす（暖機は知らない話者で走行ごと failed になる＝`_warmup_run_shots`。「入れ直す」側は `VoicesViewModel.SettingsChanged` で `MainViewModel` が保存＋設定頁の写しの取り直し＝取り直さないと「適用」で旧 id が甦る）⑷ 正典＝契約 ⑷ 4-1 に箇条「プリセットの表示名（＝id）は版で変わりうる・旧 id は一覧から消え送れば `ywk_unknown_voice`・版をまたいで id を固定せず一覧を読み直す」と、見本の行を `{"id":"琴葉茜（関西弁）","display_name":"琴葉茜（関西弁）"}` に（出荷の行は id と display_name が同じ字）。申し送り §3-1 も同じ。設計書 ben-p（規則と表）・ben-d §4（引き継ぎ）・試聴 §1・schema・生成器 2 本（`make_presets.py`／`gen_voicevox.py`）。**席の経過**＝実装席 1→敵対検分 2 席（高 1・中 6・低 11＝18 件）→是正席。検分が捕まえた要点＝⒜ 本席が渡した前提「旧 id は `ywk_invalid_voice_id`」が誤り（正しくは `ywk_unknown_voice`＝⑶ 3-3 の 1 行目）→契約・申し送り・テストの docstring を是正 ⒝ 潜在の削除が台帳の保存より前だった ⒞ 空白の鍵で起動が止まる ⒟ 「入れ直す」側で設定が檔に落ちず「適用」で巻き戻る。**検証**＝契約テスト **360**（+6＝`test_presets_manifest.py`＝12 行・id／表示名の一意・改名の釘・生成器との突合・二次 wav の実在と大きさ）・xUnit **603**（+11＝`PresetRenameTests.cs`）・build 警告 0・installer-build -All 20 門 0 失敗（A-1 の記録値 37,125,613→**37,125,643**＝+30 B は「（セクシー／あん子）」10 字×3 B）・両 setup 16:46（cuda 85,012,778 B `d14729af…`／radeon 85,027,377 B `ad1d56ff…`）・N: の RTX 向け配布物を差し替え。**実射（この機体・Radeon 版・旧名の台帳と旧 id の潜在 `ywk-75b15fb1eea7` を持つ状態から入れ直し→引数なしで exe）**＝起動直後に台帳は 12 名のまま新名だけ（caption／added_at 持ち越し・`ref_latent` null・旧 .pt＋sidecar 消滅・wav 残存・`voices.json` の鍵も新名だけ）→ `/health` 200 は exe から 11.2 s（入れ直し直後の cold＝106 の 8.9 s より遅い・契約の目安は動かさない）→ `loaded` 67.6 s（同じく cold・106 は 34.5 s）→ その 2.2 s 後に起動時の事前計算が新 id の `ywk-31ddd9fd2d29.pt` を焼く（sidecar の voice_id＝新名）→ `/ywk/voices` 12 名・新名の行 `latent=true`・旧名なし→新名で speech 200（RTF 0.88）・旧名で **400 `ywk_unknown_voice`**（契約の文言どおり）。ランチャは 18088 で待機のまま。改名前の台帳の写し＝scratchpad `ledger-backup-20260907-164849`。本体席へ知らせる＝契約 ⑷ 4-1（箇条＋見本の行）が動いた・写しを採り直す・話者 id「もち子さん」は存在しなくなった。

109. **CUDA 版と ROCm 版の見せ分け（司令官の指示・2026-09-07 21:0x）＝アプリ名・既定の導入先・窓題を版で分ける。**逐語＝「『CUDA版とROCm版の区別』インストーラーが別のはずだが、アプリ名称も(CUDA版)(ROCm版)としてデフォルトインストールフォルダも分けたい。yomiwakechanからは無いが、Python無し環境での利用で両方インストールしたい人も居ると思われる。Windowタイトルも別に分ける。本PCにインストールされている版はそのままでよい。」**決めたこと**＝⑴ **表示名は両方に版を足す**＝「irodori-TTS for 読み分けちゃん（CUDA 版）」「…（ROCm 版）」（全角括弧・版の前に半角空白 1 つ）。この文字列が `.iss` の `AppName`／`AppVerName`／`UninstallDisplayName` の頭／スタートメニューの近道の名／ランチャの窓題／「このアプリについて」の見出しに出る。トレイの吹き出しは短い幹で「irodori-TTS（ROCm 版） v0.1.0／待機」（両版×全 ServerState の最長 30 字＝Win32 の 63 字の枠に収まることを xUnit が釘付け）・初回取得ウィザードの題は「初回取得（ROCm 版）」。**利用者に見せる版の名だけ**を「Radeon 版」から「ROCm 版」に改めた（司令官の呼び方＝2026-09-06 の「Rocm版を今インストールしてほしい」と同じ）。ハードウェア／アーキテクチャ／道具の名（Radeon 8060S・gfx1151・`rocm-gfx1151`・`docs/radeon.md` の檔名）は据え置き。⑵ **既定の導入先は CUDA 版を `{autopf}\irodori-tts-ywk` → `{autopf}\irodori-tts-ywk-cuda` に改める**（ROCm 版の `…\irodori-tts-ywk-radeon` は 1 字も変えない）。理由＝本体（読み分けちゃん2）の `UI/Services/Launch/EngineLaunchDefaults.cs:73-75` が既に `Programs\irodori-tts-ywk-radeon` → `Programs\irodori-tts-ywk-cuda` の順で exe を探し、本体側の試験 `IrodoriYwkCompositionTests.cs:50-53` が素の `Programs\irodori-tts-ywk` を候補から**除く**ことを釘付けしている＝改訂前の CUDA 版は本体の自動発見に乗らない食い違いだった。**こちらが本体に合わせる側**であり、本体は無改変でよい。⑶ **内部の識別子は 1 つも変えない**＝Flavor の id（`cuda`／`radeon`）・`/DFlavor=`・setup の檔名（`…-cuda.exe`／`…-radeon.exe`）・`AppId` の GUID（裁定 90＝永久）・`AppMutex`＝ランチャの単一起動の錠 `Local\irodori-tts-ywk-launcher`・台帳名 `runtime-rocm-gfx1151`・データ樹 `%LOCALAPPDATA%\irodori-tts-ywk`・enum `ReleaseFlavor.Radeon`・`Directory.Build.props` の `<Product>`（exe は 1 本で両版を兼ねるので版なし）。⑷ **版はアプリに焼き込まず配布樹から読む**（裁定 5・90 の `ReleaseFlavors.Detect` と同じ）＝`ReleaseFlavors.DetectFrom(paths.LedgerDir)`（読めない樹でも投げず CUDA 版）→ 純関数 `FlavorLabel`／`AppTitle`／`WizardTitle`／`TrayText` が名札を組む。窓題は `MainWindow.xaml.cs` のコンストラクタ、見出しは `AboutView.xaml.cs`、吹き出しは `App.xaml.cs` の `UpdateTray`、ウィザードは `FirstRunWizard.xaml.cs` が差す。⑸ **改名の引き継ぎ**＝既存の導入は Inno の `UsePreviousAppDir`（既定 yes）で**場所はそのまま**（裁定 109 より前に入れた CUDA 版は `…\Programs\irodori-tts-ywk\` に留まり本体の自動発見には依然乗らない＝入れ直すか本体で路を手で指す・`docs/install.md` §4・§5-1）。旧名のスタートメニューの近道は Inno が消さないので `[InstallDelete]` に 5 行目 `Type: files; Name: "{autoprograms}\{#OldAppName}.lnk"`（cuda＝版なしの旧名・radeon＝「（Radeon 版）」）を足した。⑹ **同居の意味**（書き留めただけ・変えていない）＝2 版は `AppId`・導入先が別で同居でき、データ樹は共有。**同時に走るランチャは 1 個体**（同じ錠・同じ 127.0.0.1:18088＝2 個目は 1 個目の窓を前に出して退く）。`AppMutex` も共有なので、もう一方の版のランチャが走っている最中の導入・撤去は「実行中の <これから入れる側の版名> を検出しました」と断られる（既知の癖・害なし・直さない）。**両版を入れた機体で本体が自動で選ぶのは ROCm 版**（候補順 `…-radeon` → `…-cuda` で実在する最初の 1 つ）＝CUDA 版を起こすなら本体の元栓のパス欄に手で指す。候補順そのものは本体側の票。⑺ **本機の導入済み ROCm 版（`Programs\irodori-tts-ywk-radeon`・鍵 `{ECA98712…}_is1`・表示名「…（Radeon 版） v0.1.0」）はそのまま**（司令官の逐語）＝入れ直していない。次に上書き更新したとき表示名と近道が「（ROCm 版）」に変わり、場所は動かない。**入れた物**＝`installer/irodori-tts-ywk.iss`（define 4 本・`[InstallDelete]` 5 行目・`[Messages]`／`[Code]` の版名・`AppMutex` の注釈）・ランチャ 8 檔（`ReleaseFlavor.cs` の純関数 6 本＋`DetectFrom`・`App.xaml.cs`・`MainWindow.xaml(.cs)`・`AboutView.xaml(.cs)`〔見出しに `AboutTitleText`〕・`FirstRunWizard.xaml(.cs)`・`FirstRunViewModel.cs` の VariantNote）・xUnit 7 本（603 → **610**）・`probe/e-install-probe.ps1`（cuda の期待路＝`-cuda`・段 1 に「DisplayName がその版の AppName で始まる」「新しい近道が `<AppName>.lnk`」の 2 本・`Get-ExpectedAppName`／`Get-ExpectedAppDir`）・`probe/rtx-e1-runbook.md`・`licenses/first-run-notices.md`（B 節の見出しと :81＝ウィザードが逐語で出す檔＝同意の sha256 は証跡でしかなく再同意は要らない〔`NeedsFirstRun` は `firstRunCompleted` だけを見る〕）・檔＝`docs/install.md`（冒頭の路・§0 の 2 本と同居の意味・§1／§2／§4／§5-1／§5-4）・`README.md`・`installer/README.md`・`docs/radeon.md`（リリースを指す「Radeon 版」9 箇所）・設計書 `ben-e` §1-1／§1-2／§1-4／§7-2＋新設 §17・`ben-d` 新設 §26・`ben-a` §2。**検分**＝Workflow（実装 1 席 → 敵対検分 3 席〔インストーラ／ランチャ／檔と本体整合〕→ 是正 1 席・全席 Opus 5）＝所見 20 件（high 2・medium 10・low 8）をすべて適用・棄却 0。本席の再検証＝build 警告 0・xUnit **610**・契約テスト **360**（触っていない）・installer-build -All＝**20 門 0 失敗・WARN 0**（A-1 の記録値 37,125,643 → **37,125,639**＝`licenses/first-run-notices.md` の −4 B・檔数 108 は不変）・setup（21:37）＝cuda **85,013,064 B** `ecb7090a…`／radeon **85,027,724 B** `ac225734…`＝N: `rtx-handoff\installer` に差し替え済み（hash 一致）。**実射**（本機・裁定 ⑺ を守って CUDA 版 setup を `/VERYSILENT /SUPPRESSMSGBOXES`・`/DIR` なしで**一時的に**入れて畳んだ・ランチャは `YWK_LAUNCHER_DATA_DIR` で使い捨てのデータ樹を指して起こした＝共有の `settings.json`／台帳は書かない）＝導入 exit 0・`User privileges: None`・HKCU 鍵 `{F228543A…}_is1` の `DisplayName`＝「irodori-TTS for 読み分けちゃん（CUDA 版） v0.1.0」・`InstallLocation`＝`%LOCALAPPDATA%\Programs\irodori-tts-ywk-cuda\`・近道＝「…（CUDA 版）.lnk」1 本（Radeon 版の近道は残る）・cuda の `ledger\` に rocm の台帳なし → 窓題（Win32 EnumWindows で読んだ逐語）＝導入した CUDA 版の exe「irodori-TTS for 読み分けちゃん（CUDA 版）」＋「初回取得（CUDA 版）」／新しい exe を導入済みの Radeon 版の樹（`YWK_LAUNCHER_APP_DIR`）の上で起こして「…（ROCm 版）」＋「初回取得（ROCm 版）」（UIA の根の直下にはウィザードが並ばないので UIA では取れない＝`probe/common.ps1:254` の癖のとおり）・18088 は開かず → 撤去 `/VERYSILENT` exit 0（ROCm 版の鍵が在るので 1 段目は出ず・2 段目は無人で既定「残す」・本文は「ROCm 版がまだこの機体に入っています」の新しい文言）→ 鍵・`{app}`・近道が消え、**Radeon 版の鍵・近道・アプリ樹（112 檔 111,271,704 B）と共有データ樹（54,058 檔 12,858,523,821 B・`settings.json`・`voices.ywk.json` の sha256）は前後で差 0**。本機の Radeon 版はランチャを起こしていない（実射の前から止まっていた・司令官が起こすときは exe を引数なしで）。**残り**＝本体席（yomiwakechan2）へ「CUDA 版の既定の導入先が候補と一致した・表示名が版つきになった・本体は無改変でよい」を通知。RTX 機（月曜以降）の実射は新しい setup で。
110. **GPU メモリの帯は Windows の計数から（司令官の指示・2026-09-08 01:5x〜02:1x）＝専用メモリだけ・TTS エンジンが使っている GPU だけ・複数 GPU は GPU ごと。**逐語＝「GPUの総計メモリが取れてないみたいだね、あとちょくちょくメモリ開放してるっぽい？」→「Windowsの計数を読もうか。複数GPUの場合も想定して適切に対処よろしく。」→「共有メモリは除外。あくまでTTSエンジンが使用しているGPUが集計対象。」**きっかけ（本席の実測・この機体・wrapper pid 31556）**＝帯の「GPU 全体」は wrapper の `gpu_used`＝`torch.cuda.mem_get_info`（HIP）の total − free で、裁定 87 ⑴ は「カード全体の占有＝他プロセス込み」と定めていたが、Windows の ROCm では成り立たない＝同時刻に HIP 3.18〜3.66 GiB／99.74 GiB に対し、OS の計数 `GPU Adapter Memory\Dedicated Usage`（LUID 0x137d0）は **30.07 GiB**（タスクマネージャの専用 29.8 GB と一致）、`GPU Process Memory\Dedicated Usage` は python（wrapper）**10.84 GiB**・llama-server 16.20 GiB・dwm 0.81 GiB。HIP の値は他プロセスを含まず、自プロセスの分すら OS より小さい（torch の reserved 2.34〜2.64 GiB）。共有メモリの使用量（1.0 GB）とも合わない。**「ちょくちょく解放」の実体**＝1 発合成の前後で torch の使用量は 2.07→2.21→2.07 GiB（作業テンソルの解放＝torch のキャッシュ内）、占有量は 2.34→2.64 GiB に増えたまま、OS の 10.84 GiB は不動＝合成ごとに OS へ返す動きは無い。上流の `empty_cache` は `IRODORI_EMPTY_CACHE_INTERVAL=0`＝無効（`app.py:488-500`＝interval ≤ 0 は撃たない）、unload 時のみ。占有量＜最大使用量（4.57）は過去 1 度だけキャッシュが返された痕跡（読込時の一時ピークと推定・未確認・裁定 110 の範囲外）。**決めたこと**＝⑴ 出所＝Windows の PDH 計数をランチャ（C#）が pdh.dll への P/Invoke で読む（NuGet を増やさない・第三者バイナリ 0）。道は 3 本＝`\GPU Process Memory(*)\Dedicated Usage`・`\GPU Adapter Memory(*)\Dedicated Usage`・控えの `\GPU Local Adapter Memory(*)\Local Usage`（`_part_N` は LUID ごとに足す）。`PdhAddEnglishCounterW`（地域設定に依らない）・query は 1 本を開きっぱなし（ワイルドカードは collect のたびに展開し直される＝後から起きた pid も拾う・検分席が実射）・`PdhGetFormattedCounterArrayW` の 2 度呼び・`CStatus` が 0／1 でない item は捨てる。⑵ 総量＝DXGI（`CreateDXGIFactory1`→`EnumAdapters1`→`GetDesc1`）の **`DedicatedVideoMemory` だけ**＝`SharedSystemMemory` は読まない（共有メモリは除外）。ソフトウェアアダプタと Basic Render Driver は除外。LUID の突合は `luid_0x<HighPart:X8>_0x<LowPart:X8>`（大小無視）。この機体の実測＝8060S の DedicatedVideoMemory **68,535,640,064 B（63.83 GiB）**＝タスクマネージャの専用 63.8 GB と一致。⑶ **集計の的＝wrapper の pid が `GPU Process Memory\Dedicated Usage` に値 > 0 の instance を持つ LUID だけ**＝設定の GPU 名／UUID とは突き合わせない（この機体の DXGI は同名「AMD Radeon(TM) 8060S Graphics」を 4 枚名乗るが、pid の instance が在るのは 1 枚＝行は 1 つ）。複数 GPU（モデルとコーデックを別 GPU に置いた・内蔵 GPU 併存）は LUID ごとに 1 行（「このプロセス」の多い順・同点は LUID 順）。pid が無い・計数が読めない・CPU 変種は OS の組を出さず既存の文言（`UiText.NotRunning`／「CPU（GPU メモリなし）」）のまま。⑷ 帯の綴り＝「torch 使用量 X（最大 Y）／占有量 Z／このプロセス P／GPU 全体 A / T（アダプタ名）」（1 枚）・2 枚以上は「／<名前>＝このプロセス …／GPU 全体 … / …」を GPU ごとに（同名は `名前（0x<LowPart>）` で読み分け）。wrapper の `gpu_used`／`gpu_total` は帯から消す（JSON と契約 ⑹ は据え置き・wrapper は無改変）。「GPU 全体」は使える数が出たときだけ＝アダプタ側が 0 なら控えへ、控えも 0 なら「—」、自分の分より小さい全体は嘘なので「—」。説明文＝使用量・占有量＝torch の allocator の中／このプロセス＝OS が見た TTS プロセスの専用 GPU メモリ／GPU 全体＝TTS が使っている GPU の専用メモリの占有（他のプログラム込み・共有メモリは含めない）／複数 GPU は GPU ごと。⑸ 配線＝見張りの糸（`ServerProcess.SampleAsync`・起動待ち 1 s／ready 後 2 s）が `/ywk/status` の標本の直後に `OsGpuMemorySampler.Sample(pid)` を撃ち `LatestOsGpuMemory` に置いてから `StatusSampled` を上げる＝窓は読むだけ（低 3・裁定 88 ⑶）。PDH の query の開設（実測 222〜236 ms）も**最初の標本の回に見張りの糸で**（検分の高 1＝UI の糸で開いて起動を 0.23 s 遅らせていたのを是正）。標本 1 回の代金＝連続 0.03〜0.14 ms・2 s 間隔 0.17〜0.27 ms。DXGI は 1 度読んで持ち、知らない LUID が出たときだけ 60 s に 1 度まで数え直す。失敗（dll 無し・計数無し・PDH_NO_DATA・COM 失敗）は行が出ないだけ・理由 1 行はプロセスにつき 1 度・例外は UI へ出さない。DllImport は `DefaultDllImportSearchPaths(System32)`。⑹ **触っていない物**＝server/（JSON 不変）・installer/・`$ExpectedAppBytes`（37,125,639 のまま）・AutomationId（`StatusMemoryText` ほか）・設定の GPU 欄。**入れた物**＝新規 `Services/Gpu/OsGpuMemory.cs`（純関数 `ParseInstance`／`LuidToken`／`Aggregate`＋記録型 5・口 2）・`PdhGpuMemoryCounters.cs`・`DxgiGpuAdapters.cs`・`OsGpuMemorySampler.cs`（遅れ開き）・`ServerProcess`／`IServerProcess`／`NullServerProcess`（`LatestOsGpuMemory`）・`MainWindow.xaml.cs`・`MainViewModel`／`StatusViewModel`（`DescribeMemory(memory, serverAnswered, osRows)`・`DescribeOsGpuRows`）・`StatusView.xaml`（説明文）・xUnit `OsGpuMemoryTests.cs`＋既存 4 檔の帯の釘（603→**649 合格＋実機 1 本は `YWK_LIVE_GPU=1` のときだけ・無ければスキップ**）・`probe/d-launch-probe.ps1`（段 a を torch 部と OS の組に分けた＝段数 89→90 の見込み・ASCII/CRLF）・檔＝設計書 `ben-d` 新設 §27＋§15 の撤回註・`docs/contract.md` ⑹ 6-1 の `gpu_total`／`gpu_free`／`gpu_used` に読み替えの註（形は不変）・`docs/radeon.md` §7-6／§7-7・`launcher/README.md`・`tests/contract/test_memory.py`（docstring のみ）。**検分**＝Workflow（実装 1 席 → 敵対検分 3 席〔interop／統合とテスト／檔と検分台本〕→ 是正 1 席・全席 Opus 5）＝所見 **25 件（high 3・medium 8・low 14）**をすべて処置（1 件＝過去席の記帳の段数は書き換えず註で処置）（要点＝PDH の開設が UI の糸だった／値 0 の instance で「GPU 全体 0 B」を綴っていた／設計書の「実測」が固定値だった／実機テストが無検証で合格していた）。本席の再検証＝build 警告 0・xUnit **649 合格＋1 スキップ**・実機テスト（`YWK_LIVE_GPU=1`）合格＝「[row] AMD Radeon(TM) 8060S Graphics このプロセス=11.20 GiB GPU 全体=30.10 GiB / 63.83 GiB」・契約テスト **360**・installer-build -All＝**20 門 0 失敗・WARN 0**（A-1 は 37,125,639 のまま）・setup（02:53）＝cuda **85,028,205 B** `5916b259…`／radeon **85,043,090 B** `a654f0b0…`＝N: `rtx-handoff\installer` に差し替え済み（hash 一致）・新 exe 69,637,242 B。**実射（司令官の許可「TTSエンジン落としてよし」のもと・02:54）**＝導入済みのランチャ（pid 29112・wrapper 31556）を「サーバ停止」→終了で畳み、新 exe を開発起動（`YWK_LAUNCHER_APP_DIR=build/out/app`・データ樹は本物・autoStartServer）→ `/health` 9.6 s・loaded 35.2 s（wrapper pid 31424）→ 帯の UIA 逐語＝「**torch 使用量 1.73 GiB（最大 3.01 GiB）／占有量 2.07 GiB／このプロセス 3.34 GiB／GPU 全体 22.24 GiB / 63.83 GiB（AMD Radeon(TM) 8060S Graphics）**」＝同時刻の PDH（`pid_31424_luid_…137d0` 3.34 GiB／`luid_…137d0` 22.24 GiB）と 1 字も違わず、wrapper の HIP 側（gpu_used 2.33 GiB）はもう帯に出ない。窓を PNG に撮った（scratchpad `band-20260908-025441.png`）。副産物＝起こしたばかりの wrapper は OS から見て 3.34 GiB（便 C §7-7 の読込直後 3.37 GiB と一致）で、長く走らせた個体の 10.84〜11.20 GiB は利用に伴う伸び（MIOpen／HIP のプール・torch の allocator の外）である＝裁定 110 の範囲外・記帳のみ。→ 新個体を「サーバ停止」→終了で畳み、導入済みのランチャを引数なしで起こし直した（pid 47368・wrapper 46568・loaded 36.1 s・`/health` 200）＝司令官の環境は元どおり（導入済みの Radeon 版は入れ直していない＝帯は旧のまま。次の入れ直しで新しい帯になる）。**残り**＝本体席へ契約 ⑹ の註だけ通知（JSON 不変・再写し不要）。`probe/d-launch-probe.ps1` の全段の実射（段 90）は次に窓を立てられる回で。
111. **プリセット二次 wav 3 本を `cfg_scale_speaker` 5.0 → 7.0 で撃ち直した（司令官の検分と指示・2026-09-09 01:xx）＝採否は司令官の耳・検分合格まで次へ進まない。**逐語＝「サンプルボイスをよく検分してなかった。２次生成ボイスが途中から参照ボイスになってないね。CFG Scale Speakerを2ほど上げて再生成を頼む。co_kana_naisho / co_ofutonp_kiza / vr2_akane_west 出来たら検分するので、検分合格で次に進もう」**事実**＝二次 wav（一次＝各エンジンの声を参照に Irodori-TTS が合成した配布物）は 2026-09-05 の生成で `cfg_scale_speaker` を明示しておらず、上流既定 **5.0**（`Irodori-TTS-Server config.py:54`）で撃っていた（台帳 `secondary.irodori` は `num_steps`・`seed` だけ）。**入れた物**＝⑴ `tools/preset-voices/run_secondary.py` に `--cfg-scale-speaker <float>`（既定 None＝送らない＝上流既定）。与えたときは通常経路と掃引経路（`trim_tail=false` の一巡も）の全射の `irodori` 欄に載せ、no_ref の暖機には載せない。`report['irodori_params']`・掃引の行・各射の request に残る。`merge_result` は畳み込む行に **その run の `generated_at`／`server_log` を刻む**ようにした（是正＝撃ち直した行の日付が 9/5 のままだった穴）。⑵ `make_presets.py` が `request.irodori.cfg_scale_speaker` を在るときだけ写す（無い行に既定値を捏造しない）・行の `generated_at` はその行を撃った run の時刻・台帳 notes に 2 行（欄が無い行は上流既定 5.0 で撃った／`ref_10s_file` は撃ち直していない）。⑶ schema に `secondary.irodori.cfg_scale_speaker`（number）。⑷ 檔＝`docs/preset-voices-listening.md` 追記 ③（3 段の聴取順路・A/B の置き場・出どころ表の是正・A 群表は取り消し線で履歴）・設計書 `ben-p` §5 の欄の列挙と新設 §10・`tools/preset-voices/README.md` §4。**生成**（私設 8090・`.venv-rocm`・`--sweep-seed --sweep-ids co_kana_naisho,co_ofutonp_kiza,vr2_akane_west --cfg-scale-speaker 7.0`・本文・参照・末尾規則は不変・18088 には触れず・全体 116.4 s・10 射・trim_tail の落ち先は不要）＝KANA（ないしょばなし）seed **1235**（2 射目・1234 は delta+abs+rise で失格）33.28 s・Δ −53.74 dB・末尾 50 ms −72.97 dBFS・clean・md5 `ef329c24…`→**`cfb2786f…`**／おふとんP（きざ）seed **1239**（6 射目・1234〜1238 失格）30.60 s・Δ −53.81 dB・−73.76 dBFS・clean・`3dbf8574…`→**`cd37a2ae…`**／琴葉茜（関西弁）seed **1235**（2 射目）34.92 s・Δ −56.77 dB・−73.36 dBFS・clean・`d42b02df…`→**`c8e1e619…`**。採用 seed は 9/5 と同じ番号だが中身は別の射（同 seed・同本文で音が変わっている＝cfg が効いた証拠）。秒数は 3 本とも 1/100 秒まで同じ・バイト数も同じ（3,194,924／2,937,644／3,352,364 B）。他 8 本は md5 一致・台帳の他 8 行は欄ごと同一。11 本すべて 3 条件 clean。**A/B の置き場**＝旧 cfg 5.0 の 3 本＝`build/out/preset-work/secondary/cfg5/<id>.wav`・掃引の全射＝同 `sweep/cfg5/`・N: `preset-voices/secondary/cfg5/<id>.wav`。新版＝`voices/presets/<id>.wav`（N: は `secondary/<id>_secondary.wav`）。**検分**＝Workflow（実装＋生成 1 席 → 敵対検分 2 席〔檔と台帳の実物／檔と引き渡し〕→ 是正 1 席・Opus 5）＝所見 **14 件（medium 8・low 6）**をすべて処置・棄却 0（要点＝撃ち直した行の `generated_at` が 9/5 のままだった〔根から直した〕・設計書の「result.json の irodori_params にも残る」が事実と違った・A 群表の一部を取り消し線なしで上書きしていた・N: の新版の在処が書かれていなかった）。本席の再検証＝契約テスト **360**・jsonschema OK・installer-build -All＝**20 門 0 失敗・WARN 0**（A-1 の記録値 37,125,639 → **37,126,798**＝3 本の wav はバイト数不変で、配布樹に入る台帳 `voices/presets.json` が +1,159 B 伸びた分）・setup（02:13）＝cuda **85,022,149 B** `e40934df…`／radeon **85,037,038 B** `3bc3e060…`・N: `rtx-handoff\installer` 差し替え（hash 一致）。**残り（司令官の耳で決まる）**＝3 本を聴いて「途中から参照ボイスから離れなくなったか」を判定。合格なら次へ。外れなら `--cfg-scale-speaker` をさらに上げて撃ち直す。**申し送り**＝⑴ 他 8 本は cfg 5.0 のまま（合格後に揃えるかは司令官の裁定）⑵ 3 名の `ref_10s_file` は 5.0 のまま⑶ 導入済みの機体では wav の差し替えが `latent_stale` を立てて潜在が作り直る（`ywk_server.py:2597-2609`＝sidecar の sha256 突合）＝入れ直し後に `/ywk/voices` で 3 名の `latent_stale` を目視⑷ B 群の F0 の数字は cfg 5.0 の測定のまま（⚠ 付き）。
112. **プリセット二次 wav 3 本を本文約 20 秒（`expressive_20s`・90 字）で撃ち直した（司令官の検分と指示・2026-09-09 02:xx）＝裁定 111 の cfg 7.0＋30 秒本文は司令官が聴いて外した・採否は司令官の耳。**逐語＝「3ファイルともおなじところで参照ボイスが外れるね。アプローチを変えよう。文章を20秒程度に見積もって再生成。」**所見**＝3 本とも**同じ位置**で声が参照ボイスから離れる＝話者や cfg ではなく**長さ／位置に依る**現象（146 字の本文＝24.8〜35.8 s）。手当て＝cfg_scale_speaker は 7.0 のまま（裁定 111 の指示は撤回されていない）、**本文だけを約 20 秒に縮める**。**本文**＝`tools/preset-voices/corpus.json` の `secondary.expressive_20s`（target_s 20・emotion_arc 5 段・絵文字は同じ 4 つ😊🎉😢😆）＝逐語「こんにちは！いい知らせがあるんです😊 新しい企画が、ついに完成しました🎉 正直、何度もくじけそうになりました😢 でも、ここまで来られました。さあ、本番ですよ！一緒に楽しみましょう😆」（90 字＝うち絵文字 4・半角空白 3）。校正の経緯＝見積り（3 名の読み速 4.2〜4.8 字/秒 → 20 s ≈ 85〜95 字）→ 初稿 95 字を実射＝22.40／19.96／**23.24** s で琴葉茜が許容 18〜23 s を 0.24 s 超過 → 「みなさん、」の 1 句（5 字）を落として 90 字 → **20.24／18.32／21.00 s** で 3 本とも内側（両方の実射を `$calibration` に記録・95 字の射は `secondary/sweep/20s-95chars/` に保全）。**入れた物**＝⑴ `run_secondary.py` の `--text` に `expressive_20s`・**射ごとに `text_id`／`text` を item に刻む**（通常・掃引の両経路・`merge_result` は保つ・top-level は差し替えない＝裁定 111 の `generated_at` と同じ方針）⑵ `make_presets.py` が行ごとに `text_id`／`text` を読む（無い行は頭に落ちる）・台帳の頭に `corpus_texts`（行が名乗る id の集合＝`[expressive_20s, expressive_30s]`）・notes 1 行（20 s の行の素性）・`ref_10s_file` の手順の註（cfg と本文の両方）⑶ schema（`corpus_texts`・説明）⑷ 契約テスト `test_presets_manifest.py` に「secondary の全行の text が名乗る corpus の本文と逐語一致・`corpus_texts` が行の集合と一致」の門 1 本（360 → **361**）⑸ 檔＝聴取表 追記 ④（3 段の聴取順路・A/B の 3 世代の在処・追記 ③ は「司令官が聴いて外した」と明記・A 群表は取り消し線で 3 世代）・設計書 `ben-p` §11（§10 の「バイト数は動かない見込み」は取り消し線＋⚠）・README §4／§6・`secondary/sweep/README.txt`（同名で世代の違う射の読み分け）。**生成**（私設 8090・`--sweep-seed --sweep-ids co_kana_naisho,co_ofutonp_kiza,vr2_akane_west --cfg-scale-speaker 7.0 --text expressive_20s`・18088 には触れず・読込 32.1 s・4 射）＝KANA（ないしょばなし）seed **1234**（1 射目）**20.24 s**・Δ −56.40 dB・末尾 50 ms −73.96 dBFS・clean・peak −1.52 dBFS（11 本で唯一 0 dBFS に張り付かない射）・md5 `cfb2786f…`→**`494a3adc…`**・1,943,084 B／おふとんP（きざ）seed **1234**（1 射目）**18.32 s**・Δ −57.32 dB・−74.52 dBFS・clean・`cd37a2ae…`→**`d606722e…`**・1,758,764 B／琴葉茜（関西弁）seed **1235**（2 射目・1234 は abs で失格）**21.00 s**・Δ −57.92 dB・−74.86 dBFS・clean・`c8e1e619…`→**`27a93475…`**・2,016,044 B。他 8 本は md5 一致・台帳の他 8 行は欄ごと同一（`text_id` は `expressive_30s` のまま）。11 本すべて 3 条件 clean。3 本の合計バイト数 9,484,932 → 5,717,892（−3,767,040）。**A/B の置き場**＝cfg 7.0＋30 s 本文（裁定 111 の射）＝`build/out/preset-work/secondary/cfg7-30s/<id>.wav`・N: `preset-voices/secondary/cfg7-30s/`／cfg 5.0＋30 s（9/5 の射）＝同 `cfg5/`／新版＝`voices/presets/<id>.wav`（N: は `secondary/<id>_secondary.wav`）。掃引の射は世代ごとに `secondary/sweep/{cfg5,cfg7-30s,20s-95chars}/`（N: も同じ木に撮り直し）。**検分**＝Workflow（実装＋生成 1 席 → 敵対検分 2 席 → 是正 1 席・Opus 5）＝所見 **14 件（medium 7・low 7）**をすべて処置・棄却 0（要点＝「95 字」の書き残し 3 箇所・空白の数・emotion_arc の段数・聴取表の「11 名で最短」「untracked」の誤り・N: の `sweep/` が旧世代のまま）。本席の再検証＝契約テスト **361**・jsonschema OK・installer-build -All＝**20 門 0 失敗・WARN 0**（A-1 の記録値 37,126,798 → **33,359,577**＝3 本の wav が −3,767,040 B・台帳の増減込み）・setup（03:14）＝cuda **82,915,405 B** `2b7653e5…`／radeon **82,930,305 B** `9a5ff075…`・N: `rtx-handoff\installer` 差し替え（hash 一致）。**残り（司令官の耳で決まる）**＝3 本を聴き、途中で参照ボイスから離れなくなったかを判定。合格なら次へ＝**他 8 本も `expressive_20s`（＋cfg 7.0）に揃えるかは司令官の裁定**（揃えるなら `--sweep-ids` に 8 名・約 3 分）。外れなら本文をさらに縮める（`$calibration` の目盛り＝90 字で 18.3〜21.0 s）。**申し送り**＝⑴ 3 名の `ref_10s_file` は cfg 5.0＋146 字のまま⑵ 導入済みの機体では wav の差し替えで `latent_stale` が立ち潜在が作り直る（裁定 111 ⑶）⑶ `voices/presets/*.wav` 11 本は既に git の追跡対象（decisions 22 との線引きは司令官の判断）。
113. **裁定 112 の検分結果と、残り 2 名の本文候補 4 本（司令官の検分と指示・2026-09-09 03:xx）＝おふとんP（きざ）は完成・KANA と 琴葉茜 は本文を変えて候補 2 案ずつ・採用は司令官が選ぶまで動かさない。**逐語＝「co_ofutonp_kiza_secondaryのみ完成として残り2本かな、発話途中で入れ替わる・・・。参照ボイスの動きが読めないね。文章を変えて残り2ファイルを再トライ」**確定**＝`co_ofutonp_kiza`（`expressive_20s`・seed 1234・md5 `d606722e…`・18.32 s）は**完成**＝以後撃ち直さない。**所見**＝同じ 90 字の本文で 1 名は通り 2 名は発話途中で声が入れ替わった＝話者×本文の相性であり、参照ボイスの振る舞いは予測しにくい。**仮説（未確認）**＝Irodori が絵文字・感嘆符で演技を切り替える位置で調子が大きく動き、別人に聞こえる。**手当て**＝切り替えを弱めた本文と無くした本文の 2 案を作り、2 名 × 2 案＝**候補 4 本**を出して司令官に選んでもらう。cfg_scale_speaker 7.0・seed 掃引・末尾 3 条件は据え置き。**本文**（`corpus.json`）＝① `calm_20s`（94 字・絵文字は 😊 1 つ・「！」なし・emotion_arc 1 段）「今日は、来てくれてありがとう。この配信では、みんなのコメントを読みながら、のんびりお話ししていくね😊 途中で気になることがあったら、いつでも声をかけてね。最後まで、ゆっくり楽しんでいこう。」／② `plain_20s`（89 字・絵文字なし・「！」なし・平坦）「はじめまして。これは、声の見本です。今日は、この声で読み上げをしていきます。ゆっくり、はっきり、最後まで同じ調子で話しますので、落ち着いて聞いてください。それでは、始めましょう。」（草案 78 字＝17.5／18.2 s で 20 s に届かず、撃つ前に『これは、声の見本です。』を足した）。目盛り＝2 名の読み速（4.45／4.29 字/秒）で 18〜23 s は 81〜98 字。**入れた道具**＝`run_secondary.py` に**候補モード** `--candidate <名>`（射の行き先を `build/out/preset-work/candidates/<名>/`〔採用相当＋`sweep/`〕と `logs/run_secondary.<名>.{log,sweep.json}` に移し、採用側の `secondary/`・`result.json` には畳み込まない・`--sweep-seed` と併せてのみ・名の検査・`--adopt-candidate` との同時指定は弾く）と**採用の口** `--adopt-candidate <名> --sweep-ids <ids>`（サーバを起こさず候補の wav と掃引の射を `secondary/`・`secondary/sweep/<名>/` へ複写し、その run の item〔text_id・text・cfg・generated_at〕を `merge_result` で `result.json` に畳む＝あとは `verify_wavs.py` → `make_presets.py --copy` の従来手順）。**掃引の合格条件にクリップの門**（`max_clip_run ≥ 3` は失格・`--allow-clip` で外せる）＝検分で「末尾 3 条件だけを見て全振幅 3 サンプル連続の射を採っていた」穴が出たため。候補ログの取りこぼし（stdout/stderr の tee・停止理由の記録）も是正。**候補 4 本**（私設 8090・cfg 7.0・4 本とも 18〜23 s の内側・末尾 clean・非クリップ・48 kHz mono 16-bit）＝① calm／KANA seed 1234（1 射）20.08 s・rms −21.37 dBFS・md5 `021feb6a…`・1,927,724 B／① calm／琴葉茜 seed 1234（1 射）20.48 s・−16.09 dBFS・`610ec04b…`・1,966,124 B／② plain／KANA seed **1239**（1234〜1237 は末尾で失格・1238 はクリップで失格）21.48 s・−21.93 dBFS・`e7d54eed…`・2,062,124 B／② plain／琴葉茜 seed 1234（1 射）21.24 s・−16.55 dBFS・`fa12fee4…`・2,039,084 B。置き場＝`build/out/preset-work/candidates/{calm_20s,plain_20s}/<id>_secondary.wav`・N: `preset-voices/candidates/<名>/`（md5 一致）。副産物＝読み速は本文でも動く（expressive_20s 4.45／4.29 → calm 4.68／4.59 → plain 4.14／4.19 字/秒＝平坦な地の文ほど遅い）。琴葉茜の候補は頭の無音が 0.88〜0.93 s（同梱 11 本の帯 0.0〜0.73 s より長い）＝採用時に詰めるか別 seed かそのままかを決める。**動かしていない物**＝`voices/presets/*.wav` 11 本・`voices/presets.json`・`build/out/preset-work/secondary/`・`logs/run_secondary.result.json`（md5 一致）・`presets.json.schema`・`make_presets.py`・installer・`$ExpectedAppBytes`（配布樹は不変なので組み直し不要）。**檔**＝聴取表 追記 ⑤（候補の表・聴く順路・採用コマンド・KANA／琴葉茜の欄に「外した・候補あり」・きざの欄に「完成」・数値の出どころに 5 度目の行群）・設計書 `ben-p` §12（§10／§11 に「今の版は §12」の連鎖）・`README` §4-3／§6。**検分**＝Workflow（実装＋生成 1 席 → 敵対検分 2 席 → 是正 1 席・Opus 5）＝所見 **19 件（high 1・medium 11・low 7）**をすべて処置・棄却 0。本席の再検証＝契約テスト **361**・`git status` は 5 檔（`corpus.json`・`run_secondary.py`・`README.md`・聴取表・`ben-p`）だけ。**残り（司令官の耳で決まる）**＝話者ごとに ① calm／② plain／どちらも駄目 を判定。選ばれた案を `--adopt-candidate <名> --sweep-ids <id>` → `verify_wavs.py` → `make_presets.py --copy` で採用し、installer と A-1 を更新して記帳する。どちらも駄目なら次の案（本文をさらに平坦に・尺をさらに短く・別 seed の候補列）。
114. **裁定 113 の候補から calm_20s を採用（司令官の検分と指示・2026-09-09 04:2x）＝KANA（ないしょばなし）と 琴葉茜（関西弁）の二次 wav は calm_20s の射に確定・インストーラを組み直して main へ。**逐語＝「calm採用。 インストーラーくみなおしとマージを頼む。」**採用**（`run_secondary.py --adopt-candidate calm_20s --sweep-ids co_kana_naisho,vr2_akane_west` → `verify_wavs.py` → `make_presets.py --copy`・サーバは起こしていない）＝KANA seed 1234・**20.08 s**・md5 `021feb6a…`・1,927,724 B・末尾 clean／琴葉茜 seed 1234・**20.48 s**・`610ec04b…`・1,966,124 B・clean（頭の無音 0.93 s は司令官が聴いたままの形で採用＝詰めない）。行の `text_id`＝`calm_20s`・`generated_at`＝候補の run（2026-09-09T03:32:21+0900）・cfg 7.0。plain_20s の候補は 2 名とも不採用（`candidates/plain_20s/` と N: に残す）。**これで 11 本が確定**＝おふとんP（きざ）`expressive_20s`（裁定 112）・KANA／琴葉茜 `calm_20s`（本裁定）・他 8 本 `expressive_30s`（146 字・cfg 5.0・2026-09-05 のまま）。**他 8 本を 20 秒級の本文に揃えるかは未決**（司令官の裁定待ち＝揃えるなら候補モードで撃って耳で選ぶ）。**検証**＝契約テスト **361**・jsonschema OK・installer-build -All＝**20 門 0 失敗・WARN 0**（A-1 の記録値 33,359,577 → **33,294,829**＝2 本の wav −65,280 B と台帳の増分）・setup（05:00）＝cuda **82,743,969 B** `48b0923b…`／radeon **82,758,874 B** `b5980c81…`・N: `rtx-handoff\installer` と `preset-voices/`（`secondary/` の 2 本・`sweep/calm_20s/`・`run_secondary.result.json`・`presets.json`・聴取表）差し替え（hash 一致）。**檔**＝台帳 notes（calm_20s の行の素性・expressive_20s の行は きざ だけ）・schema の `text_id` 説明・聴取表 追記 ⑥（11 本の確定表・KANA／琴葉茜の欄は採用行を calm_20s に・§2 凡例 8／1／2）・設計書 `ben-p` §12-6（11 本の最終表）・README §6。Workflow（実装 1 席 → 検分 1 席 → 是正・Opus 5）＝所見 **4 件（medium 1・low 3）**＝3 件を檔で処置・1 件（A-1 の記録値）は主席が installer-build で処置。檔の席が「N: は複写待ち」「114 の条は未記入」と記した箇所は、主席が複写と記帳を済ませた事実に直した。**申し送り**＝⑴ 導入済みの機体では wav の差し替えで `latent_stale` が立ち潜在が作り直る（裁定 111 ⑶）＝本機の Radeon 版は入れ直していない（司令官の許可があれば入れ直す）⑵ 3 名の `ref_10s_file` は cfg 5.0＋146 字のまま⑶ RTX 機の実射は新しい setup で。
115. **本機の Radeon 版を 05:00 の setup で入れ直した（司令官「入れ直しを頼む」・2026-09-09 05:0x）＝上書き導入・データ樹の 3 本の参照 wav を手で差し替え・潜在は起動時の事前計算が焼き直した。****手順**（scratchpad `reinstall-live.ps1`）＝⓪ 台帳・設定・3 本の参照 wav・潜在 11 組を退避 ① 窓がトレイに隠れていて UIA から見えなかったので **2 個目を起こして 1 個目の窓を出させ**（単一起動の錠の合図＝裁定 108 の教訓）「サーバ停止」→ 18088 と wrapper が消えてからランチャを落とす ② `irodori-tts-ywk-setup-v0.1.0-radeon.exe /VERYSILENT /SUPPRESSMSGBOXES /NORESTART`（`b5980c81…`・exit 0）＝上書き導入・場所は `Programs\irodori-tts-ywk-radeon` のまま・「アプリと機能」の表示名 **「irodori-TTS for 読み分けちゃん（ROCm 版） v0.1.0」**・近道は旧「（Radeon 版）.lnk」が消え新「（ROCm 版）.lnk」1 本（裁定 109 の `[InstallDelete]` が効いた）・配布樹の 3 本は repo と md5 一致 ③ **データ樹の `voices/refs/` の 3 本を配布樹の新版に差し替え**（KANA `ef329c24…`→`021feb6a…`・きざ `3dbf8574…`→`d606722e…`・琴葉茜 `d42b02df…`→`610ec04b…`）④ 導入済みランチャを引数なしで起こし直し＝窓題「…（ROCm 版）」・`/health` 12.2 s・loaded **106.2 s**（入れ直し直後の cold）・**起動時の事前計算が 10 s で 3 名の潜在を焼き直し**（`latent=true`・`latent_stale=false`・sidecar の sha256 が新 wav と一致）・帯「torch 使用量 1.73 GiB（最大 3.01 GiB）／占有量 2.06 GiB／このプロセス 5.90 GiB／GPU 全体 30.18 GiB / 63.83 GiB（AMD Radeon(TM) 8060S Graphics）」（裁定 110 の帯が導入済みの版でも出た）⑤ 新しい KANA で speech 200（wav 5.01 s・RTF 1.57＝1 発目）。ランチャは走らせたまま（pid 78100／wrapper 76772）。**判った穴（便 D（4）の切符）**＝「入れ直す」（`PresetVoices.Restore`）は **既存の檔を上書きしない**（`PresetVoices.cs:343` `File.Copy(…, overwrite: false)`＋`skipExisting`）＝同梱プリセットの**中身**が変わった版を上書き導入しても、利用者のデータ樹の参照 wav は旧いまま（潜在も旧 wav のまま）。今回は本席が手で差し替えたが、配布版としては **配布樹の `presets.json` の md5 と台帳のプリセット行の実檔を突き合わせ、違えば差し替えて潜在を stale にする**口が要る（起動時か「入れ直す」で・利用者が触った行は除く）。裁定は司令官へ。**確認**＝退避＝scratchpad `reinstall-backup-20260909-050836`（28 檔）。共有データ樹の他の檔・設定・話者の並びは触っていない。
116. **タブ「試し撃ち」の表示名を「発話テスト」に改める（司令官の指示・2026-09-09 15:1x）＝利用者の目に入る札だけを替え、内部名は据え置き。**逐語＝「タブ"試し撃ち"の表示名変更"発話テスト"」。**替えた札 4 つ**＝⑴ 主窓のタブ `Header`（`Views/MainWindow.xaml`・id `TabTry` は据え置き）⑵ 初回取得ウィザードの完了文「「発話テスト」のタブで、本文と話者を選んで 1 発鳴らしてみてください。」（`Views/FirstRunWizard.xaml`）⑶ 同ウィザードの完了ボタンの札「試し撃ちへ」→「**発話テストへ**」（`FirstRunViewModel.NextButtonText`＝押下 5 の最後の 1 押し・裁定 94 ⑴）⑷ 話者一覧の id 列のツールチップ「本体・発話テストはこの名前で話者を選びます」（`Views/VoicesView.xaml`）。**据え置き**＝AutomationId（`TabTry`・`Try*`）・`TryView`／`TryViewModel`・設定の鍵 `lastTestVoice`／`lastTestNumSteps`・契約（JSON は 1 字も変わらない）・本体側（不触＝司令官の確認 2026-09-07）。**註と文書の作法**＝「」で括って**札を逐語で引く**箇所は新名（`MainViewModel` の 2 箇所・`FirstRunViewModel` の 3 箇所・`TryView.xaml`／`.xaml.cs` の頭の註・`MainWindow.xaml` の頭の註）。**機能の呼び名としての「試し撃ち」**（`TryViewModel`・`SpeechRequestBuilder`・`LauncherSettings`・`WavInfo` ほか 28 本の註・試験名・`docs/contract.md` の「UI で試し撃ちすると」）と**歴史の記帳**（裁定 52／94／95・設計書の実測の §・`ben-e` の便 E（2）の実測・受け入れ表の改訂前の実測）は旧名のまま。**現行の規則を書く文書は新名に揃えた**＝`launcher/README.md` §7-3（id 表の行頭は「発話テスト」・表の下に改名の 1 段落）／§11 ⑴⑵・`docs/acceptance.md` 導入行（〔裁定 116 で改名〕の註つき）・設計書 `ben-d` §6 の段 6・§21-1 ⑴⑵・§22-3（段 h／i と押下の数え方の文）・§22-5・`probe/README.md` 段 i・`licenses/dotnet/README.md`（`assemble-app.ps1` が配布樹に写す＝利用者の目に入る）・台本 `probe/d-launch-probe.ps1` の文言表 `$T.ToTry`（コードポイントで「発話テストへ」＝未使用の札だが §22-5 の「変えるなら `$T` も」に従う・走は id `FirstRunNextButton` で掴むので割れない・parse 0 error）。**検証**＝`dotnet test` **649 合格・1 skip**（`YWK_LIVE_GPU` 門・総数 650）・Release の dll に BAML（UTF-8）の「発話テスト」×3・C# 文字列（UTF-16）の「発話テストへ」×1・「試し撃ち」×0・installer-build -All **20 門 0 失敗・WARN 0**（A-1 の記録値 33,294,829 → **33,294,832**＝`licenses/dotnet/README.md` の +3 B・exe は配布樹の外なので数に入らない・裁定 51・exe 69,637,254 B）・setup（15:1x）＝cuda **82,743,363 B** `ed53db87…`／radeon **82,758,268 B** `8e7eafad…`・N: `rtx-handoff\installer` 差し替え（`SHA256SUMS.txt` で hash 一致）。Workflow（検分 1 席・Opus 5）＝所見 **9 件（medium 3・low 6）**＝**8 件を当て込み**（§6・§22 の 3 行・`$T.ToTry`・README の表の行頭・§21-1 の双子・licenses・§22-3 段 i と `probe/README`・`TryView.xaml` の註の折り）・**1 件は上の作法で裁いて据え置き**（機能名の註 3 本を戻すか 28 本を揃えるか＝「札の逐語だけ新名」の線で切った）。**本機の導入済み Radeon 版は裁定 115 の版のまま**（タブは「試し撃ち」のまま＝入れ直しは司令官の許可があってから・走っている個体は止めない）。窓を立てての UIA 実射はしていない（錠 `Local\irodori-tts-ywk-launcher` が 2 個目を弾くため＝新 exe を起こせない）。
117. **v1.0.0 を GitHub Release として公開した（司令官「簡単なリリースページを作ってv1.0としてpublish頼めるかな。」・2026-09-09 16:0x）＝版は v0.1.0 → v1.0.0（3 桁＝この repo の版の作法に揃えた・司令官の「v1.0」は 3 桁で読んだ）・タグ `v1.0.0`（commit `5b0568a`）・Release は Latest・下書きでも pre-release でもない。**URL＝https://github.com/mugonkun/irodori-tts-for-yomiwakechan/releases/tag/v1.0.0（repo は **private** なので、いま見えるのは collaborator だけ＝公開するかは司令官の裁定）。**資産 3 つ**＝`irodori-tts-ywk-setup-v1.0.0-cuda.exe`（82,741,884 B・`ce817787…`）・`irodori-tts-ywk-setup-v1.0.0-radeon.exe`（82,756,787 B・`6a65eed2…`）・`SHA256SUMS.txt`（その 2 行だけ＝v0.1.0 の 2 本と旧い一覧は組む前に `build/out/installer` から scratchpad へ退避し、門 B-3 が旧行を残さないようにした）。本文＝`docs/release-notes/v1.0.0.md`（新設・101 行・配布物には入らない＝`.iss` の docs は明示列挙）。**版の繰り上げ**＝定義 2 箇所（`launcher/Directory.Build.props` の `AppDisplayVersion`・`server/ywk_server.py` の `YWK_VERSION`）と現行の記述（契約 ⑹ の `installedAppVersions` の見本・`ben-f-handoff` の骨 2 箇所と本文・`launcher/README` の settings 見本・`probe/e-install-probe.ps1` の既定の setup 名・`installer-build.ps1` と `.iss` の註）。**触らなかった**＝設計書 ben-d／ben-e の実測の記帳（53 箇所）・Tests の fixture（21 箇所）・`docs/install.md`（`v<版>` の置き場）・`AppVersion.cs`／`ReleaseFlavor.cs` の註の例（`ViewModelsTests` の fixture と対）。README の「案」札を外し（便 D／E は着地済み・確定手順は `docs/install.md`）「起動」→「サーバ起動」に揃えた。**v0.1.0 からの上書きの挙動（リリース文の「上書き」の段の根拠）**＝`RuntimeStamp.Compare` は台帳の sha256 が同じで版だけ違えば 1 手を出さず（`RoundThreeTests`「焼き印_版だけ違えば1手を出さない」）、ログに註 1 行を 1 度だけ流して焼き印を v1.0.0 に焼き直す＝モデルと実行系の取り直しは起きない。同梱プリセットの中身は「入れ直す」で上書きされない（裁定 115 の穴）。**検証**＝Workflow（実装 1 席 → 検分 2 席〔版の繰り上げの眼・リリース文の事実の眼〕→ 是正・Opus 5）所見 12 件＝**11 件当て込み**（SHA256SUMS の 4 行化・もち子さんの改名〔裁定 108〕・裁定 109 以前の CUDA 版の導入先・ディスクの二重計上・同居の癖・本体側の対応版・SmartScreen・RTF の出典・README の「案」ほか）・**1 件据え置き**（設計書 `ben-e` :159／:494・`ben-d` :112 の v0.1.0＝歴史と読んだ・動かすなら「（当時＝v0.1.0）」を添える形）。`dotnet test` 649＋1 skip・契約テスト 361・installer-build -All 20 門 0 失敗・WARN 0・A-1 33,294,832（動かず）・A-6 ProductVersion v1.0.0／FileVersion 1.0.0.0。N: `rtx-handoff\installer` を v1.0.0 に差し替え（v0.1.0 は消した・写しは scratchpad `installer-v0.1.0-final`）。**していないこと**＝⑴ RTX 機での実射は v1.0.0 では未（今日の setup はどれも RTX 機で撃っていない）⑵ 本機の導入済み Radeon 版は裁定 115 の v0.1.0 のまま（入れ直しは許可があってから）⑶ 本体側の対応版の番号はリリース文に書いていない（repo に記録が無い＝本体席に確かめてから）。切り方の手順は設計書 `ben-e` §18。
118. **司令官が録音を直に渡した話者 1 名を同梱に足し、v1.0.1 として切る（司令官の指示・2026-09-10）＝エンジン由来でない `engine: "external"` の路を台帳・道具・schema・門に通した。**逐語＝「**話者追加。"シャンパンコール(ホスクラ)" 参照ボイスファイル実体は"c:\yomiwakesozai\hostclub.wav"。既に2次生成なのでこのまま取り込んで配布ページ更新まで頼む。**」。**足した 1 名**＝id `ext_hostclub_champagne`（接頭辞 `ext_` を新設＝外部提供）・表示名**「シャンパンコール（ホスクラ）」**（司令官は ASCII の `(ホスクラ)` で書いたが、表示名はそのまま話者 id になる〔裁定 17〕ので他の 11 名に揃えて**全角括弧に正規化**した＝裁定 108 の作法）・`engine_speaker`「シャンパンコール」・`style` `{name: "ホスクラ", id: null}`・`status` `done`。**「既に2次生成」＝届いた wav が最終形**なので、**一次 wav は無く**（`primary: null`＝pending の「まだ無い」ではなく「そもそも存在しない」）、**この席は 1 発も撃っていない**（`secondary.text_id`・`text`・`irodori` を `null`＝既定値〔`num_steps` 40・`seed` 1234〕で埋めると「その値で撃った」という嘘になる。`ref_variant`・`ref_10s_file`・`server`・`elapsed_s` は**欄ごと無い**）。**実測**（`verify_wavs.analyze`）＝`voices/presets/ext_hostclub_champagne.wav` md5 `149eddeca796616e916e2b83dc67f692`・**787,244 B**・**8.20 s**・48 kHz・mono・16 bit・ピーク **−1.76 dBFS**（他の 11 本は −0.01〜0.00 で全部フルスケール）・RMS −17.03 dBFS・頭の無音 **0.976 s**（同梱 12 本で最長・次点 琴葉茜 0.930 s）・末尾判定（decisions 39 の 3 条件）**clean**（Δ −53.38 dB／絶対 −70.41 dBFS／末端の立ち上がり無し）。**新設した欄 `provenance`**（`kind: external`・`provided_by` 司令官（mugonkun）・`provided_at` 2026-09-10・`source_file` `c:/yomiwakesozai/hostclub.wav`・`note` に司令官の逐語）＝**在ること自体が「席の生成物ではない」の印**。**権利**＝`decisions.md` 18（各エンジンの生成ボイスは再配布可）は**引かない**＝当たらないので、行の `rights_note` には「司令官（mugonkun）が 2026-09-10 に提供した録音」という事実と「**席はこの録音の出所を一切確かめていない**（誰がいつ録った物か・元の権利者が誰か・第三者の声が入っていないか）」を書いた（`rights_confirmed_by` 司令官（mugonkun）・`rights_confirmed_at` 2026-09-10・`rights_source_fetched` false）。**台帳**＝`voices/presets.json` は 12 行 → **13 行**（`done` 12・`skipped` 1＝弦巻マキ 英）・**同梱 wav は 11 → 12 本**・`corpus_texts` は不変（外部行は本文を名乗らない）・頭の `generated_at` `2026-09-10T17:40:00+0900`・**52,495 → 56,182 B**。**道具**＝`make_presets.py` に `EXTERNAL_ENGINE`／`EXTERNAL_ROWS` と外部路を新設（`gen_*.result.json` も `run_secondary.result.json` も読まず、実檔を `verify_wavs.analyze` で測るだけ）＝**正本は手で書かず道具に書かせた**うえで、scratch への空撃ちと突き合わせて**差分は頭の `generated_at` 1 行だけ**（バイト数も 56,182 で一致）を確認した。**schema**＝`engine` の enum に `external`・`id` の接頭辞に `ext_`・`secondary.text_id`／`text`／`irodori` を nullable・`provenance` を新設（`additionalProperties: false` なので schema に無いと落ちる）。「`null` ⇔ external」の**対**は条件分岐では書かず説明文で約束し、**契約テストで当てた**（`uv run --with jsonschema` の実測＝`schema OK`）。**門**＝`build/installer-build.ps1` の `$ExpectedPresetWavs` 11→**12**・`$ExpectedAppFiles` 108→**109**（wav が 1 本増えた＝A-1 は WARN ではなく**失敗**なので上げないと止まる）・`$ExpectedAppBytes` 33,294,832→**34,087,580**（＝33,294,832 ＋ 787,244〔wav〕＋ 3,687〔台帳 52,495→56,182〕＋ 1,817〔`licenses/README.md` 19,408→21,225＝`assemble-app.ps1` §3b が `licenses/` を**丸ごと**配布樹に写すので、§6-1〜§6-3 を書き足した分がそのままバイトに乗る〕）＝**検分で拾った項**。`docs/` は配布樹に入らない（`.iss` が `docs\install.md` を 1 檔ずつ名指す）のでバイトは動かないと、`installer/irodori-tts-ywk.iss` の `PresetWavCount != 11`→`!= 12`（2 枚の網の両方・11 は decisions 37／88 ⑸ の逐語＝**歴史**として残し、12 の出所は裁定 118 と註に書いた）。**版**＝v1.0.0 → **v1.0.1**（設計書 `ben-e` §18 の段 1 が名指す箇所だけ＝定義 2 箇所〔`launcher/Directory.Build.props` の `AppDisplayVersion`・`server/ywk_server.py` の `YWK_VERSION`〕と現行の記述〔契約 ⑹ の見本・`launcher/README` の settings 見本・`probe/e-install-probe.ps1` の既定の setup 名・`ben-f-handoff` の 3 箇所・`.iss` と `installer-build.ps1` の註〕。設計書の実測の記帳と Tests の fixture は**動かしていない**＝裁定 117 の作法）。**現行の規則を書く文書は 13 名／同梱 12 本に揃えた**（裁定 116 の作法）＝`docs/contract.md` ⑷・`docs/ben-f-handoff.md` §3・`licenses/README.md` §6-1（13 行目と「由来のエンジン＝無し」・作り方が違う旨・権利は 6-2 の表の外）／**§6-1 と §6-2 の既存の誤りも同時に正した**＝**月読アイ・月読ショウタは VOICEROID2 であって VOICEVOX ではない**（正本の `voices/presets.json` が `engine: "voiceroid2"`・id も `vr2_`・`gen_voiceroid2.py` の 4 名に入っている）。誤りは §6-2 の**規約表**にも伝播していて、**リリース前に読むべき規約を 2 名ぶん取り違える**形だった（VOICEROID2 の行を 4 名に・VOICEVOX の行を 2 名に）／**§6-3**（機械可読な正本の欄の説明）にも `external` を通した＝`provenance` の行を足し、`engine`／`primary.file` に「external の行はエンジンが無く primary は null」を添え、`rights_source_fetched` false の **WARN は external の行では永久に消えない**（取得すべき規約原文が無い）ことを書いた・`launcher/README.md` §10-2・`probe/d-launch-probe.ps1` の段 g・`IVoiceStore.cs` の `Preset` の註・`tools/preset-voices/README.md`（題・§5・**新設 §5-2**＝外部提供の手順）・`corpus.json` の `$comment`・設計書 `ben-p` **§13**（新設）・聴取表 `docs/preset-voices-listening.md`（**追記 ⑦**＝§2 の 13 に実測と 4 つの問い）・**検分で拾った 5 檔**＝`docs/install.md` §2 の樹の表（**配布物に入る唯一の手順書**＝「プリセット話者 11 檔・32,571,364 B」→「12 檔・**29,526,288 B**」。バイトは裁定 111／112／114 の撃ち直し前の便 D 期の値で v1.0.0 の時点で既に 2 世代古かった＝実測で入れ直した）・設計書 `ben-e` §3-2 の門 A-2 の**判定の列**（`*.wav` が 11 檔 → **12 檔**＝門の規則であって記帳ではない。§12-2 の逐語表と M-1 の記帳は据え置き）・設計書 `ben-p` §2 の**接頭辞の正本**（`ext_` が抜けていた）と題・`LauncherComposition.PrepareVoices` の註（「一覧にも試し撃ちにも 11 名が出てこない」＝日付も実射の札も無い現行の挙動の説明）・`tools/preset-voices/README.md` §5 末（「今そうなっている」と現在形で書いた本文の世代の内訳＝「同梱 12 本のうち、本文を撃った 11 本の内訳は」に改めた）。**歴史の記帳は旧いまま**（設計書 ben-c／ben-d／ben-e の実測・聴取表の A〜C 群の「11 本」・`launcher/README` §10-1／§10-3 の実射）。**リリース文**＝`docs/release-notes/v1.0.1.md`（新設・短い差分だけ＝v1.0.0 の文へ送る形。sha256 とバイトは `<sha256>`／`<bytes>` の placeholder で組み、組んだ後に主席が埋めた（実測＝installer-build -All **20 門 0 失敗・WARN 0**・A-1 **109 檔／34,087,580 B**〔算と一致〕・A-2 12 本・A-6 ProductVersion v1.0.1／FileVersion 1.0.1.0・setup cuda **83,262,330 B** `42e8dacf…`／radeon **83,277,211 B** `16101a49…`・SHA256SUMS.txt は 2 行・tag `v1.0.1`＝commit `fd3d936`・Release **公開 2026-09-10 09:21:13Z（18:21 JST）・Latest**・URL＝https://github.com/mugonkun/irodori-tts-for-yomiwakechan/releases/tag/v1.0.1・N: `rtx-handoff\installer` を v1.0.1 に差し替え `sha256sum -c` OK・v1.0.0 の 2 本と旧 SHA256SUMS は scratchpad `installer-v1.0.0-final` へ退避）。**v1.0.0 の文への 3 つの送りは相対リンクを使わない**＝この檔は `gh release create --notes-file` でRelease 本文に貼られ、相対路は `/releases/tag/v1.0.1` を基点に解かれて repo の檔に当たらない〔`v1.0.0.md` は markdown リンクを 1 つも持たない＝それが前例〕。裁定 117 が記録したRelease の絶対 URL を平文で書いた）。**上書きの挙動を実装で確かめて書いた**＝起動時の `LauncherComposition.PrepareVoices` は `PresetVoices.InstallIfFirstRun` を呼ぶが、既存の利用者は `presets_installed` が立っているので **0 件**を返す＝**上書き導入しただけでは新しい話者は一覧に出ない**。「話者」タブの「**同梱のプリセットを入れ直す**」（`PresetVoices.Restore`＝`skipExisting: true`・`File.Copy(..., overwrite: false)`）を 1 度押すと**まだ持っていない id だけ**が足される＝名付け直した話者・利用者が足した話者・既存 11 名には触れない。裁定 115 の穴（既存の檔を上書きしない）はこの版でも塞がっていないが、**既存 11 本の中身は 1 バイトも変えていない**ので v1.0.1 では影響しない。新規導入は初回展開で 12 名とも入る。**検証**＝契約テスト **369 合格**（361 ＋ 新設 8 本＝external の行・生成の記録が null であること・その逆向き・`provenance`・他行に `provenance` が無いこと・wav の md5 とバイト・末尾 clean と規則の逐語一致・生成器の ROSTER と EXTERNAL_ROWS の顔ぶれ）・`dotnet test launcher -c Release` **649 合格・1 skip**（総数 650・skip は `YWK_LIVE_GPU` 門）・`make_presets.py` の空撃ち差分＝頭の `generated_at` 1 行のみ・`jsonschema` で `schema OK`・**検分（Opus 5・2 席）所見 12 件＝10 玉（重複 2 件）を全件当て込み・据え置き 0**（A-1 のバイトの取りこぼし〔`licenses/README.md` が配布樹に丸ごと入る〕・`docs/install.md` の樹の表・§6-1／§6-2 の由来のエンジンの取り違え・§6-3 に `external` を通す・門 A-2 の判定の列・`ben-p` §2 の接頭辞・`LauncherComposition` の註・`tools/README` §5 末の内訳・RTX 掃引の表・リリース文の相対リンク）。**RTX の掃引にも足した**（検分で拾った）＝`probe/bench_cases.json` の `preset_sweep.presets` は 11 名の**べた書き**で、`cuda-bench.ps1` はこの表の id だけを使い捨ての `voices` に写して撃つ＝足さなければ**未実施の RTX 実射が新話者を 1 射も撃たない**（＝この便の最大の未知「8.20 s の参照で足りるか」を、唯一それを試せる段で試さない）。13 行目を足して`probe/rtx-remote-runbook.md` の「プリセット 11 射＝59 射」→「12 射＝**60 射**」・「30 s 版 11 本」→「同梱の 12 本（うち 11 本が 30 s 級・`ext_` だけ 8.20 s）」に直した。同じ表の `vv_mochiko_sexy` の表示名が裁定 108 前の「もち子さん」のままだったのも正した（この値は使い捨て `voices.json` の別名の鍵になる）。**していないこと**＝⑴ **音を聴いていない**（席は聴けない）＝この録音が話者として使えるかは司令官の耳が決める⑵ **録音の出所を確かめていない**（上記）⑶ **この参照ボイスで合成を撃っていない**（サーバを起こしていない）⑷ **組んでいない**＝`build/installer-build.ps1` は走らせていない（主席の持ち場）＝`$ExpectedAppFiles` と `$ExpectedAppBytes` は**算で出した値**であって実測ではない ⑸ RTX 機での実射は無い ⑹ 本機の導入済み Radeon 版は裁定 115 の版のまま（入れ直しは許可があってから）⑺ commit・tag・Release・N: の差し替えは主席（tag は `v1.0.1`＝commit `fd3d936`）。切り方の手順は設計書 `ben-e` §18。
119. **v1.0.1 のリリース文を単体の全文に書き直した（2026-09-10 18:4x）**＝司令官の指示（逐語）「**V1.0.0は誰にも配布していないから差分情報は削除。司令官という文言も落として。"この 1 名について（正直に書いておくこと)"この節も削除**」＝`docs/release-notes/v1.0.1.md` を v1.0.0 の文の型（これは何か・落とす物・必要なもの・導入の流れ・判っている制約・文書）に戻し、「v1.0.0 からの差分」「v1.0.0 からの上書き」「この 1 名について」の 3 節を落とした。「これは何か」に同梱 12 名の名を 1 行で添えた（由来や出所の但し書きは無し）。「司令官」の語は配布ページに出さない（repo 内の台帳・設計書・decisions には残る）。GitHub Release v1.0.1 の本文を `gh release edit --notes-file` で差し替え（資産・tag・Latest は不変）。同じ回に README §0「最新版を落とす（配布ページ）」＝版を含まない固定 URL https://github.com/mugonkun/irodori-tts-for-yomiwakechan/releases/latest を入口にした（司令官「配布ページはもっと一般的な形を想定してた。最新版がすぐにわかるように。URLにバージョンを含まない形で」・commit `f181e29`）。v1.0.0 の Release は消していない（誰も持っていないなら消してよいが、消すかは司令官の裁定）。新話者の出所・未聴取の但し書きは台帳 `voices/presets.json`・`licenses/README.md` §6-1・裁定 118 に残る。
120. **repo を public に切り替えた（司令官「public に切り替え」・2026-09-10 18:5x）**＝`gh repo edit --visibility public`。切り替え前に作業木と全履歴（upstream/ を除く）をトークン・鍵・PIN の値・秘密鍵の型で機械検査＝該当 0（裁定 74 は PIN の存在だけを記し値は書いていない・機体名と N: のパスは残るが秘密ではない）。切り替え後に無認証で検めた＝repo 表紙 200・`releases/latest` 302 → v1.0.1・資産の download 302（取得可）。これで配布ページ https://github.com/mugonkun/irodori-tts-for-yomiwakechan/releases/latest は誰でも開ける。public になった結果として同梱のプリセット wav 12 本と `licenses/` の記述も世に出ている＝規約原文の取得（`licenses/README.md` §6-4・rights_source_fetched false）は引き続き未了。
121. **増えたプリセットが入らない／取得への導線が無い（司令官の報告・2026-09-10 19:27・RTX 3090 機・古いデータ樹の上に CUDA 版 v1.0.1 を導入した個体・画面写真つき）＝v1.0.2**＝報告は 2 つ（逐語）⑴「**話者一覧にシャンパンコールがないね。**」⑵「**初回起動から、モデルダウンロードへの導線を追加してほしい。単に、サーバー起動失敗となるから。**」。**⑴ の原因**（コードで確認）＝`PresetVoices.InstallIfFirstRun` は利用者の台帳に `presets_installed=true` が立っていると 0 を返す（利用者が消したプリセットを毎起動書き戻さないための印＝裁定 108 の註）ので、**後の版で増えたプリセット id は永久に届かない**＝v1.0.1（裁定 118）で足した `ext_hostclub_champagne`「シャンパンコール（ホスクラ）」は「同梱のプリセットを入れ直す」を押した人にしか入らなかった。**⑴ の直し方**＝台帳 `voices.ywk.json` に `presets_installed_ids`（この台帳へ 1 度でも差し出したプリセット id）を足し、新設 `PresetVoices.InstallNew` が「正本 `voices/presets.json`（`status: done`）に居て、その記録にも台帳にも居ない id」だけを入れる（wav を写し・行を足し・id を記録に追記）。記録に在って台帳に居ない id＝**利用者が消した**ので起動では二度と戻さない（戻す口は `Restore` のまま）。何も変わらなければ台帳は書かない。起動席は移送 → 改名（裁定 108）→ 初回展開 → **増分** → `voices.json` の順で通し、足した名は `同梱の話者を N 名足しました：…` の 1 行にして状態帯のログへ流す（窓より前に走るので控えに積む）。**正直に書く副作用**＝`presets_installed_ids` の無い台帳（≦ v1.0.1）は「いま台帳に居るプリセット」で種を蒔くので、そこで**消してあった**プリセットが v1.0.2 の初回起動で 1 度だけ戻る（次からは戻らない）。v1.0.0 は配っていないので該当しないが、**v1.0.1 は public 化（裁定 120）以後 `releases/latest` の Latest として誰でも落とせる**（v1.0.2 を切るまでそのまま）＝該当するのは開発機と、v1.0.1 を落とした利用者の台帳である。だから**この 1 度だけの戻りは利用者に見せる文にも書く**（`docs/release-notes/v1.0.2.md`・`docs/install.md`＝是正・検分。「開発機だけ」は repo の記録で支えられない）。**改名（裁定 108）は記録も一緒に改める**（是正・検分）＝`MigrateRenamed` は `presets_installed_ids` の旧い id を新しい id へ移す。移さないと新しい名が「まだ差し出していない」と読まれ、利用者がその 1 名を消した次の起動で戻る（不変が破れる）。同じ理由で `InstallNew` は**足した話者が 0 名でも記録が増えた回は台帳を書く**（書かずに捨てると次の起動が忘れる）。**⑵ の原因**＝`MainWindow.OnLoaded` は `firstRunCompleted`（設定の札）しか見ない＝古いデータ樹・消したモデル置き場では札だけが残り、ウィザードは出ず、「サーバ起動」は「実行系がまだありません」で断られるか、起こした個体が`HF_HUB_OFFLINE=1` でモデルを読めず `モデルの読込に失敗＝…` で落ちる＝**取得への導線が無い行き止まり**（出口は頭の「初回取得をやり直す」だけで、利用者が自分で見つけるほかなかった）。**⑵ の直し方**＝新設 `Services/Models/AcquisitionCheck.cs` が実行系（変種の `python.exe`）とモデル（`HF_HOME/hub` の snapshot と `refs/main`＝規則は取得台本 `ywk_fetch_models.py` の `_resolve_cached`／`refs_main_path` と同じ物）の在否だけを安く見る（通信も python も無し・`models.json` が読めなければ「揃っている」）。主窓は `MainViewModel.NeedsAcquisition` が真なら**自動起動をやめて**ウィザードを出し、`実行系／モデルが揃っていないため、取得からやり直します（不足＝…）` を Trail とログに残す（閉じれば主窓は使えるまま）。「サーバ起動」の事前検査は実行系が無い断りにもモデルが無い断りにも`上の「初回取得をやり直す」から取得してください。` を付ける。状態帯は取得が未了だと読める失敗のときだけ`取得が未了です。` と「取得へ進む」（`StatusAcquireButton`）を出す（判定は `StatusViewModel.IsAcquisitionFailure`）。**ウィザードの再開は読んで確かめ、1 箇所直した**（是正・検分）＝取得は cache hit で落とし直さず・展開は意図して組み直し・モデルは在る檔を返すだけ、までは読み通りだったが、**取得キャッシュは 1 射目で空にする**（裁定 90 Q-E2 ⑶）ので初回取得を通した機体には原檔が 1 檔も無い＝この導線が当たる相手（モデルだけ無い機体）では 2.77 GiB（cu126）を落とし直したうえで健全な実行系を消して入れ直すことになっていた。`FirstRunViewModel.RuntimeLooksSound()`（`python.exe` が在る・展開の件数が台帳と合う〔`RuntimeStamp.LooksComplete`〕・展開に使った台帳が配布樹と同じ〔`Compare` の `LedgerChanged` が偽〕）を新設し、真なら取得と展開の段を飛ばしてモデルの段へ行く（Trail に `実行系は揃っているので取得と展開は飛ばします（モデルだけ取り直します）。`）。ほかに検分で当て込んだ 3 件＝`AcquisitionCheck` は台帳が長さを名乗る檔の `FileInfo.Length` も見る（切れた檔を「揃っている」と読まない）・設定の「適用」でも取得の済み具合を引き直す（変種が動く）・檔を見た結果は事前検査の断り書きに勝つ（取得を済ませて閉じれば帯が下がる）。**版を v1.0.1 → v1.0.2**（設計書 `ben-e` §18 段 1 の 7 箇所＝`Directory.Build.props`・`ywk_server.py`・契約 **⑻** の見本〔⑹ は `GET /ywk/status` の可視化で、版は placeholder のまま〕・`launcher/README` の settings 見本・`e-install-probe.ps1` の既定の setup 名・`.iss` と `installer-build.ps1` の註・`ben-f-handoff` の現行値）。**釘 25 本新設**（`NewPresetsAndAcquisitionTests.cs`＝増分の 8 本・`AcquisitionCheck` の 7 本・導線の 10 本＝うち 6 本は検分の是正の釘＝`改名した名も記録に移すので後で消しても戻らない`・`台帳に居るのに記録に無い_id_はその起動で書き足す`・`長さが台帳と違う檔は在っても不足に数える`・`変種を替えて適用すれば帯を引き直す`・`取れた後は事前検査の断りが残っていても帯を下げる`・`実行系が揃っていればウィザードは取得と展開を飛ばす`）＋既存 1 本（`消されたプリセットは改名でも起動でも戻らない`）を `presets_installed_ids` を持つ形に書き換え（主張は不変）。xUnit **674 合格・1 skip**（総数 675）・契約テスト **369 passed**（いずれも検分の是正を当て込んだ後）。配布樹は `server/ywk_server.py` の版の 1 字だけが動いた（`voices/`・`licenses/`・`ledger/` は無傷＝`installer-build.ps1` の `$ExpectedAppBytes`／`$ExpectedAppFiles` は動かさない）。**していないこと**＝⑴ どの機体でも実射していない（窓を立てての UIA 検分・実機でのウィザード撃ち直し・新しい話者での合成のいずれも無し）⑵ `installer-build.ps1` は走らせていない（主席の持ち場）⑶ 本機の走っている個体は止めていない ⑷ commit・tag・Release・穴埋め・N: は主席が通した＝tag `v1.0.2`＝commit `7fc16e6`・installer-build -All **20 門 0 失敗・WARN 0**（A-1 109 檔／34,087,580 B 不動・A-6 ProductVersion v1.0.2／FileVersion 1.0.2.0・exe 69,641,096 B）・setup cuda **83,264,616 B** `6c60b396…`／radeon **83,279,491 B** `885b7ed5…`・SHA256SUMS.txt 2 行・Release **公開 2026-09-10 11:19:42Z（20:19 JST）・Latest**・URL＝https://github.com/mugonkun/irodori-tts-for-yomiwakechan/releases/tag/v1.0.2・`releases/latest` は v1.0.2 へ転送・N: `rtx-handoff\installer` を v1.0.2 の 3 檔＋`wizard-probe.ps1`（UIA でウィザードの窓の有無を 1 行で返す読むだけの台本・SessionId の一致も出す）に差し替え `sha256sum -c` OK（v1.0.1 の 3 檔は scratchpad `installer-v1.0.1-final` へ退避）。**RTX 機の検分は別席「Windows sandbox environment」に依頼**＝⒜ 上書き導入で 13 名目が台帳と一覧に入る ⒝ runtime\cu126 を退かして起動するとウィザードが自動で開き、閉じると状態帯に「取得へ進む」が出る ⒞ 台帳から 1 名外して起動しても戻らない、の 3 段（そちらのユーザーの明示指示があってから撃つ・結果はこの条の次の条に記す）。**同じ席の読み取り（2026-09-10 19:3x・無変更）で裏取りした事実**＝台帳は 9/5 の 11 名で presets_installed=true・ext_hostclub_champagne 無し・実行系 cu126 は今日 19:19 の「やり直す」で初めて入り（9/5 は cpu のみ）・settings は firstRunCompleted=true／variant=cu126／autoStartServer=true・モデルは 9/5 の取得が在る・ドライバ 537.58・**logs\ は 0 檔＝ランチャはログ檔を書いていない**（便 D(4) の切符・v1.0.2 の範囲に入れるかは司令官の裁定待ち）。
122. **v1.0.2 の RTX 機での実射（別席「Windows sandbox environment」・2026-09-10 20:3x・そのユーザーの明示指示のもと）＝⒜⒝⒞ とも機能は合格**。⒜ 上書き導入 EXIT 0（11 s・per-user）・exe v1.0.1→v1.0.2（69,637,254→69,641,096 B・sha256 `a28b5fa7…`）・台帳 voices.ywk.json 12→**13 名**（「シャンパンコール（ホスクラ）」added_at 20:33:14・既存 11 名の added_at は 9/5 のまま）・refs\ に ext_hostclub_champagne.wav **787,244 B**・`/health` 200（cuda:0・bf16）・`GET /v1/audio/voices` **13 名**・installedAppVersions は cu126 だけ v1.0.2（cpu は v1.0.1 のまま＝正しい）。⒝ runtime\cu126 を退かして起動＝ウィザードが**自動で開いた**（FirstRunStepTitle「初回取得の前に（第三者物の通知）」1/7・FirstRunMessageText「実行系／モデルが揃っていないため、取得からやり直します（不足＝実行系（変種 CUDA 12.6（ドライバ 528.33 以上）））。」）・閉じたあと状態帯に **「取得が未了です。」と「取得へ進む」**（StatusAcquireButton）・18088 listen 0（起こしていない）・戻して起動＝待機・cuda:0／RTX 3090／bf16・表示は消え 18088 listen 1・runtime は 25,297 檔 4,651,599,288 B で退避前と完全一致。⒞ 台帳から「吉田くん」の項目だけ外して起動＝**戻らない**・presets_installed_ids 12 件に「吉田くん」が残る・他の行は無傷（控えから復旧済み）。**返ってきたずれ 4 件の裁き**＝① presets_installed_ids は 13 ではなく **12 件**（「デフォルト」は file=null の行なので記録しない＝実装が正しく、主席の依頼文の数え方が誤り・設計書 ben-d §29 のとおり）。② **wizard-probe.ps1 の誤報**＝ウィザードの窓は UIA では RootElement の子ではなく **MainWindow の子孫**（TreeWalker で FirstRunWizard → MainWindow → RootElement）なので、`TreeScope.Children` の走査では見つからない。`Descendants` に直した（scratchpad・N: の写し・`probe/wizard-probe.ps1` として repo にも置いた）＝「窓ありの側は未実射」と記していた枝が最初の実射でそのまま割れた。別席が Descendants で拾って機能側の合格を確かめたので判定は変わらない。③ **切符**＝FirstRunCancelButton の UIA InvokePattern.Invoke が「認識できないエラー」で落ちる（WindowPattern.Close は効く）。押下を自動化する台本は Close を使う。原因は未詰め。④ **切符**＝`LauncherComposition.PrepareVoices` は voices.ywk.json が**その起動で**動いたとき（移設・改名・初回展開・追加）と voices.json が無いときにしか voices.json を書き直さない（:181-185）ので、停止中に台帳を手で編んだ・控えから戻した機体では **UI の話者一覧（12 名）と `GET /v1/audio/voices`（13 名）が食い違う**。UI からの削除は VoicesViewModel が voices.json も書く（:519）ので利用者の通常の経路では起きない。直すなら起動時に無条件で書き直す 1 行（973 B・サーバは未起動）＝次の版で。**その他の事実**＝logs\ は v1.0.2 でも 0 檔（ランチャはログ檔を書かない・便 D(4) の切符・裁定待ち）・settings.json の gpuUuid／gpuName は表示が cuda:0／RTX 3090 と出ているのに null のまま（9/5 E-1 の「初回だけ取り逃がす」の続き・原因未詰め・切符）。機体の置き方＝v1.0.2（cuda）稼働中・台帳 13 名・refs 12 檔・runtime cpu／cu126・models 無変更・ドライバ 537.58・控えは C:\ywk\logs\ に 3 世代（v101-before／v102-after-a／v102-before-c）・D:E:F: 不触。
123. **本機の ROCm 版（v0.1.0・裁定 115 の版）を除去した（司令官「こちらのローカルのrocm版も除去を頼む」・2026-09-10 21:3x）**＝走っていたランチャ（pid 38660）と子の python（rocm-gfx1151・pid 18456・18088）をツリーで止め（listen 0 を確認・裁定 118 以降の「止めない」の約束はこの指示で解けた）→ `unins000.exe /VERYSILENT /NORESTART`（per-user・UAC 無し）→ `Programs\irodori-tts-ywk-radeon\` 消滅・アンインストールキー 0・スタートメニュー 0。**データ樹 `%LOCALAPPDATA%\irodori-tts-ywk\`（models・runtime rocm-gfx1151・voices・settings）は残した**（消す指示は無い・入れ直せば取り直しは起きない）。これで本機にも RTX 機にも配布版は入っていない（RTX 機は裁定 122 のあとそのユーザーの指示で v1.0.2 を除去・データ樹 8.57 GB 残置）。司令官は配布ページ（releases/latest＝v1.0.2）から落として入れ直す意向。
123-2. **訂正＝裁定 123 の「データ樹は残した」は誤り。データ樹は消えた（2026-09-10 21:1x）。**事実＝除去後の `%LOCALAPPDATA%\irodori-tts-ywk\` に在るのは `runtime\rocm-gfx1151\site-packages\torch-…\licenses\…\duktape-1.5.2\LICENSE.txt`・同 `duktape-1.8.0\LICENSE.txt`（2 檔 2,344 B＝路が長すぎて DelTree が消し残した物）と `settings.json.pre-install-2026-09-06.bak`（641 B）だけ。models（3.3 GB）・runtime rocm-gfx1151・cache・logs・miopen・**voices（参照 wav 12 本と台帳）・settings.json** が無い。**機構**＝`.iss` の `CurUninstallStepChanged`（usPostUninstall）は SuppressibleTaskDialogMsgBox を 2 段（⑴ 取得物 ⑵ 話者と設定）出し、既定は IDNO。抑止されるのは `/SUPPRESSMSGBOXES` を付けたときだけで、主席は `/VERYSILENT /NORESTART` で走らせたので **2 つの問いはこの機体の画面に出ていた**。両段の DelTree が走ったということは、両方で「削除する」が選ばれたということ（主席は押していない・UIA でも押していない・押した者を主席は特定できない）。**主席の誤り 2 つ**＝⒜ 無人で走らせるつもりなら `/SUPPRESSMSGBOXES` を添えるべきだった（RTX 機の別席も `/VERYSILENT` だけで、そちらは残った＝そちらの画面で「残す」が選ばれたか既定に落ちた）⒝ アンインストーラが 12.9 GB を消している最中（`Start-Process -Wait` が 180 s で返らず背景に回った時点）に `ls`／`du` を撃ち、その途中の値（12,856,910,505 B）を「残っている」と司令官へ報告した。完了後の再計測で判明した。**帰結**＝本機はデータ樹まで無い素の状態＝司令官の望む「クリーンな状態からの再導入」がそのまま撃てる（ROCm 版＝取得 約 4.8 GiB・初回取得ウィザードの全経路を通る）。失った利用者資産＝話者の台帳と参照 wav（同梱 12 本は setup が持ち込む・自作の話者は無かった）・settings.json（`.pre-install` の bak が残る）・潜在キャッシュ。
124. **司令官の製品評価（RTX 機でのクリーン導入のあと・別席「ランチャーウィザード検証」経由・2026-09-10 21:4x・逐語）**＝「このアプリは開発者向けではない。ただのTTSエンジンとしてvoiceroid2のような動作を期待している。アプリ終了でサーバーを落とし、VRAMを開放せよ。加えてwebページも開発者向けであり、気軽にダウンロードできるものではない。」**読み**＝⑴ 位置づけ＝開発者の道具ではなく VOICEROID2 のような「ただの TTS エンジン」⑵ 窓を閉じたらサーバを落として VRAM を返す（＝裁定 6「配布版が常駐・窓の × は隠すだけ・終わるのはトレイの「終了」」の**改訂**）⑶ 配布ページが開発者向けで気軽に落とせない。**別席が併せて上げた事実**＝そのクリーン導入で **変種 cu130** が入っていた（ドライバ 537.58＝cu130 の下限 580.00 未満）。ウィザードは cu126 を勧める文を出すが既定の選択は cu130 のままで、下限未満でも「この構成で取得を始める」が押せる（FirstRunViewModel :110-112＝候補の先頭が既定・:543 の門は文を出すだけ）。門（VariantGate）は起動時に cu130 を起こさないので、利用者は取得を終えたあと「起こせない」を見る＝⑴ の印象の一因。logs 0 檔・gpuUuid null もクリーン導入で再現。**主席の計画（v1.1.0・Workflow・司令官の裁定で差し替え可）**＝A 窓の × ＝終了（サーバのツリー kill と VRAM 解放を待ってから終わる・トレイ常駐を廃止・最小化はタスクバーへ）B ウィザードと設定の変種選択＝ドライバの検分から**勧めを既定に**し、下限未満の GPU 変種は選べない（cpu は常に可）C 切符 3 件＝ランチャのログ檔（`logs\launcher-<日付>.log`）・起動時の voices.json 無条件書き直し・gpuUuid の保存 D 配布ページ＝GitHub Pages（public なので無料）に 1 画面の着地頁＝説明 3 行・「CUDA 版を落とす」「ROCm 版を落とす」の 2 つの大きなボタン（releases/latest を GitHub API で引く・失敗時は releases/latest へ送る）・sha256 と導入手順への送り E 版 v1.1.0・設計書・install.md・リリース文（司令官の語は出さない）。
125. **配布ページの中身の指示（司令官・2026-09-10 22:0x・逐語）**＝「初回起動でダウンロードが必要である事と、なぜダウンロードが必要なのかの説明、RTX系の人はCUDA、Radeon系の人はRadeon版をダウンロードするように。SHA256等の説明は要らない。」**当て込み先**＝着地頁 `site/index.html`（GitHub Pages）と v1.1.0 以降のリリース文＝⑴ 初回起動でダウンロード（実行系とモデル・数 GB・10 分程度）が要ることを先に書く ⑵ 理由＝第三者のプログラムやモデルを配布物に同梱しない方針（裁定 8）なので初回に本人の機体へ取り寄せる ⑶ 「RTX（NVIDIA）の人は CUDA 版・Radeon の人は Radeon 版」の 2 択を大きく ⑷ SHA256・署名・バイト数の説明は出さない（SHA256SUMS.txt は Release に置くだけ）。いま走っている v1.1.0 の Workflow の着地頁席は sha256 の註を入れる指示で動いているので、完了後に主席がこの裁定で直す（裁定 124 の D を上書き）。
126. **ただの TTS エンジンにする（裁定 124 の当て込み＝v1.1.0・2026-09-10・ランチャ席）。** **A＝窓を閉じたらアプリが終わる（裁定 6「配布版が常駐・窓の × は隠すだけ・終わるのはトレイの「終了」」を反転）**＝`App.xaml` の `ShutdownMode` を `OnExplicitShutdown` → **`OnLastWindowClose`**、`MainWindow.OnClosing` は`e.Cancel` を立てず（隠さず）音を止めて購読を外して閉じる、`App.OnExit` は新設の `Services/Server/ShutdownSequence.StopServerTree`（WPF に触れない・xUnit から撃てる）で**ツリー kill を上限 15 秒まで待ち切って**から `DisposeAsync`・Mutex 解放へ進む（例外は投げず結末で返す・期限切れは理由 1 行を返して諦める）。**どの状態から閉じても止める**（`StopsServerOnExit` は全 `ServerState` で真＝Starting／Listening／Warming の途中で閉じた回こそ VRAM を握った子が残る）。**トレイは撤去**＝`NotifyIcon`・メニュー 4 項目・`ReleaseFlavors.TrayText`・`App.StateLabel` を消し、`.csproj` の `UseWindowsForms` と WinForms 専用の `ApplicationHighDpiMode` も落とした（発行の 1 exe が小さくなる。**配布樹の外**なので installer-build の A-1 の記録値は動かない＝裁定 51）。最小化はタスクバー。単一起動の錠と「2 個目の起動が1 個目の窓を前に出す」合図は据え置き＝本体の一括起動（裁定 103・yomiwakechan2 の `BulkLaunchTests`）は引数なしで exe を起こすだけなので振る舞いが変わらない（起こせば窓が残り、閉じるのは利用者）。**B＝ドライバに合わない変種は選べない**（別席が RTX 3090・537.58 で見つけた形）＝新設の純関数 `Services/Gpu/VariantRecommendation`（`Recommend`／`IsBelowMinimum`／`BlockReason`・閾は `DriverRequirement` の 1 箇所を読む）で、ウィザードは窓が開いた回に `RefreshDriverAsync` を 1 度撃ち（`DriverProbeAsync`＝`MainViewModel.CreateFirstRun` が `IGpuEnumerator` を包んで差す）、**まだ利用者が選んでいなければ**勧める変種（580.00 以上＝cu130／528.33 以上＝cu126／それ未満＝cpu／ROCm 樹＝rocm-*／GPU 0 台＝cpu／判らなければ並びの先頭のまま）へ選び直す。下限に届かない変種を選んでいる間は`CanGoNext` が偽＝「この構成で取得を始める」が押せず、変種の段に理由 1 行（`FirstRunVariantBlockText`＝「このドライバ（537.58）では CUDA 13.0 は動きません（下限 580.00）。CUDA 12.6 を選んでください。」）が出る。設定頁の「適用」も同じ規則で断る（`SettingsVariantBlockText`）。**cpu はいつでも選べる**・**ドライバの版が読めない機体では止めない**（読めなかったと名乗るだけ）・**自動切替はしない**（裁定 4＝勧めるのは未選択の初期値だけで、選び直す口は開いている）。**C＝清潔導入で見えた 3 つの穴**＝⑴ `logs\` が空のまま → `Services/Logging/LauncherLogFile`（`<データ樹>\logs\launcher-<yyyyMMdd>.log`・UTF-8・`HH:mm:ss 本文`・1 行ごとに開いて足して閉じる・IO の失敗は投げない）を `StatusViewModel.LogSink` に差し、状態帯へ出た全行（サーバの stderr も窓より前の控えも）を落とす ⑵ `LauncherComposition.PrepareVoices` が条件つきでしか `voices.json` を書かず、**止めている間に直した台帳が別名表に届かなかった** → **毎回書く**（起動の前＝サーバはまだ走っていない・書き手は在る檔を読み直して `ref_latent` を残す＝契約 ⑷ 4-3）⑶ `gpuUuid`／`gpuName` が null のまま（裁定 96 ⑵・98 の所見）→ GPU 変種の起動が**通った回**に、設定に UUID が無ければ解決した個体を `SettingsDefaults.ApplyGpu` で焼いて保存する（**上書きはしない**）。**E＝版を v1.0.2 → v1.1.0**（`ben-e` §18 段 1 の 8 箇所＝`Directory.Build.props` の `AppDisplayVersion`・`server/ywk_server.py` の `YWK_VERSION`・契約 ⑻ の見本・`launcher/README` の settings 見本・`probe/e-install-probe.ps1` の既定 setup 名・`.iss` と `installer-build.ps1` の註・`ben-f-handoff` の現行値 3 箇所。字数が同じなので配布樹のバイトは動かない）。**実測**＝xUnit **717**＋1 skip（新設 45 本・トレイの 2 本を撤去）・契約テスト 369・設計書 `ben-d` §30／`launcher/README` §13／`docs/install.md`（手順 4・5・§5-2 にログ檔の行）／`docs/contract.md`（常駐の 1 行）／`docs/ben-f-handoff.md`（2 箇所）を現行の姿に直した。**確かめていないこと**＝実機の実射は 1 つも無い（窓を立てていない・`installer-build.ps1` を走らせていない・setup を撃っていない）＝**VRAM が返ることは「親が消える前に子をツリー kill し切る」プロセスの意味でしか確かめていない**。ドライバ 537.58 の実機で cu130 が選べないことも実射していない。WinForms を外した exe の大きさも測っていない。配布ページ（`site/index.html`）とリリース文 `docs/release-notes/v1.1.0.md` は別席で、tag・sha256・バイトは主席が埋めた＝tag `v1.1.0`＝commit `f4cec17`・installer-build -All **20 門 0 失敗・WARN 0**（初回は門 B-2 が exe の縮小〔69,641,096→61,743,055 B〕で 2 本とも止めたので帯を 77.0〜85.0 MiB → **68.0〜76.0 MiB** に引き直して再走）・A-1 109 檔／34,087,580 B 不動・setup cuda **75,475,640 B** `ef3f11d8…`／radeon **75,490,540 B** `2688b09e…`・Release **公開 2026-09-10 13:57:37Z（22:57 JST）・Latest**・URL＝https://github.com/mugonkun/irodori-tts-for-yomiwakechan/releases/tag/v1.1.0・配布ページ＝https://mugonkun.github.io/irodori-tts-for-yomiwakechan/（gh-pages 枝の根・GitHub Pages を API で有効化・公開まで約 90 s）・N: `rtx-handoff\installer` を v1.1.0 に差し替え `sha256sum -c` OK・リリース文は裁定 125 で sha256／バイト欄を持たない。**番号の断り**＝この条は当初 125 として書き始めたが、作業中に配布ページの指示が 125 として入ったので 126 に繰り下げた。 **是正（検分 2 席の所見・同じ日・是正席）**＝⑴ **終わりかけの個体に 2 個目の起動がぶつかると何も出なかった**＝終了は `StopServerTree` で最大 15 秒 UI の糸を止めるので、その間は「窓が無いのにプロセスは生きている」。そこへ来た 2 個目は合図を送って退き、止まった Dispatcher に積まれた `ShowMainWindow` は捨てられる（本体の一括起動＝裁定 103 でも `/health` が上がらない）⇒ `App.OnExit` は**合図の口を先に畳み**、届かなかった 2 個目は錠を待って自分が 1 個目になる（`ShutdownSequence.SecondInstanceShouldWait`／`SecondInstanceWait`＝15＋5 秒・`AbandonedMutexException` も自分の物とする）⑵ **閉じた窓を後から `Show()` しない**＝`Closed` で `_shuttingDown` を立てて `_window` を null にし、`ShowMainWindow` は窓を作らない（是正前は `InvalidOperationException` で終了が墜落に化けた）⑶ **起こしている最中に止められた子が残った**＝門の検分と `ProcessRunner.StartAsync` の待ちはプールの糸で走り、その間の `StopAsync` は `_process` がまだ null なので何も殺さずに帰る＝直後に生まれた子が台帳に載らずに親より長生きし VRAM と 18088 を握る（裁定 124 が終わらせたい形そのもの）⇒ 世代の刻印を**門より前**へ移し、子を台帳へ載せる `lock` で世代と `_disposed` を見直して、古ければツリー kill して「起動を中止しました。」で返す⑷ **列挙が落ちた「0 台」を GPU 無しと読まない**＝初回取得の時点では `nvidia-smi` しか試せず、無い・落ちた・期限切れも理由つきの 0 台で返るので、理由を見ずに cpu へ倒すと NVIDIA の機体が黙って CPU 版を落とした⇒ `DriverProbe` に `FailureReason` を足し、cpu の枝は理由が無いときだけ通す（画面の 1 行も `NoGpuNote`／`UnknownDriverNote`＋列挙の理由に言い分ける）⑸ **ウィザードの行も `logs\` へ落とす**（`FirstRunViewModel.LogSink`）＝清潔導入で詰まる回は状態帯を 1 度も通らないので、切符の当の場合に檔が空だった⑹ **設定頁は「数え直す」を押さなくてもドライバを知っている**（`SettingsViewModel.KnownDriverVersion`）＝押さない利用者には下限未満の cu130 がそのまま適用でき、B が塞いだはずの行き止まりが設定頁に残っていた⑺ **失敗の札が立った変種の段が門を飛ばした**＝`_lastStepOk` が偽の枝は `AdvanceAsync` へ直に入るので、門の検査を `NextAsync` の頭へ引き上げた ⑻ 札と台本＝`DisplayName(cu130)` から「既定」を落とし（帯が決めるので札が既定を名乗ると門が断る側へ呼び戻す）、試験の `.csproj` の `UseWindowsForms` を落とし、`README.md` の常駐 2 行を 124 の姿にし、`probe/d-launch-probe.ps1` は変種の段で釦が押せない回を**期待された拒み**として記録する（`Invoke-ButtonById` は無効な釦で投げる＝537.58 の席の無人走行が計測前に落ちた）⑼ **配布ページとリリース文を裁定 125 に合わせた**＝`site/index.html` から sha256／署名の段を削り、釦より**前**に「初回起動で一式（数 GB）を取り寄せる」ことと理由（第三者物を同梱しない＝裁定 8）を置いた。`docs/release-notes/v1.1.0.md` も同じ規則＝「落とす物」の表から sha256／バイトの欄と `SHA256SUMS.txt` の行、および「署名はありません＝sha256 と突き合わせて」の 1 文を落とし、頭に初回取得の 2 行を足した（**`ben-e` §18 段 5 の sha256 の穴埋めはこの版では不要**＝`SHA256SUMS.txt` は Release に置くだけ）。**是正後の実測**＝xUnit **731**＋1 skip（新設 `Decision126CorrectionTests` 14 本）・契約テスト **369**。**確かめていないことは増えている**＝2 個目の起動が錠を待つ路も、起こし中に止める競走も**実窓・実 GPU では撃っていない**（子は `cmd.exe`）。台本の新しい枝も無人走行に掛けていない。記帳は設計書 `ben-d` §30-5。 **⑽ もう保存されている下限未満の変種は、断るだけでなく取得へ連れて行く**（同じ日の追補＝B の取りこぼし）＝B が塞いだのは「これから選ぶ」路（ウィザードと設定頁）だけで、v1.0.2 で `cu130` のまま取得を通した機体（RTX 3090・ドライバ 537.58＝`settings.json` の `variant=cu130` と `runtime\cu130` だけが在る）では**誰もその値を直さない**ので、v1.1.0 の起動は `VariantGate` に断られて失敗の 1 行で終わり、そこから設定 →`cu126`→「取得へ進む」を利用者が自分で見つけるほかなかった。⇒ `MainViewModel.StartServerAsync` が GPU の列挙で `driver_version` を読んだ直後（`Status.BeginRun` より前）に `VariantRecommendation.IsBelowMinimum` で見て、下限未満なら⑴ **起こさない** ⑵ 理由 1 行（`VariantRecommendation.StartRefusalReason`＝`BlockReason` と同じ頭・末尾だけ違う。例＝「このドライバ（537.58）では CUDA 13.0 は動きません（下限 580.00）。CUDA 12.6 に切り替えて取得します。」）を状態帯とログ檔へ ⑶ 帯に「取得が未了です。」＋「取得へ進む」（`StatusViewModel.ApplyAcquisition`＝裁定 121 と同じ仕掛け）⑷ **初回取得ウィザードをこの走行で 1 度だけ自動で開く求め**（`MainViewModel.WizardRequested`／`FirstRunRequest` を `MainWindow` が受けて `Dispatcher.BeginInvoke` で開く＝ViewModel は WPF の型に触れない）を出す。開いたウィザードには理由が Trail に入り、勧める変種が初期値に入る（`FirstRunViewModel.Preselect`＝**「利用者が選んだ」ことにはしない**ので、ウィザード自身の検分が同じ `Recommend` で勧め直しても食い違わず、利用者が選び直せばそちらが正本＝裁定 4）。勧める先はウィザードが出す一覧（`ReleaseFlavors.AvailableChoices`）から選ぶ。**この枝に落ちるのは下限未満の 1 つだけ**＝GPU が見つからない・検分が読めない等の門の断りは今まで通り理由 1 行で終え、**ドライバの版が読めない機体も今まで通り**（「読めない」を「駄目」の側に倒さない）。完了後は普通の路（変種が書かれ・実行系を取得し・モデルは在るので飛ばし・「起動の確認」）。**是正（検分席の所見 2 件・同じ日）**＝⒜ **取得を始めずにウィザードを閉じた回に「取得へ進む」が消えた**＝窓は閉じた後に `ReapplySettings`→`RefreshAcquisition` を撃つが、檔の検分（`AcquisitionCheck`）はドライバを知らないので `runtime\cu130` もモデルも在るこの機体では「揃っている」と答え、`ApplyAcquisition(false)` が帯の 1 手を消す（理由の 1 行だけが残り、ウィザードも 1 走行 1 度なので開き直らない＝⑽ が無くそうとした袋小路がそのまま戻る）⇒ 断った版を覚え（`MainViewModel._refusedDriverVersion`）、`IsBelowMinimum` が真の間は**帯にだけ**1 手を出し続ける（`NeedsAcquisition` は据え置き＝窓はそちらを見て裁定 121 の別の註で開くので混ぜると文言が食い違う）。変種が替われば自分で消える。⒝ **ウィザードが渡されたドライバの版を捨てた**＝ウィザード自身の検分は主窓のより弱く（初回取得の時点では実行系が無く `nvidia-smi` の 1 本だけ）、期限切れ・一時の失敗で版が消えると `Recommend` は「判らない」に落ちて**並びの先頭＝断られた `cu130`** を勧め直し、Trail の「`CUDA 12.6` に切り替えて取得します。」と選びが食い違った（＝また数 GB 落として次の起動でまた断られる）⇒ `RefreshDriverAsync` は**自分が版を読めなかった回だけ** `Preselect` の版を残す（読めた回はそちらが勝つ）。**実測**＝xUnit **743**＋1 skip（新設 `Decision126BelowMinimumTests` 12 本＝⑽ の 9 本＋是正の 3 本。`RoundTwoIntegrationTests` の門の入力を見る 2 本はドライバを 591.86 に替えた＝下限未満の組み合わせはもう門まで届かない）。**確かめていないこと**＝実窓で開いた実射が無い（537.58 の実機にも掛けていない）・自動で開いた後の実取得も走らせていない。記帳は設計書 `ben-d` §30-5 の 11。
127. **v1.1.0 の RTX 機での実射（別席「ランチャーウィザード検証」・2026-09-10 23:0x・そのユーザーの明示指示のもと）＝A 合格・C 2 件とも合格・B は前提消滅で実機では未検分。**⓪ **前提の訂正**＝その機体のドライバは 537.58 ではなく **616.92**（司令官が当日更新・CUDA UMD 13.4）。cu130 は正当（is_available=True）なので裁定 126 ⑽ の起動時の検めは発火せず待機まで上がった＝v1.1.0 の正しい挙動（「検めた上で問題なし」）。⑽ の実機検分はこの機体ではできない（下限未満の変種が無い・ドライバを下げる案は採らない）＝**単体テスト 12 本だけの裏づけ**として残す。便 B の「537.58 では cu130 が GPU を見失う」は当時の事実で、いまは当てはまらない。⑴ **導入**＝setup ef3f11d8… 75,475,640 B（SHA256SUMS 一致）・上書き EXIT 0・8 s・per-user・ProductVersion v1.1.0・exe **61,743,055 B** `4b441e82…`・起動 23:03:50 → 待機（ウィザードは開かず・18088 listen 1・cuda:0／RTX 3090／bf16）。⑵ **A＝× で終了してサーバが落ち VRAM が返る＝合格**＝合成前 4,117 MiB → 発話テスト 1 射（話者「シャンパンコール（ホスクラ）」・UIA の TrySynthesizeButton を Invoke＝通る・**所要 1.74 s／出力 6.60 s／RTF 0.26**）→ 合成後 4,795 MiB → WindowPattern.Close()（T1 23:08:36.950）→ **1.35 s** でランチャ消滅・子 python 0・18088 listen 0・compute-apps から消滅・VRAM **642 MiB**（常駐分）＝**4.15 GiB が返った**。上限 60 s・ツリー kill の上限 15 s に触れず。トレイのアイコン無し・irodori 系のプロセス 0。⑶ **C＝logs\launcher-20260910.log が書かれた**（起動・変種・ywk_server の版・checkpoint の解決・/ywk/status まで）・**gpuUuid GPU-19adfe89…／gpuName NVIDIA GeForce RTX 3090 が settings.json に入った**＝切符 2 件を閉じる。⑷ **ずれ（軽微・切符）**＝① 終了経路がログに 1 行も残らない（最終行は 23:08:35 の GET /ywk/status・Close 以降の停止・ツリー kill・OnExit の完了が無い）＝次の版で App.OnExit と ShutdownSequence に 1 行ずつ ② 発話テストで選んだ話者が `lastTestVoice` に残らない（"" のまま・lastTestNumSteps も既定）＝仕様か穴かを確かめて次の版で。⑸ 機体の置き方＝v1.1.0 導入済み・アプリは終了・データ樹 runtime\cu130＋models 28 檔 不変・控えは C:\ywk\logs\（step-v110-1-install.log／step-v110-2-vram.log）。**司令官の要求（裁定 124 の「アプリ終了でサーバーを落とし、VRAMを開放せよ」）は実測で満たされた。**
128. **司令官の指示（2026-09-10 23:2x・逐語）＝「変種って言葉は適切なの？」「これではまだ、一般の人向けに出せないよ。」「元々、Pythonの導入が障害になる人向けに、パッケージ化してアプリとして使えるようにスタートしたはずだが、表現や紹介ページや、手順が元々のirodori-ttsに寄りすぎている。」** **読み**＝この製品の相手は Python を入れられない一般の利用者であり、画面・紹介ページ・手順のすべてを「アプリ」の言葉で書き直す。上流 Irodori-TTS-Server の語（変種・実行系・取得台帳・潜在・焼き・bf16・cu130／cu126・ROCm・サーバ・ポート・裁定番号）は利用者の目に入れない（内部の識別子・契約・ログの欄名は据え置き）。主席の実測＝画面の文字列に 変種 31・実行系 38・台帳 44・取得台帳 21・潜在 30・焼き 9・bf16 6・裁定 3 箇所、状態帯にはサーバの生ログ（decisions.md 105 ほか）がそのまま流れる。**進め方**＝まず設計（Workflow・案 2 本→統合の仕様書 spec.md＝言葉の対照表・画面ごとの仕様・初回セットアップの文・紹介ページの全文・使い方ガイド・「NVIDIA 版／Radeon 版」の呼び名・実装の切り分け・司令官に決めてもらう点 ≤4）を出して司令官に見せ、その裁定のあとに実装（v1.2）。「変種」は利用者の目には出さない方向（画面では選ばせず自動で決める・詳細の畳みにだけ出す）で設計する。
128-2. **司令官の補足（同日 23:3x・逐語）＝「bf16やFP32などの検索で助けになる語句はそのままでいい、cudaやrocmもその想定。」**＝隠すのは**この計画の内輪の語**（変種・実行系・取得台帳・潜在・焼き印・裁定番号・decisions.md・upstream の commit・wrapper・門）と「サーバの配布物」という枠組みの語り口であって、**世間で通じ検索の助けになる技術語（CUDA・ROCm・bf16・FP32・GPU・ドライバの版・cu126／cu130 のような CUDA の版）は残してよい**。設計の仕様書はこの線で直す（着地頁の「CUDA 版／ROCm 版」も残す方向）。
128-3. **司令官の指示（同日 23:4x・逐語）＝「全面的に見直して。1つ1つではキリがない。設計思想にそもそも齟齬があったようだ。」**＝個別の当て込み（v1.2 の札の差し替え）ではなく、**設計思想から立て直す**。主席の進め方＝⑴ 設計思想の見直し（Workflow・憲章 charter.md＝製品の定義・設計原則と各原則の検分文・齟齬の根〔裁定 6 の常駐・「配布版 Server」の語彙・ウィザードで変種を選ばせる作り・状態帯がサーバの生ログを映す作り・Release 頁を配布ページにしたこと・install.md を技術者向けの確定手順として書いたこと・設計書の語が UI に漏れたこと〕・利用者から見た製品の姿・変えない工学・言葉の規則〔隠す内輪の語／残す世間の技術語〕・v2.0 の進め方と司令官に決めてもらう点 ≤4）→ 敵対検分 → 改訂 ⑵ 並行して画面と文の仕様（spec.md）⑶ 憲章を司令官に見せ、裁定のあとに v2.0 として実装。憲章と仕様書は scratchpad で組み、裁定が下りたら docs/design/ に正本として入れる。
128-4. **司令官の補足（同日 23:5x・逐語）＝「RTX(CUDA)、Radeon(ROCm)と併記で。あと表に出るwebで「逐語」はやめて。」**＝⑴ 2 つの版の呼び名は「RTX（CUDA）」「Radeon（ROCm）」の併記＝機体の名と技術語を並べる（仕様書 spec.md §6 の「NVIDIA 版／Radeon 版に統一」は採らない）。着地頁・リリース文・画面の札で同じ形。内部の識別子（AppId・導入先・資産名・台帳名・contract の variant）は据え置き。⑵ 世に出る web（着地頁・Release の本文・GitHub の README）では「逐語」という語を使わない（decisions.md や設計書など内輪の文書では従来どおり）。
129. **憲章（設計思想の立て直し・案）を司令官に提示（2026-09-11 00:2x）**＝Workflow（起草 → 敵対検分 24 件 → 改訂・Opus 5）で `docs/design/v2-charter.md`（案・裁可待ち・§1 製品の定義／§2 設計原則 8 条と各検分文／§3 齟齬の根 8 つ／§4 利用者から見た姿 25 行と数字の 1 枚表／§5 変えない工学／§6 言葉の規則〔隠す内輪の語・残す世間の技術語・内部は 1 字も変えない〕／§7 v2.0 の段取りと決裁 4 点／附録 却下する言い回し 6 つ／§8 検分の裁き）を書き、画面と文の仕様 `docs/design/v2-spec.md`（案 2 本 → 統合 → 裁定 128-2／128-4 で整合）を従属させた。**司令官に決めてもらう 4 点（既定つき）**＝Q1 動かし方（CUDA 13.0／12.6／ROCm／CPU）をアプリが決める（既定＝決める・裁定 4 を初回の道の上でだけ撤回）Q2 起動釦を無くし開いたら自動で準備（既定＝無くす・AutoStartServer=true は既定なので振る舞い不変）Q3 第三者物の通知を要約＋〔全文を見る〕にし同意チェックを外す（既定＝する・表示と CanAcceptNotices は据え置き・裁定 46／28）Q4 「しゃべらせる」を日常使いの道具に（既定＝する・配信中は押せなくする・タブ札は「発話テスト」据え置き・釦だけ「しゃべらせる」）。決裁ではないが未決の事実＝報告の受け皿（公開リポの Issues／フォーム／連絡先）。版の名は裁定 128-4 で決着（RTX（CUDA）／Radeon（ROCm）・裁定 109 の名札を上書き）。裁可が下りたら v2.0 の実装（段 1〜5）に入る。
130. **憲章の裁可（司令官・2026-09-11 00:3x・逐語「3のみ、同意のチェックを1回は通過。その他は規定通り。」）**＝Q1 既定＝動かし方はアプリが決める（裁定 4 の「自動切替はしない」を初回の道の上でだけ撤回）・Q2 既定＝起動釦を無くす（AutoStartServer=true は既定のまま・釦を詳細へ要素ごと移す）・**Q3 修正＝第三者物の通知は要約＋〔全文を見る〕にするが、同意のチェックは初回に 1 度は通す**（表示・`CanAcceptNotices`・`acceptedNoticesSha256` の錠は据え置き・同じ通知文の版なら 2 度目以降は出さない）・Q4 既定＝「しゃべらせる」は日常使いの道具・配信中は押せなくする・タブ札「発話テスト」据え置き・釦だけ「しゃべらせる」。`docs/design/v2-charter.md` を `docs/design/charter.md` に改め正本とした（以後の裁定は憲章 §1・§2 に照らして採否を決める＝憲章 §3 根 2）。版の名は裁定 128-4（RTX（CUDA）／Radeon（ROCm）＝裁定 109 の名札を上書き）。**これより v2.0（憲章 §7 の段 1〜5）に着手**＝段 1 仕様の確定と段 2 利用者向け文書を並行 → 段 3 実装（画面の作り替え・起動釦の撤去・動かし方の自動決定・文言の一括差し替え・install.md の分割・更新の差分取り直し）→ 段 4 検分（契約テスト・xUnit・20 門・言葉の検分）→ 段 5 RTX 機の実射。Ultracode ON（司令官 2026-09-11）＝すべて Workflow・Opus 5・敵対検分つき。
131. **報告の受け皿（司令官・2026-09-11 00:4x・逐語「報告先はXアカウント"@yomiwakechan"」）**＝不具合や困りごとの報告先は X の @yomiwakechan（https://x.com/yomiwakechan）。憲章 §7 末尾の「決まるまで報告先の文字を出さない」は解け、公式ページの「困ったら」・アプリの〔このアプリについて〕（〔報告用のログを保存〕の隣に「@yomiwakechan へ」）・使い方ガイド・リリース文の 4 面に同じ 1 行で置く。GitHub の Issues は受け皿にしない（開発者の画面なので利用者には指さない）。上流 Aratako 氏へ問い合わせない旨は据え置き。いま走っている段 1 の Workflow は「報告先を出さない」前提で書いているので、完了後に主席がこの裁定を当て込む。
132. **アンインストールの既定（司令官・2026-09-11 00:5x・逐語「アンインストールで数Gバイト残るのは普通にアプリの範囲を逸脱している。」）**＝アンインストールは**ダウンロードした一式（実行系・モデル・取得キャッシュ・ログ・MIOpen db＝約 8 GB）をアプリと一緒に消す**。問わない（いまの `.iss` の 1 段目の問い「取得した実行系とモデルも削除しますか」＝既定 IDNO は廃止）。問うのは**1 つだけ**＝「自分で追加した声と設定も消しますか」（利用者の資産＝数 MB・既定＝残す・2 段目の問いはこの形で残す）。無人（/SUPPRESSMSGBOXES）でも一式は消える。もう一方の版（RTX（CUDA）／Radeon（ROCm）の同居）が入っている機体では共有のデータ樹を道連れにしない規則（設計書 §5-4）は据え置き＝その場合だけ一式を残し、その旨を 1 行出す。憲章 §4-25 を「既定はどちらも残す」から改めた。同じ趣旨で、更新（上書き導入）では 1 檔も消さない（従来どおり）。段 1 の Workflow の成果物（v2-spec §11・v2-copy・v2-plan）にも完了後に当て込む。
133. **削除と置き場の規則（司令官・2026-09-11 01:0x・逐語「追加時に参照したファイルまで消去しない事。取り込んだファイルは消す。RTX版とRadeon版は明確に別アプリと裁定済み。設定をそもそも共有しない。」）**＝⑴ アンインストールは**このアプリが自分の置き場に持っている物をすべて消す**（ダウンロードした一式・取り込んだ声の写し＝`voices\refs\`・設定・ログ・キャッシュ）。**問わない**（裁定 132 の「追加した声と設定を消すか」の問いも廃止）。⑵ **置き場の外には触れない**＝声を追加するときに利用者が選んだ元の wav は利用者の檔で、アプリは写しを取り込んで使う。元の檔は削除でも更新でも触らない（いまの実装＝`VoiceStore` は refs\ に写す＝この形を守る）。⑶ **RTX（CUDA）と Radeon（ROCm）は別アプリ**（裁定 109 で表示名・導入先・AppId を分けた線を徹底）＝**データ樹も設定も声も共有しない**。データ樹は版ごと＝`%LOCALAPPDATA%\irodori-tts-ywk-cuda\`／`…\irodori-tts-ywk-radeon\`（`AppPaths` の既定を flavor で分ける・`settings.json` の `dataDir` の明示指定は従来どおり優先）。裁定 109 の「取得物の置き場だけは共有」と設計書 §5-4 の「共有のデータ樹を道連れにしない」の規則は廃止（共有が無いので道連れが起きない）。⑷ 単一起動の錠（Mutex）も版ごとに分ける＝別アプリなので互いを起こし直さない。同時に走らせたときは後から起きた側がポート 18088 の塞がりで「止まりました」と理由（原則 5 の例外）を出す。⑸ 旧い共有樹（`%LOCALAPPDATA%\irodori-tts-ywk\`）が在る機体（開発機だけ）＝v2.0 の初回起動で、その版が持っていた物を版の樹へ**移す**（同じボリュームなら改名・両版が入っていた機体は写す）。憲章 §4-25・§5 を改めた。
134. **v2.0.0 を公開した（2026-09-11 06:1x＝Release 公開 2026-09-10 21:09:52Z）＝憲章（裁定 130）に基づく作り直しの版。**URL＝https://github.com/mugonkun/irodori-tts-for-yomiwakechan/releases/tag/v2.0.0（Latest・tag `v2.0.0`＝commit `a67434b`）・配布ページ＝https://mugonkun.github.io/irodori-tts-for-yomiwakechan/（gh-pages `8f14aaa`＝site/index.html の v2 全文・画像 3 枚は撮影待ちで枠は非表示）。**組み立て（配る回＝主席）**＝installer-build -All **20 門 0 失敗・WARN 0**・A-1 109 檔／34,088,381 B・A-6 ProductVersion v2.0.0・setup cuda **75,510,997 B** `9710a9fd…`／radeon **75,512,562 B** `5c02b90f…`（exe は非決定的＝公開する sha256 は配る回の出力＝SHA256SUMS.txt を Release に置き、リリース文には出さない＝裁定 125）・N: `rtx-handoff\installer` を v2.0.0 の 3 檔＋wizard-probe.ps1 に差し替え `sha256sum -c` OK。**段の記帳**＝段 1・2（仕様・全文言・工学の計画）64f9f61／文書と公式ページ e989bb5／段 A 15c0e10／段 B 1a6596c／段 D 8c6d6d2／段 C bf7f283／段 E 2ee6f3e／段 F 4e2df45／段 G a67434b＝Workflow 9 本（実装→検分→是正・Opus 5）・所見 合計 200 件超を当て込み。**実測**＝xUnit 743→**929**＋1 skip・契約テスト 369→**372**・WordLint（隠す語 0・日本語直書き 0）・AutomationId 132＋新設（退役 7 は台本と錠を同じ段で直した）・台本 7 本 parse 0・DryRun OK。**司令官に回付（段 G の検分席から）**＝⑴ 憲章 §6-1 に「詳細の畳みの中の記録は字を据え置く」の例外 1 文を足すか（v2-spec §10 原則 1 に例外 2 件として実態を先に記帳）⑵ 憲章 §4-15 の「音量」と §4-12 の「その場で試聴」は版の直前に機能を足さず見送り＝v2-spec §9 ⒄⒅⒆ に逸脱として記帳（次の版で入れるかは司令官）。**未実射**＝v2.0.0 はどの機体でも撃っていない＝RTX 機の段 H（12 射＋画像 3 枚の撮影）を別席に依頼した（そのユーザーの明示指示待ち）。本機には配布版が無い（裁定 123）。
135. **v2.0.0 の RTX 機での段 H（別席・2026-09-11 15:1x〜15:4x・素の機体・ドライバ 616.92・setup 9710a9fd… 一致）＝12 射のうち合格 7・不合格 3・飛ばし 2（判定は主席）。**合格＝射 1 導入（UAC 0・完了で自動起動・表示名「… － RTX（CUDA） v2.0.0」・約 96 MB の頁文）／射 4 しゃべらせる（1.7 s・「4.5 秒の音を 1.4 秒で作りました。」・畳みの外に RTF や ms 0）／射 6 排他（外部の POST 中は〔しゃべらせる〕が 1.6 s で押せなくなり「いま読み分けちゃん2 の読み上げに使われています」）／射 7 ×（0.71 s で消滅・子 0・18088 0・VRAM 8,879→649 MiB・トレイ 0）／射 8 詳しい状態（見える行 4・生ログ 0・〔ログを開く〕）／射 9 上書き（設定と追加した声 14 名が残り model.safetensors の時刻不変＝落とし直し無し・再起動 12.45 s）／射 11 削除（置き場 %LOCALAPPDATA%\irodori-tts-ywk-cuda が消え、元の wav C:\ywk\myvoice\test-voice.wav は残る＝**合格と裁く**。出た窓 2 つ「削除します。よろしいですか？」「正常に削除されました」は Inno／Windows の標準の確認で、裁定 132／133 が禁じた「データを消すかの問い」ではない＝段 H の表の「問いは 1 つも出ない」を「データを消すかの問いは出ない」に改める）。**不合格 3**＝⑴ 射 2 はじめの準備＝見える段 3・選ぶ段なし・押下 4・バー逆行 0・取得〜モデル 2 分 0 秒までは正解だが、**取得直後の初回起動で声の読み込みが 120 s に届かず「止まりました。準備に時間がかかりすぎました。」**（runtime loaded が出ないまま上限＝3.06 GB の safetensors を書いた直後でディスクキャッシュが冷えている・〔もう一度〕で 36.62 s・以後 12 s 前後）＝**上限の設計不良**＝v2.0.1 で「はじめの準備の起動の確認は上限を 600 s にし、子が生きて読み込み中なら失敗にしない」に直す。UAC 0 回は msvcp140 が残っている機体のためで欠陥ではない。⑵ 射 10 CUDA 12.6 へ落とす＝設定 › 詳細で cu126 を選び告知（5 GB 前後・10 分）と保存までは正しいが、**次の起動でウィザードが自動判定の cu130 を黙って選び直し settings が cu130 に戻る**（cu126 は 1 バイトも取られない）＝**利用者の明示の選択が捨てられる欠陥**＝v2.0.1 で「取得のためにウィザードが開く回は settings.Variant を明示の選択として尊重し、告げる 1 行も『設定で選んだ CUDA 12.6 で動かします』にする（下限未満のときだけ勧め直す）」に直す。併せて**設定のコンボの選択肢が内部名（cu130／cu126／cpu）のまま**（段 G の VariantDisplayConverter が ComboBox の一覧に効いていない）＝v2.0.1 で直す。⑶ 射 3〔あとにする〕→続きから＝未実施（再試行で使えますまで通り firstRunCompleted になったため）＝v2.0.1 の実射で撃つ。**逸脱の記録**＝射 11 の 1 回目でアンインストーラの確認窓が出ている最中に WindowPattern.Close() を撃つと 15 s 待っても閉じず taskkill を要した（射 7 では 0.71 s）＝原因未詰め・切符。**画像 3 枚**＝N:\…\rtx\shots\ に撮影済み（1100 px 幅・PrintWindow）＝着地頁に入れる。**控え**＝N:\…\rtx\logs\ の step-v200-*.log と v200-install／reinstall.log。機体は素の状態（C:\ywk に道具と控えだけ）。
136. **段 H 射 2 の「初回起動 >120 s」の原因の見直し（別席の実測と多視点分析・2026-09-11 16:xx・司令官「本家・派生版でそこまで遅くならない」の疑いから）**＝⑴ 「ディスクキャッシュが冷えていた」説は**撤回**＝その NVMe の実効読み 2,943 MB/s で 3.06 GB は 1〜2 s・115 s には 30 MB/s 級が要る・page cache は揮発なのに罰は一度きり・射 9（再導入直後）が最速 12.45 s・36→12 の 2 段目を説明できない。⑵ GPU の JIT（PTX→SASS）＝**実測で除外**＝%APPDATA%\NVIDIA\ComputeCache は 62 檔 1.4 MB・最終更新 9/10 21:23（v1.1.0 の検分時）で 9/11 15:00〜15:40 の書き込み 0＝cu130 の wheel は sm_86 の SASS を持ち JIT は要らない。Triton／inductor 未使用・HF 解決 0.42 s も除外。⑶ **残る本命＝Windows Defender のオンアクセス初回スキャン**＝そのとき初めて書かれた実行系 25,300 檔・3.3 GB（cublasLt64_13 478 MB・torch_cuda 409 MB・cufft 284 MB・cudnn_engines 188 MB）とモデル 3.06 GB を、Python が import／LoadLibrary／mmap する瞬間に同期スキャン（＋未知 PE のクラウド照会）＝ログが無言なのは open の中で停まるから・一度きり（檔の同一性で結果を保持）・射 9 は檔が据え置き＝12 s・本家や派生版は前から在る檔・9/10 の 51 s と 9/11 の >120 s の差は全檔を消して入れ直したため。見積り 80〜180 s。RealTimeProtection True を実測。**v2.0.1 への含意**＝上限 600 s は「壊れない」ための緩和で原因には届かない＝⒜ **展開の最後に実行系とモデルの檔を一度読んで温める段を入れる**（進捗バーの下でスキャンを済ませ、起動の段に持ち込まない・所要は進捗に出す）⒝ 待ちの 1 行は「初めてのときは、パソコンの安全機能が新しいファイルを確認するので数分かかります」の趣旨に（実態に近い）⒞ Defender の除外は入れない（利用者の防護設定を黙って変えない＝憲章）⒟ 檔数の削減・署名は次の版の検討。**決定実験**（別席が用意・5.3 GB の再取得と司令官の承認が要る）＝素の状態から新しいパスに入れ直し、無言の間に MsMpEng.exe の CPU 時間と python の I/O を 1 s 間隔で採る。撃つかは司令官。いま走っている v2.0.1 の Workflow（600 s と設定の尊重とコンボ）の完了後、⒜⒝ を追加の Workflow で入れてから v2.0.1 を切る。
137. **はじめの準備の受け入れ条件（司令官・別席経由・2026-09-11 16:xx・逐語「使用者からは固まった(フリーズした)ように見えなければよい。」）**＝速さではなく「固まって見えないこと」が条件。段 H 射 2 の 1 回目はこれを破っていた＝15:21:16〜15:23:11 の約 2 分、画面が「95 %　動くか確かめています。」のまま静止（バーも文言も動かない）。600 s の上限だけでは静止が 10 分に延びるだけで条件を満たさない。**測れる形（別席の案を採る・段 H 射 2 の正解に置く）**＝⑴ 準備の開始から「使えます」まで FirstRunProgressText／FirstRunPhaseText／FirstRunStepTitle を 1 s 刻みで採り、**画面の文言が 1 字も変わらない最長区間 ≤ 5 s**（Windows が「応答なし」を付ける約 5 s より短い）⑵ IsHungAppWindow が True になる回数 **0** ⑶ 静止区間に動く材料（経過秒・点滅／回転・「n / 25,300」の小刻みな文言）が在れば ⑴ を超えても可＝⑴⑵⑶ を並べて報告し判定は主席 ⑷ 参考＝開始→使えますの所要と runtime loaded in N s。**v2.0.1 の実装への含意**＝⒜ 温め段は檔数で進捗を出す（「新しいファイルを確認しています n / 25,300」）⒝ 起動の確認は /health の loaded:false の間も**経過秒を毎秒更新**し、待ちの 1 行（安全機能の確認で数分）を添える＝無言で待たせない ⒞ UI の糸を塞がない（IsHungAppWindow 0）⒟ 主画面の帯も「準備しています…（N 秒）」のように毎秒動かす。
138. **v2.0.1 を公開した（2026-09-11 18:1x＝Release 公開 09:14:02Z）＝裁定 135〜137 の当て込み。**URL＝https://github.com/mugonkun/irodori-tts-for-yomiwakechan/releases/tag/v2.0.1（Latest・tag `v2.0.1`＝commit `4863f24`）。**中身**＝⑴ 起動の確認は子が生きて /health が loaded:false・error 無しなら 600 s まで待つ（契約の 120 s は口が開くまで据え置き・CanExtendForLoading・期限の起点は待ちの開始）⑵ 取得のためにウィザードが開く回は設定の選択を明示の選択として尊重（IsExplicitChoice・「設定で選んだ CUDA 12.6 で動かします。」・真の初回だけ勧め・下限未満は勧め直す・ログの「変種＝」を廃す）⑶ 動かし方のコンボを両方とも表示名（ItemContainerStyle＋AutomationProperties.Name）⑷ 温め段 FirstRunStep.Warmup（NewFileScan＝新しく書いた実行系とモデルの檔を一度読む・檔数と秒で毎秒動く・通知は毎秒 2 回まで・中断可・冪等・失敗しない・重み 50/10/25/10/5）⑸ 起動の確認と帯の経過秒（ElapsedTicker・「起動しています（N 秒）…」→「声を読み込んでいます（N 秒）… 安全機能…」・帯「準備しています…（N 秒）」）⑹ ウィザードの同期 IO を UI の糸から外す ⑺ probe/freeze-meter.ps1（裁定 137 の計器）。**組み立て（配る回）**＝installer-build -All 20 門 0 失敗 WARN 0・A-1 109／34,088,381 不動・setup cuda **75,522,312 B** `95abfcaa…`／radeon **75,523,877 B** `50434f58…`（配布樹は不変＝製品側の差は exe だけ）・N: を v2.0.1 の 3 檔＋wizard-probe.ps1＋freeze-meter.ps1 に差し替え `sha256sum -c` OK。**検証**＝Workflow 3 本（135 の 3 件＝所見 16 件全件／136・137＝所見 19 件全件／試験の競り＝製品不触）・xUnit 951→**987**＋1 skip（時間の競りを持つ試験 2 本を TickGate で確定的にし Release 5 回 Debug 2 回 全緑）・契約 372・WordLint・台本 8 本 parse 0。commit＝6c7b187（135 の 3 件）・3bd9024（136／137）・4863f24（試験の是正・tag）。**未実射**＝RTX 機の段 H の撃ち直し（射 1・2＋3・4・10・7・11・freeze-meter・画像②）を別席に依頼（そのユーザーの明示指示待ち）。Defender の決定実験は司令官の判断。
139. **公開直後の未署名 setup は素の Windows 11 で起動できない（別席の実測・2026-09-11 18:1x）**＝v2.0.1 の setup を RTX 機で起こそうとして「アプリケーション制御ポリシーによってこのファイルがブロックされました」＝Smart App Control（SAC）のブロック（CodeIntegrity/Operational id 3118・VerifiedAndReputablePolicyState=1・Enforcement=2・MOTW 無し・AppLocker ルール 0・Defender は無関係）。v2.0.0（15:17）は通った＝差はハッシュの評判だけ（Releases に載って時間が経ち評判が付いた）。v2.0.1 は 18:14 に組んだ新規ハッシュ＝評判ゼロで即ブロック。setup が通っても中の IrodoriTtsYwk.Launcher.exe も新規ハッシュで弾かれる。**帰結**＝Windows 11 の新規セットアップでは SAC が既定で有効なので、**未署名のリリースは公開直後に素の機体で起動できない**＝憲章 §1「気軽に落として入れて開く」に直結する製品の根本問題。**唯一の直し＝コード署名**（setup と Launcher の両方・信頼チェーンのある証明書）。**司令官に決めてもらう点**＝署名の手段＝⑴ Azure Trusted Signing（月額 約 10 USD・Microsoft の信頼・SAC と SmartScreen に効く・本人確認が要る・個人開発者の受付は国や時期で変わるので要確認）⑵ 認証局の OV／EV コード署名証明書（年 100〜400 USD 級・2023 年以降はハードウェアトークン必須・EV は SmartScreen の評判が即時）⑶ 署名せず評判が付くまで待つ（初めて落とす人ほど弾かれる＝憲章に反する・採らない）。決まれば組み立てに signtool の段（Launcher exe を ISCC の前に・setup を後に・門 A-8／B-4 で署名を検分）を入れる。**RTX 機の扱い**＝SAC は一度切ると Windows の入れ直しまで戻せない設定で、別席は触らない（主席も頼まない）＝切る／署名を待つ／別の機体で見るは司令官の判断。v2.0.1 の撃ち直しと画像②と Defender の実験は、走れる版が来るまで保留。**当面の緩和（検分用・利用者には勧めない）**＝Release から落として時間が経ち評判が付くのを待つ、または SAC の無い機体で撃つ。
140. **司令官の手動検分（RTX 機・v2.0.1・2026-09-11 18:3x）＝参照ボイスを変えて〔しゃべらせる〕と Smart App Control が Python の拡張を弾く。**画面の逐語＝帯「読み上げの用意の中で失敗しました。（ImportError: DLL load failed while importing _spline: アプリケーション制御ポリシーによってこのファイルがブロックされました。）」＋Windows セキュリティ「このアプリの一部がブロックされています／アプリが読み込もうとした _spline.cp312-win_amd64.pyd を発行したユーザーを確認できないため、Python の一部の機能が動作しない可能性があります。」（`_spline`＝scipy.signal の拡張＝参照 wav の再標本化で初めて import される。「デフォルト」の声は参照を持たないので通っていた）。setup と Launcher は通った（v2.0.1 が「使えます」まで到達・裁定 139 のブロックは MOTW／評判の差か SAC の評価の揺れ＝原因は詰めていない）。**読み**＝SAC が有効な機体では、初回にダウンロードする Python の実行系に含まれる**未署名で評判の無いネイティブ拡張（.pyd／.dll）が、どれが弾かれるか予測できない形でブロックされる**＝torch の DLL は通り scipy の 1 本が落ちた。第三者の wheel を配布側で署名し直す手は無い（裁定 8＝第三者バイナリを同梱しない・公式配布元から取る）。**手立て（v2.0.2 の候補・司令官の裁定）**＝⑴ 起動時（できれば着地頁と導入の前）に SAC の状態（HKLM\SYSTEM\CurrentControlSet\Control\CI\Policy\VerifiedAndReputablePolicyState＝1 有効／2 評価／0 無効）を検め、有効なら原則 6 の 3 部品で告げる＝「この PC では Smart App Control が有効です。このアプリが使う Python の部品は読み込めません。設定 › プライバシーとセキュリティ › Windows セキュリティ › アプリとブラウザー コントロール › スマート アプリ コントロールで無効にできます（一度無効にすると Windows を入れ直すまで戻せません）」＋〔設定を開く〕（ms-settings:）⑵ 着地頁の「必要なもの」と guide.md に同じ 1 行 ⑶ 帯に生の ImportError を出さない（「アプリケーション制御ポリシー」を含む失敗を SAC の枝に畳む）⑷ setup と Launcher の署名（裁定 139）は別途。**SAC を切る指示は出さない**（利用者の防護設定は利用者の判断・文言で事実と手順を告げるだけ）。
141. **司令官の指示（2026-09-11 19:0x・逐語「本機ではOFFだが、評価機ではONになっている。スマートアプリコントロールが弾いてる事を明示。」）**＝本機（Radeon）の Smart App Control は「オフ」（Windows セキュリティの画面で確認・「評価」は灰色＝一度オフにすると戻れない状態）、RTX 機は「オン」。**弾いているのが Smart App Control であることを利用者に明示する**＝v2.0.2 の中身（裁定 140 の ⑴⑵⑶＝状態の検知・はじめの準備の前の告知・失敗の 3 部品への畳み・着地頁と guide の 1 行）がそのまま当て込み先。Workflow 実行中。
142. **司令官の補足（2026-09-11 19:1x・逐語「最近のWindowsアップデートで戻せるようになっている。」）**＝Smart App Control を一度オフにしても、最近の Windows の更新で再びオンにできるようになった。したがって v2.0.2 の告知文・着地頁・guide.md・裁定 140／141 の「一度無効にすると Windows を入れ直すまで戻せません」は**書かない**（誤り）。文は「設定はここにあります（Windows セキュリティ › アプリとブラウザー コントロール › スマート アプリ コントロール）」までにとどめ、オン／オフの可逆性には触れない。いま走っている v2.0.2 の Workflow はその文を含む指示で動いているので、完了後に主席が外す。
143. **「なぜ v1 は通り v2 から弾かれるか」の答え（別席の読み取りと多視点分析・2026-09-11 19:2x）＝主席の仮説「評価モード→強制の切替」は否定。**確定した事実＝⑴ RTX 機の SAC は 2026-09-03 の初日から強制（9/03 に Chrome の vk_swiftshader.dll／vulkan-1.dll と Intel の igd10iumd64.dll を、9/10 に nvspcap64.dll をブロック済み＝3033／3077 は強制でしか出ない）⑵ 評価モードの監査（id 3076）は全履歴で 0 ⑶ 最終起動 9/10 19:17 の同一セッション内に v1.1.0（9/10 23:07）・v2.0.0（9/11 15:3x）・v2.0.1（9/11 18:xx）が全部入る＝SAC の様態は起動時にしか変わらず切替は不可能 ⑷ 弾かれた _spline.cp312-win_amd64.pyd は scipy-1.18.1 wheel 由来で全版同一バイト（sha256 f82ca6be…・ディスクの実体も一致）・台帳 runtime-cu130.json 不変＝製品差ではない ⑸ 18:38 以降も同じ pyd が 20 回ブロックされ続けている（司令官の再試行）。**積極的な原因**＝未署名の檔が SAC 強制下で ISG（クラウド評判）に依存する＝未署名檔は「評判が良」の間だけ走れ、判定は檔ごとに保持され、再起動なしで allow→block に反転し得る（低流通の未署名バイナリの評判が 18:32 の前に反転した、が同一ハッシュの allow→block を説明する唯一の筋）。残る不確実（ログだけでは割れない）＝(A) 一度許可された同一ハッシュの評判が反転した／(B) scipy.signal の import が参照経路でだけ走り 18:32 が真の初ロードだった（別席は当初の「48 kHz のプリセットで _spline が初ロード」説を撤回＝_spline は scipy.signal の top-level import で載る・resample は _spline を通らない・allow は CodeIntegrity に記録を残さない）。どちらでも根本原因（未署名＋SAC 強制＋ISG）と直し（署名）は同じ。**直し**＝コード署名（setup・Launcher に加え、実行系の native .pyd／.dll も署名で評価されなければ揺れるクラウド評判に依存し続ける＝第三者 wheel の再署名は裁定 8 と衝突するので、SAC 有効機は「動かない前提で明示して告げる」＝裁定 141 の線が現実解）。**決定実験（能動操作＝司令官が撃つか許すまで保留）**＝埋め込み python を裸で `python.exe -c "import scipy.signal"` と撃ち CodeIntegrity/Operational を追う＝新たな 3033／3077 が出れば「この未署名ハッシュは import の瞬間に弾かれる・版に無関係」が確定。裁定 140 の「原因は詰めていない」をこの条で埋める。
144. **v2.0.2 を公開した（2026-09-11 19:2x＝Release 公開 10:27:45Z）＝裁定 140〜143 の当て込み＝Smart App Control が弾いていることを明示。**URL＝https://github.com/mugonkun/irodori-tts-for-yomiwakechan/releases/tag/v2.0.2（Latest・tag `v2.0.2`＝commit `3b33115`）・着地頁 gh-pages `af99e90`（「必要なもの」に SAC の 1 行）。**中身**＝SmartAppControl.cs（レジストリ VerifiedAndReputablePolicyState を起動ごとに 1 度読む・投げない・1 有効／2 評価／0 無効）・SmartAppControlNotice（「アプリケーション制御ポリシー」「Application Control policy」「blocked by」の標識と、SAC が有効な機体での「DLL load failed」を 3 部品に畳む＝「読み上げに必要な部品が読み込めませんでした／この PC の Smart App Control が…を止めました／〔設定を開く〕（ms-settings:windowsdefender の退路つき）」・帯と発話テストの両方・生の ImportError はログと詳細の記録だけ・Ready のときは「使えます」の見出しのまま理由と 1 手を出す）・はじめの準備の「これからすること」で有効なら告知（FirstRunSmartAppControlText）・設定 › 詳細に状態の 1 行（SettingsSmartAppControlText）・guide.md の症状・「戻せない」は書かない（裁定 142）・版 v2.0.2。**組み立て（配る回）**＝20 門 0 失敗 WARN 0・A-1 109／34,088,381 不動・setup cuda **75,529,419 B** `515fbf9b…`／radeon **75,530,982 B** `b510da82…`・N: 差し替え `sha256sum -c` OK。**検証**＝Workflow（実装→検分 3 席→是正・所見 10 件全件・Opus 5）・xUnit 1007→**1017**＋1 skip・契約 372・WordLint・台本 8 本 parse 0。**主席の作法の誤り（記録）**＝裁定 141／142 を `git commit -a` で記帳した際、走っていた Workflow の途中の編集を巻き込んで commit した（f066260・7a3f1f3）＝以後、Workflow 中の記帳は `git commit -- decisions.md` の路限定で行う。**未実射**＝RTX 機（SAC 有効）での v2.0.2 は、司令官の手動検分待ち（見るのは「弾かれた事実を正しく告げるか」）。決定実験（import scipy.signal）は司令官「撃ってよい」＝別席が撃つ。
145. **決定実験の結果（別席・司令官「撃ってよい」・2026-09-11 19:18）＝同一の未署名ハッシュが同じ起動セッション内で block→allow に反転した＝原因は ISG（クラウド評判）の揺れで確定。**実測＝埋め込み python を裸で `python.exe -c "import scipy.signal"`＝**EXIT 0（_spline.pyd がロードされた・再現も成功）**・import numpy／torch も EXIT 0・19:18 以降の新規 3033／3077 は 0（直近のブロックは 18:53:00 で止まった）。署名の対照＝python.exe は Valid（CN=Python Software Foundation）・_spline.pyd／torch_cpu.dll／torch_python.dll／c10.dll／numpy の _multiarray は NotSigned。**読み**＝同一ハッシュ f82ca6be… が 18:32〜18:53 は 20 回以上ブロック、19:18 は許可＝裁定 143 の (A)（評判の反転）に決着・(B)（初ロード）は否定。v1.1.0／v2.0.0 の成功も本物の許可ロードで、18:32〜18:53 だけ評判が block に振れていた。**帰結**＝未署名＋SAC 強制＋ISG では、起動できるかが Microsoft のクラウドの揺れに委ねられる＝配布物として成立しない＝**署名が唯一の直し**（裁定 139・手段は司令官の裁定待ち）。裁定 140〜144 の「明示して告げる」は次善。**v2.0.2 の告知への含意**＝告知は SAC が有効なら出るが、評判が allow の間は弾かれずに動くので、文は「読み込めません」ではなく「**読み込めないことがあります**」が正確（v2.0.3 か署名の版で直す）。v2.0.2 の告知の実射は、評判が block に振れた回に司令官が遭遇したときだけ採れる（狙って再現できない・別席は評判を操作しない）。機体＝v2.0.1 が司令官の手動導入のまま・ランチャ停止・SAC 未接触。
146. **署名と実行系の扱い（司令官・2026-09-11 19:5x・逐語「今は未署名、SACの注意にとどめる。観測期間にそこまでは割けない。本命は「実行系を自前で組んで」だったが調査も埋め込みPythonで進んでしまったのでもう仕方がない。今更作り直しもできない。」）**＝⑴ 当面は**未署名のまま**（Azure Trusted Signing／OV・EV は採らない・観測期間に工数を割かない）⑵ SAC 強制の機体は**注意の明示にとどめる**（v2.0.2 の告知・着地頁・guide＝裁定 140〜145）⑶ 実行系を自前で組んで署名する案（裁定 145 の 1）は**採らない**＝埋め込み Python の路で進んだ以上、作り直しはしない。**当て込み**＝着地頁の「必要なもの」の文を「読み込めないことがあり、動かないことがあります」に正した（評判が許可の間は動くため・裁定 145）＝gh-pages を更新。アプリ内の告知の文（「読み込めません」）は次の版で同じ形に正す（単独では版を切らない）。**残る司令官への回付**＝憲章 §6-1 の例外 1 文・§4-15 音量と §4-12 試聴（v2-spec §9 に逸脱として記帳済み）。
147. **語の指示（司令官・2026-09-11 20:0x・逐語「SACという語句ではなくスマートアプリコントロールと本来の語句で表現する。」）**＝利用者の目に入る面（アプリの文字列・着地頁・guide・リリース文）では「SAC」も「Smart App Control」も使わず、Windows の設定画面の本来の語**「スマート アプリ コントロール」**で書く（内輪の帳面と識別子＝SmartAppControl.cs 等は据え置き）。当て込み＝着地頁・guide・v2.0.2 のリリース文（本文の差し替え）は即時、アプリ内の文字列（UiStrings・SmartAppControlNotice の利用者向け文・XAML）は v2.0.3 で「読み込めないことがあります」（裁定 145／146）と一緒に。
148. **v3 の据え置きと着手条件（司令官・2026-09-11 20:1x・逐語「v3は据え置くが、着手条件として以下 実行系を自前で組んで、全部自前の置き場から配る。権利の確認。」）**＝v3（実行系を自前で組んで全部を自前の置き場から配り、署名で評価される形にする＝裁定 145 の案 1）は**据え置き**。着手するときの条件＝⑴ 実行系（埋め込み Python・torch と依存の wheel・CUDA／ROCm の実行時ライブラリ・vc_redist・モデル）を**自前で組む**（公式配布元からの初回取得＝裁定 8 の路を廃し、配布側が組んだ一式を配る）⑵ **全部を自前の置き場から配る**（GitHub Release か自前の配信・約 5 GB 級）⑶ **権利の確認**＝各構成物の再配布条件（Python＝PSF・torch／scipy／numpy＝BSD・NVIDIA CUDA 実行時ライブラリと cuDNN／cuBLAS＝NVIDIA の EULA の再配布条項・ROCm＝AMD の各ライセンス・vc_redist＝Microsoft の再配布条項・Irodori-TTS の重み＝Aratako 氏のライセンスと Ethical Restrictions・Semantic-DACVAE＝同・SilentCipher＝Sony のライセンス）を原文で読んで台帳にし、配れない物があれば v3 の形を変える。**帰結**＝v2 系は埋め込み Python の路のまま（裁定 146）。v3 の設計と権利の台帳作りは司令官の着手指示があってから。
148-2. **司令官の補足（同日・逐語「いや、モデルは取りに行く。モデルは許可されてないと予測。」）**＝v3 でも**モデル（Irodori-TTS の重み・Semantic-DACVAE・SilentCipher）は初回に Hugging Face から利用者の機体へ取りに行く**（再配布は許可されていないと予測＝自前の置き場からは配らない）。自前で組んで配るのは**実行系だけ**（埋め込み Python・torch と依存の wheel・CUDA／ROCm の実行時ライブラリ・vc_redist）。権利の確認の対象もその範囲＝PSF・BSD（torch／scipy／numpy）・NVIDIA の EULA・AMD の ROCm・Microsoft の vc_redist。モデルの取得は現行（裁定 8 の路・ledger/models.json）のまま。
