# acceptance.md — 配布版 irodori-TTS の受け入れ条件

> **原本**＝`research/report/irodori-native-handoff-2026-09-04.md` §4「配布版 irodori-TTS の受け入れ条件
> （数値で書けるものだけ・値は卓が動かしてよい・`38` §5）」。
> 本檔は**その表を原文のまま写し**、設計書 `docs/design/ben-a-skeleton-and-build.md` §7 の改訂を当てたもの。
> **原文の列は 1 字も変えていない**。改訂は右端の「改訂」列にだけ書き、改訂した理由（根拠の檔:節）を併記する。
> **値は卓（司令官）が動かしてよい**（原本の但し書きをそのまま引き継ぐ）。
>
> 原本の表の**実数は 21 行**（設計書 §7 は「22 行」と書くが、原本を数え直すと見出し行を除いて 21 行。
> 本檔は原本にある行を全部写した＝欠落なし）。加えて、便 A の裁定で**新たに要る 1 行**（ポート）を
> 末尾に「追加」として足した（＝21 ＋ 1 ＝ 22 行）。
>
> 便 A 固有の受け入れ条件（A-1〜A-8＝ビルド台本・契約テスト・`licenses/` の検分）は設計書 §0 が正で、
> 本檔は**製品としての受け入れ条件**を扱う。

---

## 1. 受け入れ条件（原文＋改訂）

| 区分 | 条件（**原文**） | 根拠値（**原文**） | 改訂 |
|---|---|---|---|
| 速度 | 3090・cu130・bf16・40 steps・warm・参照なし＝短文 RTF ≤ 0.25／長文 ≤ 0.10 | 実測 0.215／0.084（`27`） | — |
| 速度 | 参照 10〜30 s ありで RTF ≤ 0.35（fp32 ≤ 0.40） | 実測 0.27〜0.34／0.31〜0.39（`33`） | — |
| 速度 | 10 steps を採るなら短文 ≤ 0.10・参照ありで ≤ 0.15 | 実測 0.080／0.11〜0.15 | — |
| 速度 | CPU（fp32・OMP=8）＝RTF ≤ 4.0（速さは要求外） | 実測 3.27 | — |
| コールド | 起動後 1 発目 ≤ 2.0 s（3090・bf16・短文） | cold 1.032 s（`27`） | — |
| スパイク | 同文連射で 1 発目 ≤ 45 s・3 発目 ≤ 20 s（現状を悪化させない） | ROCm 実測 40.8／19.4 s（U-6） | **改訂**＝この行は **Radeon 版にだけ**掛ける（CUDA 機では該当する罰が観測されていない＝3090 は cold 1.1〜2.1 倍のみ・参照長の罰も無し）。**bf16 固定**を前提に **「同一形状・MIOpen db 温で 1 発目 ≤ 5 s」**に書き換える。**3 発目の条件は落とす**。根拠＝`research/lab/notes/40-rocm-warmup.md` §8＝同一形状 6 連射で bf16 は 1 発目 **2.841 s**・2〜6 発目 0.848〜1.087 s、**3 発目の再発（19.4 s）は in-process では再現しない**。原文の 40.8／19.4 s は **fp32 の値**（同 §8 の 2＝fp32 1 発目 32.077 s が 20〜40 s の帯に入る）であり、**bf16 固定の配布版の根拠に使わない**。※ 機体で初めて見る形状（MIOpen のカーネル生成）は別枠＝同 §7-4 で 1 発 8.5 s の実測があり、これは暖機と db の育ちで償う。 |
| VRAM | bf16 常駐 ≤ 3.2 GiB／fp32 ≤ 4.8 GiB（1 プロセス）・kill 後 10 s で idle 値へ | 3,079／4,134〜4,682 MiB・8 s で解放（`27`・`29`） | **改訂（注記）**＝「bf16 は形状に依らず 3,079 MiB 一定」はプロセス全体の累積ピークであり、**射ごとにリセットして採ると出力潜在フレーム数 S_lat に比例する（≒3.5 MB／フレーム）**。実測＝S_lat 25→2,062 MiB／100→2,331／200→2,683／300→3,031／**363→3,248 MiB（＝14.5 s 出力で 3.17 GiB）**。根拠＝`research/lab/notes/40-rocm-warmup.md` §1。**上限 3.2 GiB は 12 s 出力（3.03 GB）までなら満たすが、14.5 s 出力で 3.17 GiB に達する**＝上限の読み方を「常駐値」ではなく「S_lat 比例のピーク」に改める。 |
| サイズ | 実行系 ≤ 2.2 GiB（cu130）＋モデル ≤ 3.4 GiB・1 資産 ≤ 2 GiB・導入後 ≤ 7.0 GB | 報告 §5-3・`27` | — （※「1 資産 ≤ 2 GiB」は `decisions.md` 8 の初回取得方式では **Release 資産に第三者バイナリを載せない**ので自動的に満たす） |
| 導入 | 利用者操作 ≤ 6（vc_redist 自動なら ≤ 5）・オンライン導入 ≤ 10 分（100 Mbps 級） | 報告 §5-1・`27` 表 2 | **改訂**＝`vc_redist.x64.exe` は**初回取得の一段として配布版が黙って通す**前提に固定する（`ledger/vc_redist.json` の版固定 URL＋sha256）。よって**利用者操作 ≤ 6** を条件とし、「自動なら ≤ 5」の但し書きは**目標**として残す。根拠＝`research/lab/notes/22-runtime-license.md` 表 3b（`torch/lib/c10.dll` が `MSVCP140.dll` を要求）・`36-handoff-install-ui.md`（21 操作のうち 16 が消えて残り 6）。※ vc_redist 欠落機での実挙動は**未確認**（`30` V-5）＝便 E の検分項目。※ **落とすバイトと要る空きは別物**（便 D（2）の low 10）＝台帳の総和は cu130 **5.26 GiB**（5,646,269,459 B）・cu126 5.93 GiB・rocm 4.79 GiB・cpu 3.62 GiB で、取得中に要る空きは `FetchPlan.EstimatedPeakDiskBytes`＝cu130 **11.56 GiB**（表は `ledger/README.md` §7）。 |
| ドライバ | cu130 ≥ 580／cu126 ≥ 528.33 を起動前に検査し、不足なら合成を撃たずに UI で告知。cu126 で 528.33〜537.57 は「未実測の帯」と告知して合成は許す | 報告 §5-3（「527.41 で足りる」は誤り）・便 B 実射 | **改訂（U-14 確定）**＝**cu126 は 537.58（2023-10・CUDA 12.2 系）で GPU 合成が通った**（`is_available=True`・4 検査 PASS・warm RTF_http 0.265）。**同ドライバで cu130 は `cudaErrorNotSupported`＝`is_available=False`（import は通る）**。よって「cu126＝古いドライバ向け」と**謳ってよい（実測は 537.58 まで）**。528.33 は NVIDIA の minor version compatibility の Windows 下限で**未実測**。variant の自動選択は `is_available()`＋`device_count` で決める。根拠＝`decisions.md` 80・N: `rtx\logs\step-B7-3-8-*`／`3-9-*`。 |
| 起動 | UI 起動→`/health` 200＋`runtime.loaded=true` まで ≤ 120 s（3090・SSD なら ≤ 60 s）・ready 待ち ≥ 120 s | `30` N-5・`27` | — （Radeon 版は暖機込みで別枠＝`docs/radeon.md`） |
| 起動失敗 | 範囲外 GPU・精度不整合・checkpoint 不在は ≤ 15 s で理由 1 行に落ちる | `model.to()` 6.07 s／`codec.py:78` 11.28 s（`29`） | **改訂（強化）**＝**範囲外 GPU と device 綴り違いは 0 s で弾く**（上流に渡す前に wrapper が検査して exit 2）。`≤ 15 s` は checkpoint 不在など上流に入ってから判るものにだけ掛ける。根拠＝設計書 §4-6。 |
| GPU | GPU は UUID か PCI で保存し起動時に index へ解決（列挙 ≤ 5 s）・device は env 2 本に同時に載る・範囲外と綴り違いは UI で 0 s で弾く（上流は 6〜11 s） | `29`・`33`・`36` F-1〜F-3 | — |
| API | `GET /params` ≤ 100 ms・モデル未読込でも 200・本体が露出する全欄を 100 % 含む | `38` 実射 14 | **改訂（明確化）**＝`/params` の `default` に `null` が **0 件**という条件は、**`exposed_to_ywk: true` の欄**（本体が `ParamDescriptor` に写す 9 欄＋`voice`・`speed`）に対して撃つ。残り 35 欄は `nullable: true`＋`default_source`（`unset`／`settings_optional`／`checkpoint`）で「未指定＝上流の挙動」を明示する。根拠＝設計書 §0 A-5 行・§4-2 ⑴⑵・`docs/contract.md` ⑸ 5-2。 |
| API | 未知欄・綴り違い・範囲外を 400 で弾く（沈黙 0 件）・エラー body に絶対パス 0 件 | `14` T-2・T-3・T-8・`37` §2 | — （`voice` と `no_ref`／参照 5 欄の同時指定も 400＝`docs/contract.md` ⑶・⑷）。**「絶対パス 0 件」の適用範囲**＝エラー body 全件に加えて **`GET /health`・`GET /ywk/status`・`GET /params`・`GET /v1/audio/voices` の 200 body**（`/health` は上流が絶対パスを載せるので配布版が `<path>` に畳む＝`docs/contract.md` ⑵）。判定は `:\`／`:/` の grep **だけでは足りない**（空白入り・日本語のパスは段が残る）＝`voices_dir` の各段が body に現れないことで撃つ。 |
| 話者 | wav 選択＋名付けの 2 操作で追加・再起動不要で一覧に反映・日本語名で合成 200 | `voices.py:168-186`・`37` 実射 | — |
| 話者 | 一覧に「デフォルト」1 件が常在（先頭に出すなら配布版が並べ替え）・上流由来の `none` が 0 件 | `38` 実射 4・13b | — （配布版は**先頭に出す**＝並べ替える＝`decisions.md` 16）。**`voices.json` が無い／壊れていても「デフォルト」1 件は出る**（wrapper が注入・一覧は 500 を返さない＝`docs/contract.md` ⑷・⑼ D-8） |
| ライセンス | `licenses/` に A1〜A15 が全部（外部取得 4・自作 2 を含む）・未特定 0 件（zlib を解消するか対象 DLL を外す） | `22` | **改訂（分割）**＝A1〜A15 のうち**配布物に同梱するもの**は `licenses/`、**初回取得で利用者の機体に入るもの**は `licenses/first-run-notices.md`（**配布物に入れる**＝初回取得 UI が取得を始める前に表示する檔だから・`decisions.md` 46）に分ける。根拠＝`decisions.md` 8（第三者バイナリを配布物に入れない）・設計書 §1。**A14（`torch/lib/zlibwapi.dll` の出所と license）は未特定のまま**＝原文の「未特定 0 件」は**便 A では満たしていない**（初回取得で入る物なので配布物には無いが、通知の対象＝`first-run-notices.md` に「未特定」と正直に書く）。 |
| 安全 | 既定 bind `127.0.0.1`・api_key 自動生成・`empty_cache_interval=0`・`preload=true` を焼く | `config.py:18-39`・報告 §5-1 | **改訂**＝**api_key は持たない**（自動生成もしない）。理由＝bind が `127.0.0.1` 固定のループバック限定であり、鍵を持つと本体・ランチャ・利用者の 3 者で鍵の受け渡しが増えるだけで守るものが増えない。根拠＝`decisions.md` 2（司令官裁定・api_key なし）。他の 3 つ（bind・`empty_cache_interval=0`・`preload=true`）は原文どおり焼く。**加えて上流の書き込み 3 口（`POST /v1/audio/voices`・`PUT`／`DELETE /v1/audio/voices/{id}`）を外す**＝鍵を持たない以上、開いたままの登録口が利用者機に残る唯一の書き込み面になる（multipart/form-data は CORS の simple request＝preflight なしで届く）。本体も配布版 UI も使わない口である（`docs/contract.md` ⑷ 4-3・⑼ D-3）。 |
| ログ | stderr を本命に取り込み `Uvicorn running on` と `runtime loaded in` の 2 行で状態判定・422 の本文 echo をログに流さない | `30` N-6・`37` | **改訂（追加）**＝状態判定に使う行を **3 行**にする＝`ywk_server <版> upstream=8224daf/841fb7c`（wrapper が起動時に 1 行）・`Uvicorn running on`・`runtime loaded in`。1 行目でランチャが「配布版の wrapper が起動した」ことを確認できる。根拠＝設計書 §4-1。 |
| キット再利用 | kit-ssd（全段 6.2 分）で CUDA 実測を再走・ref-bench で参照増分を再測・聴取セットの形で新配布物 vs 現行の A/B を 10 分で聴ける | `27`・`32`・`35` | — （キットの所在＝`N:\irodori-native-research\kit-ssd\`・`decisions.md` 21） |
| **追加**（ポート） | — | — | **追加**＝**既定ポート 18088**・bind `127.0.0.1`・**既存 8088 と併用可**（同時起動で衝突しない）。判定＝配布版を起こした状態で 8088 の既存個体を起こし、両方の `/health` が 200 になること。根拠＝`decisions.md` 2。※ 18088 にする以上、本体側は接続先の改修が要る（`docs/contract.md` ⑼ の **D-9**）。 |

---

## 2. 改訂の一覧（改訂 11 件＋追加 1 件）

| 行 | 改訂の要旨 | 根拠 |
|---|---|---|
| スパイク | Radeon 版限定・bf16 固定で「1 発目 ≤ 5 s（同一形状・db 温）」・3 発目条件を落とす | `40` §8（bf16 2.841 s／fp32 32.077 s・3 発目の再発は再現せず） |
| VRAM | 「S_lat 比例（≒3.5 MB／フレーム）・14.5 s 出力で 3.17 GiB」の注記 | `40` §1 |
| 導入 | vc_redist を初回取得で通す前提で操作 ≤ 6 | `22` 表 3b・`36` |
| 起動失敗 | 範囲外 GPU・綴り違いは 0 s で弾く（上流に渡す前に） | 設計書 §4-6 |
| ライセンス | 同梱分（`licenses/`）と初回取得分（`first-run-notices.md`）に分ける・zlib は未特定のまま | `decisions.md` 8・`22` 未確認 2 |
| 安全 | **api_key なし**（自動生成もしない） | `decisions.md` 2 |
| ログ | 状態判定の行を 3 行に | 設計書 §4-1 |
| API（`/params`） | `default: null` 0 件は **`exposed_to_ywk: true` の欄**に対して撃つ | 設計書 §0 A-5 行・§4-2 ⑴⑵ |
| API（絶対パス） | 「絶対パス 0 件」の適用範囲を明示（エラー body＋`/health`・`/ywk/status`・`/params`・`/v1/audio/voices`）・判定を段の突合に強化 | 設計書 §4-7・敵対検分（契約）10・11 |
| 安全 | 上流の書き込み 3 口を外す | `decisions.md` 31・`docs/contract.md` ⑷ 4-3・⑼ D-3 |
| 話者 | `voices.json` が無い／壊れていても「デフォルト」1 件・一覧は 500 を返さない | `docs/contract.md` ⑷・⑼ D-8 |
| （追加） | ポート 18088・8088 と併用可 | `decisions.md` 2 |

（設計書 §7 が指示した改訂は **5 件**〔ポート 18088・api_key なし・スパイク・VRAM・導入〕。
本檔はそれに **3 件**〔起動失敗・ログ・ライセンス〕を足し、さらに敵対検分（便 A・A-8）の是正で
**4 件**〔API 2 行・安全・話者〕を足した＝いずれも設計書 §4-6／§4-1／§1／§4-2／§4-7 と
`decisions.md` 31 に根拠がある行で、上表に出典を明記した。）

---

## 3. 満たしていない・未確認のまま残るもの（正直な欠落）

| # | 何 | なぜ | いつ埋まるか |
|---|---|---|---|
| 1 | `torch/lib/zlibwapi.dll` の出所と license（A14） | wheel の `third_party/` に zlib 単体の LICENSE が無い（`22` 未確認 2）。**便 B 実測＝両変種に在る（89,088 B）・torch 配下に LICENSE/NOTICE は 1 檔も無い**（`decisions.md` 84） | 対象 DLL を外せるか検分したとき（`.alt.dll` 2 檔の方が削減余地は大きい） |
| 2 | ~~cu126 の古いドライバでの実効（U-14）~~ **埋まった（便 B・2026-09-05）** | 537.58 で実射＝通る（`decisions.md` 80）。528.33〜537.57 の帯は未実測のまま | — |
| 3 | vc_redist 欠落機での実挙動（`30` V-5） | 欠落機を用意していない | 便 E |
| 4 | 「licensed Visual Studio users」に Community 版が含まれるかの読み（`22` 未確認 4） | **法解釈＝席は断定しない** | 司令官の裁定 |
| 5 | LGPL-2.1 §6 の a〜e のどれで libsndfile の義務を満たすか（`22` 未確認 5） | **法解釈＝席は断定しない** | 司令官の裁定 |
| 6 | CUDA wheel を入れた NVIDIA 無し機で CPU 合成が 200 で返るか（`36` W-1） | **便 B の代用測定（GPU を隠す・537.58）＝cu126 は通る／cu130 は最初の合成でプロセスが 0xC0000005 で消える**（起動と自己申告は健全に見える）。降格後に撃ったため cu130 の原因は分離できていない（`decisions.md` 83）。本物の NVIDIA 無し機は未確認 | 便 E（NVIDIA 無し機を用意したとき）。それまでランチャは `is_available()=False` で cu130 を起動しない |
| 7 | ~~Radeon 版の「機体で初回」の実測（空 MIOpen db からの導入直後）~~ **→ 便 C で埋めた** | **実測（2026-09-05・`docs/radeon.md` §7・なま値 `build/out/probe-log/rocm-warmup-20260905-022123.json`）**＝空 db で 起動（`/health` 200＋`runtime.loaded=true`）**29.0 s** ＋ 暖機 6 射 **51.8 s** ＝ **80.8 s**。2 回目以降（db 温）は 26.1 s ＋ 12.6 s ＝ **38.7 s**。空 db でも「起動 ≤ 60 s」は満たす（罰は起動ではなく最初の合成に出る） | **埋まった**（便 C・実測） |
| 8 | **cpu 変種 torch の `torch/lib/libiomp5md.dll`（1,614,192 B）と `torch/lib/libiompstubs5md.dll`（43,888 B）の許諾＝未特定 2 件**（A19 ⑵） | `torch-2.10.0+cpu-cp312-cp312-win_amd64.whl` の `METADATA` が名乗る `License-File:` は **`LICENSE` と `NOTICE` の 2 件だけ**で、`LICENSE`（third_party 33 件の連結）にも `NOTICE` にも **OpenMP 系の項目が 1 件も無い**（2026-09-05・wheel を開いて実見＝`licenses/first-run-notices.md` A19 ⑵）。**法解釈は席が断定しない**（`decisions.md` 22） | 便 B（この 2 檔を外して cpu 変種が動くか実射する／出所を特定する）か司令官の裁定。※ **rocm 変種の torch では同じ物の許諾が名乗られている**（`License-File:` 107 件に `third_party/llvm-openmp/LICENSE.txt` がある＝同 B7 ⑴）＝この欠落は **cpu 変種に固有**である |
| 9 | ~~**Radeon 版の 11 プリセット参照で RTF < 0.5（便 C の C-8）＝未達**~~ **→ 便 C（2）＝参照潜在キャッシュで 2 巡目は解消。原因⑵ は未確認のまま残る** | **便 C の実測**（`docs/radeon.md` §7-6・db 温・プロセス 2 巡目）＝**11 本とも 200 だが RTF 0.271〜1.016**（0.5 未満は 4 本）。原因は 2 つとも実測＝⑴ 話者を切り替えるたびの `prepare_reference` 0.95〜1.44 s、⑵ **連続運転で純計算の `sample_rf` が 618 ms → 1,400 ms（2.3 倍）に伸びる**（1 分空けると戻る）。**便 C（2）の再測**（`docs/radeon.md` §7-8・なま値 `build/out/probe-log/rocm-latent-20260905-040058.json`・`…-040405.json`・対照 `…-040637.json`）＝`decisions.md` 65 の `.pt` を焼いた後は **`prepare_reference` 1.0〜4.3 ms**（2 巡目・初見でも 7.7〜16.7 ms）で、**11 本の 2 巡目 RTF は 0.222〜0.398＝11/11 が 0.5 未満**（`empty_cache_interval` 0 と 10 の両走で）。同じ日・同じ樹の **wav 対照**は 2 巡目 0.355〜0.667（0.5 未満は 6/11）＝**原因⑴ は焼けば消える**ことが実測で確かめられた | **⑴ は埋まった**（便 C（2）・実測）。**⑵ は未確認のまま**＝この便の走は **22 射・約 35 s** で、出力 1 秒あたりの `sample_rf` は前半 197〜201 ms/s・後半 196〜204 ms/s と平ら＝**2.3 倍は再現していない（消えた証拠ではない）**。**長い連射（35 射・約 2 分級）で RTF < 0.5 が保つかは未実射**＝司令官／設計席の裁定。※ **プロセスで最初の 1 発だけは潜在でも RTF 0.726〜0.848** で 0.5 を超える（暖機で償う枠＝§7-3） |
| 10 | **Radeon 版で `IRODORI_EMPTY_CACHE_INTERVAL=0` を焼いたときの GPU メモリの伸び** | 実測（`docs/radeon.md` §7-7・Windows の `GPU Process Memory` をサーバの pid で絞った値）＝読込直後 **3.37 GiB でほぼ一定**（4 個体）だが、**1 セッション（35 射）で 10.3〜13.7 GB まで単調に増える**（解放が採れた標本は 0）。この機体は共有メモリ 107 GB なので破綻しないが、**VRAM の少ない dGPU で同じ既定を使ったときの挙動は未確認**。**便 C（2）が `0` と `10`（上流既定）を並べて撃った材料**＝`docs/radeon.md` §7-8 ⑶＝⒜ **射の所要への増分は見えない**（該当した 10 射目・20 射目の「HTTP 所要 − 上流の `total_to_decode`」は 18.6／6.4 ms で、同じ走の他 20 射の 6.0〜23.9 ms に埋もれる）、⒝ **OS の `GPU Process Memory` は `=10` でも下がる標本が 0**（終端 8,638,349,312 B ／ `=0` は 9,159,557,120 B・差 ≈497 MiB だが走は 1 本ずつ）、⒞ **標本間隔が実測 1.4〜1.8 s** なので「解放の直後だけ凹む」動きは採れていない | 便 B（CUDA 機）か、Radeon 版で `empty_cache_interval` の既定を分けるかの裁定（`decisions.md` 62・**便 C（2）は数値を出しただけで裁定していない**） |
| 11 | **rocm 変種 torch の `torch/lib/liblzma.dll`（196,096 B）と `torch/lib/aotriton_v2.dll`（14,948,352 B）の許諾＝未特定 2 件**（B7 ⑵） | `torch-2.13.0+rocm10.0.0-cp312-cp312-win_amd64.whl` の `METADATA` が名乗る `License-File:` は **107 件**、`dist-info/licenses/` 配下の實檔も **107 件**で数は合うが、そのどれにも **`lzma`／`xz`／`aotriton` に当たる名が 1 件も無い**（2026-09-05・組んだ樹 `build/out/runtime-rocm-gfx1151/site-packages/` で実見）。2 件は無関係でもない＝`aotriton_v2.dll` の import に `liblzma.dll` が並ぶ（同じく実見＝`KERNEL32.dll`・`MSVCP140.dll`・`amdhip64_7.dll`・`liblzma.dll`）。**法解釈は席が断定しない**（`decisions.md` 22） | 便 B／便 E（この 2 檔を外して rocm 変種が動くか実射する／出所を特定する）か司令官の裁定。※ 8 番（cpu 変種の OpenMP）と同型の欠落であり、`check-licenses.ps1` は構造上これを拾えない（未特定の判定は台帳 item の `license` 欄の文字列だけを見るので、`Apache-2.0 AND ... AND MIT` を名乗る rocm torch は掛からない） |
| 12 | **C-5 の「本物が待たされるのは暖機の 1 射分（≤ 10 s）」のうち、`≤ 10 s` が未達** | 「高々 1 射分」は実測で満たす（`docs/radeon.md` §7-4）が、**1 射が 10 s を超える**＝なま値（`build/out/probe-log/rocm-warmup-20260905-022123.json`）で **db 温の `11s` 段が 10,888.8 ms**、**空 db の既定 `12s` 段が 11,690.8 ms**。この 2 射に本物を重ねた走は**無い**（`priority.warmup_at_fire.elapsed_s = 0.406`＝撃ったのは 6,182.2 ms の 1 射目の最中だけ）。担保する仕掛けも無い＝`stages` の上限は `_WARMUP_MAX_SECONDS = 60.0`（契約も「0 < s ≤ 60」）なので、無鍵の口（`decisions.md` 31・`127.0.0.1` 束縛）に長い段を投げれば 1 射はさらに伸びる | 設計席／司令官の裁定＝⑴ `stages` の秒に実測に合わせた上限を設ける、⑵ 条件文を「1 射分（実測 1.2〜11.7 s）」に改める、のどちらか。**既定段（4／8／12）のままでは空 db で 11.7 s** なので、「≤ 10 s」を残すなら既定段も変えることになる |
