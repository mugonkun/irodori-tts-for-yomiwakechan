# v2-copy — 利用者の目に入る文字列の全文（そのまま貼れる確定文案）

> 正本＝`docs/design/charter.md`（2026-09-11 裁可）。この檔は憲章 §7 段 2 の出る物である。
> 画面の構造と実装の切り分けは `v2-spec.md`、入れる順は `v2-plan.md`。**この檔は「文字」だけを決める。**
>
> **守った規則（全行に当てた）**
> - 憲章 §6-1 の隠す語を 1 語も書かない（変種・実行系・取得台帳／台帳・潜在・焼き印・暖機・サーバ・ポート・
>   裁定・`decisions.md`・wrapper・上流・門・便・配布版／配布物・詳細の外の RTF・詳細の外の GiB・Release の外の sha256）。
> - 憲章 §6-2 の技術語は原語のまま出す（CUDA・ROCm・cu126／cu130・GPU・VRAM・NVIDIA・AMD・Radeon・
>   GeForce・RTX・ドライバの版・bf16・FP32・Windows・wav・Python）。
> - 版の名は **`RTX（CUDA）`／`Radeon（ROCm）`** の 2 つだけ。題の飾りは **` － `**。
> - 量の数は**憲章 §4「数字の 1 枚表」の 3 つだけ**＝ダウンロード **約 5.3 GB**／残る量 **約 8 GB**／
>   作業に要る空き **約 12 GB**。ほかの量を利用者向けの面に書かない。
> - **報告の送り先は X の `@yomiwakechan`**（`decisions.md` 131・2026-09-11）＝公式ページの脚・〔このアプリについて〕・
>   使い方の「困ったときは」・リリース文の **4 面に同じ 1 行**で置く。**GitHub の Issues へは案内しない。**
> - **置き場と削除は版ごと**（`decisions.md` 133・2026-09-11）＝**RTX（CUDA）と Radeon（ROCm）は別のアプリ**で、
>   設定も声もダウンロードした物も共有しない（`%LOCALAPPDATA%\irodori-tts-ywk-cuda\`／`…-radeon\`）。
>   削除は**その版の置き場を丸ごと消し、問わない**。**置き場の外**（利用者が声を追加するときに選んだ元の wav）には触れない。
> - **内部の識別子は 1 文字も動かさない**（`AutomationId`・設定の鍵・JSON の欄名・契約・`AppId`・導入先・
>   資産の檔名・台帳名）。この檔が替えるのは**人間が読む文字列だけ**である。
> - リポジトリは 2026-09-10 から public。旧文の「private だから開けない」という註は**古い**ので引かない。

---

## §0 8 つの検分文を、この檔の全文に当てた結果

| 原則 | 検分文 | 当てた結果 |
|---|---|---|
| 1 配管を見せない | 2 段の検分（検索で他所の解説が出るか／その場の判断に直結するか） | §9-1 の禁語表で全文を検めた＝**0 件**。技術語は §9-2 の場所にだけ残した |
| 2 VOICEROID2 のように | ふだんの起動で押す釦は〔しゃべらせる〕1 つか | 主画面から起動・停止・やり直しの 3 釦が消える（§1-1・§4）＝**1 つ**で合格 |
| 3 初回だけ正直に | 釦を押す前の 1 画面に ⑴ GB ⑵ 分 ⑶ なぜ が全部あるか | 公式ページの断り枠（§5）に 3 つとも入れた。ウィザード段 1・2 にも入れた |
| 4 選ばせない | 初回の道の上に、利用者が答えを持たない問いが無いか | ウィザードから「選ぶ段」が消えた（§2 段 2 は**確かめるだけ**）。変える口は 設定 › 詳細（§4）だけ |
| 5 連携は何もしない | 「こちら側で何かする」と読めるか | 帯の右は事実 1 行のみ（§3-1）。番号 18088 は**埋まった 1 件**だけ（§3-2 E-05） |
| 6 失敗は 3 部品 | ⑴何が⑵なぜ⑶次の 1 手 が揃っているか | §3-2 の 14 態すべてを 3 部品で書いた。**⑶ は 1 つだけ**（E-02 は代替の有無で 2 行に割った）。⑶ の無い文は 1 つも無い |
| 7 主画面は監視盤でない | 「詳細」は 12 行以内か | 設定 › 詳細は **8 行**（§4＝畳みの中で数える行。段 F の是正で 10 → 8＝⑼⑽ は畳みの外へ）。入らなかった 7 件は移さずに**削除**（§4 末尾）。詳しい状態の詳細は 11 行（`v2-spec.md` §2-2） |
| 8 帳面と分ける | リポを見たことがない人が最後まで読めるか | §5 公式ページ・§6 使い方・§7 リリース文・§8 このアプリについて から、檔名・番号・実測値を全部落とした |

---

## §1 いまの文字列の全表（file:line → 新しい文字列）

**読み方**＝「新」の欄が `削除` は**その要素ごと画面から無くす**、`詳細へ` は**文字はそのままで置き場だけ
設定 › 詳細 に移す**、`据え置き` は 1 文字も替えない。`AutomationId` は**どの行でも替えない**。

### 1-1 `Views/MainWindow.xaml`

| 行 | いま | 新 |
|---|---|---|
| 5 | `Title="irodori-TTS for 読み分けちゃん"`（設計時の見本） | 据え置き（実行時は `AppTitle` が `irodori-TTS for 読み分けちゃん － RTX（CUDA）` を差す） |
| 39 | サーバ起動 | **詳細へ**（設定 › 詳細「読み上げの動作」・札は「**もう一度動かす**」／id `MainStartButton` 据え置き） |
| 42 | サーバ停止 | **詳細へ**（札は「**いったん止める**」／id `MainStopButton` 据え置き） |
| 45 | 初回取得をやり直す | **詳細へ**（札は「**はじめの準備をやり直す**」／id `MainFirstRunButton` 据え置き） |
| 48 | `MainHeaderText`＝「変種 CUDA 13.0（ドライバ 580 以上）／開発モード（…）」 | **削除**（要素と id は残し、本文は開発ビルドのときだけ入れる） |
| 55 | タブ 状態 | **歯車の中へ**（札は「**詳しい状態**」／id `TabStatus` 据え置き） |
| 58 | タブ 話者 | **声** |
| 61 | タブ 発話テスト | **据え置き**（タブ帯の先頭に置く＝既定タブ） |
| 64 | タブ 設定 | 据え置き |
| 67 | タブ このアプリについて | **歯車の中へ**（札は据え置き／id `TabAbout` 据え置き） |
| — | （新設）歯車と、その中の層 | `MainGearButton`・`MainDetailOverlay`・`MainDetailTabs`・`MainDetailCloseButton`（タブ帯は **3 枚**＝憲章 §4-13） |
| — | （新設）状態帯の文 | §3-1 の 3 語。新 id `MainBandStateText`・`MainBandReasonText`・`MainBandActionButton`・`MainOpenLogButton` |
| 23 | `MainStateText`（下の帯） | §3-1 の 3 語を入れる（要素・id 据え置き＝無人検分の錨） |
| 28 | `MainEndpointText`＝`http://127.0.0.1:18088` | **本文を空**に（要素・id 据え置き） |
| 33 | `MainVersionText`＝`v1.1.0／…` | **`v2.0.0`** の 1 行だけ（`upstream` の綴りは落とす） |

### 1-2 `Views/StatusView.xaml`（タブ「詳しい状態」）

| 行 | いま | 新 |
|---|---|---|
| 36 | 状態 | **いまの様子** |
| 40 | デバイス | **削除**（`cuda:0` は機械の綴り。使っている GPU は 詳細の「使うグラフィックス」で製品名を出す） |
| 75 | 取得へ進む | **はじめの準備をする** |
| 88 | 実行系を組み直す | **動かすための一式を入れ直す** |
| 103 | やめる | 据え置き |
| 116 | GPU | **詳細へ**（札「**使うグラフィックス**」・値は `NVIDIA GeForce RTX 3090` の製品名／UUID の行は削除） |
| 119 | 変種 | **詳細へ**（札「**動かし方**」・値は `CUDA 13.0`／`CUDA 12.6`／`ROCm`／`CPU`） |
| 122 | 接続先 | **削除**（`StatusEndpointText` の要素と id は残すが、番号を常時は出さない＝18088 を出すのは埋まって起動できないときの帯 1 件だけ） |
| 129 | 暖機 | **詳細へ**（札「**準備運転**」） |
| 132 | 参照潜在 | **詳細へ**（札「**声の下ごしらえ**」） |
| 151 | GPU メモリ | **詳細へ**（札据え置き・既定 OFF のまま） |
| 156 | 潜在キャッシュ | **詳細へ**（札「**下ごしらえ済みの声**」） |
| 161 | 参照ボイス | **詳細へ**（札「**元の音声から読む声**」） |
| 166 | 「使用量・占有量＝torch の allocator の中の値です。…焼いた参照潜在（.pt）は増えません。」 | **削除**。差し替え 1 行＝「**数字は、いま GPU にどれだけ載っているかの目安です。**」 |
| 172 | ログ（末尾 20 行） | **詳細へ**（札「**記録（末尾）**」／`StatusLogBox` は畳みの中に残す・id 据え置き） |

### 1-3 `Views/TryView.xaml`（タブ「発話テスト」）

| 行 | いま | 新 |
|---|---|---|
| 31 | 本文 | **しゃべらせる文** |
| 45 | 話者 | **声** |
| 51 | サンプリング歩数 | **品質** |
| 53 | 釦 `10` | **はやい** |
| 56 | 釦 `40` | **きれい** |
| 59 | `TryStepsBox`（数値欄） | **詳細へ**（札据え置き） |
| 64 | 演技指示（caption・空＝既定） | **話し方の指示（任意・例：明るく／落ち着いて）** |
| 70 | cfg_scale_text（空＝既定） | **詳細へ**（札「**文への効き**（cfg_scale_text）」） |
| 75 | cfg_scale_caption（空＝既定 3.0） | **詳細へ**（札「**演技の効き**（cfg_scale_caption）」） |
| 80 | cfg_scale_speaker（空＝既定） | **詳細へ**（札「**声への効き**（cfg_scale_speaker）」） |
| 85 | 読み速さ（0.25〜4.0・空＝既定） | **速さ（1.0 がふつう）** |
| 90 | 乱数の種（空＝毎回変わる） | **詳細へ**（札据え置き） |
| 99 | 合成して再生 | **しゃべらせる** |
| 102 | もう一度再生 | **もう一度** |
| 105 | 停止 | 据え置き |
| 108 | wav に保存… | **音声ファイルに保存…** |
| 24 | `TryConcurrencyText`（橙の枠） | §3-3 の 2 文（ふだんは非表示） |

### 1-4 `Views/VoicesView.xaml`（タブ「声」）

| 行 | いま | 新 |
|---|---|---|
| 46 | 列 表示名（＝話者 id） | **名前** |
| 51 | 列の ToolTip「この列が話者 id です（本体・発話テストはこの名前で話者を選びます）。日本語で構いません。」 | **読み分けちゃん2 と「発話テスト」で選ぶ名前です。日本語で構いません。** |
| 55 | 列 参照 wav の檔名 | **元の音声ファイル** |
| 59 | 列の ToolTip「配布版が voices\refs\ へ写した檔の名前です（話者 id ではありません）。」 | **アプリが写しておいた音声ファイルの名前です（声の名前ではありません）。** |
| 63 | 列 種別 | 据え置き（値＝「**同梱**」「**自分で追加**」「**なし**」） |
| 64 | 列 参照潜在 | **下ごしらえ**（値＝「**済み**」「**未**」「**やり直しが要る**」） |
| 65 | 列 メモリ（1 名あたり） | **削除**（合計だけ一覧の下に薄字で「いま 12 人ぶんの声を用意しています。」） |
| 71 | 話者を追加する | **声を追加する** |
| 75 | ① 参照する音声 | **① 元にする音声ファイル（wav）** ＋薄字「10〜30 秒くらいの、その人だけがしゃべっている録音がよく合います。」 |
| 77 | 選ぶ… | 据え置き |
| 86 | ToolTip「「選ぶ…」で選ぶか、檔の場所をそのまま貼り付けてください。」 | **「選ぶ…」で選ぶか、ファイルの場所を貼り付けてください。** |
| 90 | ② 話者の名前（日本語可） | **② 名前（日本語で構いません）** |
| 94 | 演技指示の既定（任意） | **話し方の指示（任意・例：明るく元気に）** |
| 99 | この内容で追加する | **この声を追加する** |
| 105 | 選んでいる話者 | **選んでいる声** |
| 114 | 試聴 | 据え置き |
| 119 | 停止 | 据え置き |
| 126 | 削除 | 据え置き |
| 142 | 一覧を更新 | 据え置き |
| 146 | 参照潜在を焼く | **この声を下ごしらえしておく（次から少し速くなります）** |
| 155 | 同梱のプリセットを入れ直す | **最初から入っている声を入れ直す** |

### 1-5 `Views/SettingsView.xaml`

**上（ふだんの設定・4 つだけ）**＝⑴ アプリを開いたら自動で読み上げできるようにする（`SettingsAutoStartCheck`）
⑵ よく使う声を、開いたときに準備しておく（`SettingsWarmupCheck`）⑶ 声のファイルの場所（`SettingsVoicesDirText`＋〔フォルダを開く〕）
⑷ 〔ログを開く〕（新 id `SettingsOpenLogButton`）。**それ以外は 詳細（§4）か 削除。**

| 行 | いま | 新 |
|---|---|---|
| 28 | 適用 | 据え置き |
| 31 | 取り消し | 据え置き |
| 44 | GPU | **詳細へ**（札「**使うグラフィックス**」） |
| 46 | 数え直す | **詳細へ**（札「**見つけ直す**」） |
| 61 | 選んだ GPU は UUID で覚えます（起動のたびに番号へ解決します）。 | **削除** |
| 63 | 実行系の種類（変種） | **詳細へ**（札「**動かし方**」・値は `自動（おすすめ）`／`CUDA 13.0`／`CUDA 12.6`／`ROCm`／`CPU`） |
| 76 | 精度（上級者） | **詳細へ**（札「**音質と速さ**」・値は `bf16`（速い・既定）／`FP32`（重い）の原語） |
| 85 | ポート | **削除**（`SettingsPortBox`・`SettingsPortNoteText` の要素と id は残すが、詳細に行として並べない＝§4 末尾） |
| 93 | 暖機 | **上へ**（札「**よく使う声を、開いたときに準備しておく**」） |
| 94 | サーバが待機になったら暖機を撃つ | **開いたときに、よく使う声を先に準備しておく** |
| 97 | 段（秒・カンマ区切り） | **削除**（12 行に入らない＝移さずに消す） |
| 100 | 撃つ話者（カンマ区切り・空＝段だけ） | **削除** |
| 104 | 暖機の最中でも合成はできます（本物の要求が必ず勝ち、走っている 1 射の分だけ待たされます）。 | **準備中でも、しゃべらせられます。** |
| 106 | 参照潜在キャッシュ | **詳細へ**（札「**声の下ごしらえ**」） |
| 107 | 話者の登録時に参照潜在（.pt）を焼く | **声を追加したときに、下ごしらえしておく（次から少し速くなります）** |
| 115 | 中間（■）＝変種の既定に任せる・チェック＝必ず焼く・空＝焼かない。 | **中間（■）＝おまかせ・チェック＝必ずする・空＝しない。** |
| 117 | GPU キャッシュの解放間隔 | **削除** |
| 125 | 起動 | **詳細へ**（札「**読み上げの動作**」） |
| 126 | ランチャの起動と同時にサーバを起こす | **アプリを開いたら、自動で読み上げできるようにする**（上の 4 つの 1 番目） |
| 129 | GPU メモリの欄を状態タブに出す | **削除** |
| 132 | 待機になるまでの上限（秒・0＝変種の既定） | **削除** |
| 140 | データの置き場 | **詳細へ**（札「**ファイルの場所**」） |
| 153 | データ | **データ** 据え置き |
| 157 | モデル | **削除**（「データ」に畳む） |
| 161 | 話者 | **声** |
| 165 | 実行系 | **削除** |
| 169 | 配布樹 | **アプリ** |
| 175 | 置き場の移動はまだ対応していません（配布樹は読むだけ・書き込みはすべてデータの下に入ります）。 | **場所は変えられません。** |
| 182 | 取得キャッシュ | **詳細へ**（札「**一時ファイル**」） |
| 191 | 消すのはこの変種の取得台帳に載っている原檔だけです。…（取り直しが要る機体からは消しません）。 | **ダウンロードのときに使った一時ファイルだけを消します。消しても、もう一度ダウンロードすることにはなりません。** |

### 1-6 `Views/FirstRunWizard.xaml`（はじめの準備）

| 行 | いま | 新 |
|---|---|---|
| 4 | `Title="初回取得"`（見本） | 据え置き（実行時は `WizardTitle`＝**はじめの準備 － RTX（CUDA）**） |
| 42 | 戻る | 据え置き |
| 49 | 中断 | **やめる** |
| 65 | 上の通知を読み、初回取得で第三者物が自分の機体に入ることに同意します。 | **上の内容を読みました。はじめの準備で、これらをこのパソコンにダウンロードすることに同意します。**（**チェックは残す**＝決裁 Q3・`FirstRunAcceptCheck`／`CanAcceptNotices`／`acceptedNoticesSha256` 据え置き・**同じ内容なら 2 度目は出さない**） |
| 78 | 実行系の種類 | **削除**（選ぶ段そのものを廃す＝決裁 Q1。`FirstRunVariantCombo` ほかは 詳細の畳みへ・id 据え置き） |
| 98 | 取得の見積り | **これからすること** |
| 103 | 取得は途中で中断できます。次に開いたときは続きから取り直します（檔ごとに sha256 で検証します）。 | **途中でやめられます。次に開いたときは、続きから始めます。ダウンロードした物は 1 つずつ、壊れていないか確かめます。** |
| 124 | 準備ができました。 | **使えます。** |
| 126 | 「発話テスト」のタブで、本文と話者を選んで 1 発鳴らしてみてください。 | **さっそく、ひとことしゃべらせてみましょう。読み分けちゃん2 で使うときは、このアプリを開いたままにしてください。** |
| 129 | `FirstRunDoneTrailList` | **詳細へ**（畳みの中・id 据え置き） |
| — | （新設）〔全文を見る〕 | 新 id `FirstRunNoticesFullButton`（§2 段 1） |

### 1-7 `Views/AboutView.xaml`（このアプリについて）

| 行 | いま | 新 |
|---|---|---|
| 21 | 版 | **バージョン** |
| 24 | `AboutUpstreamText`＝`Irodori-TTS 8224daf／…` | **詳細へ** |
| 26 | 電子透かし | **音に埋め込まれる印** |
| 30 | 参照ボイスの扱い | **声の元にする音声について** |
| 34 | ライセンスの所在 | **ライセンス** |
| 50 | 実行系・モデル・vc_redist は配布物に入っていません。初回取得で、取得台帳の URL と sha256 に従って利用者の機体へ入ります。各許諾文はその展開先に残ります。 | **動かすための一式と声のデータは、このアプリには入っていません。はじめの準備のときに、公式の配布元からこのパソコンへダウンロードします。それぞれのライセンス文も一緒に入ります。** |
| — | （新設）末尾の 2 釦 | **〔使い方を見る〕**（`docs/guide.md` を開く）／**〔報告用のログを保存〕**（ファイルの場所を出し、その隣に「**X の @yomiwakechan までお知らせください**」の 1 行＝`decisions.md` 131） |

### 1-8 C# 側（UI に届く文字列）

| file:line | いま | 新 |
|---|---|---|
| `ViewModels/ReleaseFlavor.cs:95` | `"ROCm 版"` | **`"Radeon（ROCm）"`** |
| `ViewModels/ReleaseFlavor.cs:96` | `"CUDA 版"` | **`"RTX（CUDA）"`** |
| `ViewModels/ReleaseFlavor.cs:103` | `baseName + "（" + FlavorLabel + "）"` | **`baseName + " － " + FlavorLabel`** |
| `ViewModels/ReleaseFlavor.cs:110` | `WizardTitle`＝`Decorate("初回取得", …)` | **`Decorate("はじめの準備", …)`**（幹だけ替える＝出来上がる窓題は「**はじめの準備 － RTX（CUDA）**」。**幹に本体名を入れない**＝` － ` が 2 つ並ぶのを避ける・`v2-spec.md` §3・`v2-plan.md` F-1 と同じ 1 つの文字列） |
| `Contracts/RuntimeVariants.cs:92` | CUDA 13.0（ドライバ 580 以上） | **CUDA 13.0（ドライバ 580.00 以上）** |
| `Contracts/RuntimeVariants.cs:93` | CUDA 12.6（ドライバ 528.33 以上） | 据え置き |
| `Contracts/RuntimeVariants.cs:94` | CPU（遅い・配信用途では非推奨） | **CPU（とても遅い・配信には向きません）** |
| `Contracts/RuntimeVariants.cs:95` | Radeon gfx1151（未保障・bf16 固定） | **ROCm（Radeon 向け・bf16 固定）** |
| `Contracts/RuntimeVariants.cs:111` | `ShortDisplayName(CudaLabel)`＝`"CUDA 版"` | **`"CUDA（版を指定しない既定）"`**＝これは**動かし方の短い名**であって版の名札ではない（「このドライバ（537.58）では 〈名〉 は動きません」の文中に差す）。**版の名札 `RTX（CUDA）` を入れない**＝同じ文で「動かし方」と「版」の名が混ざる |
| `ViewModels/StatusViewModel.cs:510-515` | 停止／起動中／読込中／待機／暖機中／失敗 | **止まっています／準備しています…／準備しています…／使えます／使えます／止まりました**（帯は起動直後の `Stopped` だけ「準備しています…」に読み替える＝§3-1） |
| `ViewModels/StatusViewModel.cs:161` | 取得が未了です。 | **まだ準備が終わっていません。** |
| `ViewModels/StatusViewModel.cs:306` | 実行系を組み直しています… | **動かすための一式を入れ直しています…** |
| `ViewModels/StatusViewModel.cs:586` | CPU（GPU を使いません）／未選択 | 据え置き（詳細の中） |
| `ViewModels/StatusViewModel.cs:587` | `名前（UUID …a1b2c3）` | **`名前` だけ**（同名 2 枚のときだけ「（1 枚目）」「（2 枚目）」） |
| `ViewModels/StatusViewModel.cs:622-626` | 設定は次回の起動から有効です（いま走っているのは …・http://127.0.0.1:18088・…）。 | **変えた設定は、次にアプリを開いたときから効きます。** |
| `ViewModels/StatusViewModel.cs:829-831` | 選んだ GPU（UUID …）と、実際に載った GPU（UUID …／名前不明）が違います。 | **選んだグラフィックスと、実際に使われたグラフィックスが違います。**（詳細の中） |
| `ViewModels/StatusViewModel.cs:903` | CPU（GPU メモリなし） | 据え置き（詳細の中） |
| `ViewModels/StatusViewModel.cs:912` | —（モデル未読込） | **—（まだ読み込んでいません）** |
| `ViewModels/StatusViewModel.cs:915-921` | torch 使用量 …／占有量 … | **いま使っている量 …／確保している量 …**（詳細の中・`GiB` は据え置き） |
| `ViewModels/StatusViewModel.cs:1019-1041` | （焼いた話者 N 名・合計 …） | **（下ごしらえ済みの声 N 人・合計 …）**（詳細の中） |
| `ViewModels/StatusViewModel.cs:1067-1074` | 1 名あたり…（全員分＝全部を同時に載せたときの上限 …） | **1 人あたり…（全員ぶん＝…）**（詳細の中） |
| `ViewModels/StatusViewModel.cs:1133-1137` / `1164-1168` | 未実施／実行中／完了／失敗／中止 | 据え置き（詳細の中） |
| `ViewModels/StatusViewModel.cs:1205` | 配布物の上流 pin（…）と、走っているサーバの pin（…）が違います。 | **アプリと、いま動いている中身の版が食い違っています。入れ直してください。**（詳細の中） |
| `ViewModels/UiText.cs:20` | 未対応 | 据え置き（詳細の中） |
| `ViewModels/UiText.cs:31` | —（サーバが動いていません） | **—（まだ準備ができていません）** |
| `ViewModels/UiText.cs:17,91-125` | `—`・時間と秒の書式 | 据え置き（`Bytes` は 1 文字も触らない＝置き場を 詳細 に限る） |
| `ViewModels/TryViewModel.cs:36` | こんにちは。読み分けちゃん用の irodori-TTS です。 | **こんにちは。文字を打つと、この声でしゃべります。** |
| `ViewModels/TryViewModel.cs:69` | 合成の手が落ちました：… | **うまく作れませんでした。**（＋〔ログを開く〕・詳しい字は檔へ） |
| `ViewModels/TryViewModel.cs:96` | 合成はサーバ 1 プロセスにつき 1 本ずつ走ります。配信中にここで撃つと、本体の読み上げがその分だけ待たされます。 | §3-3 の 2 文に差し替え |
| `ViewModels/TryViewModel.cs:336` | 撃てません。 | **しゃべらせられません。** |
| `ViewModels/TryViewModel.cs:343` | サーバが動いていません（先に「サーバ起動」を押してください）。 | **まだ準備ができていません。準備が終わるまでお待ちください。** |
| `ViewModels/TryViewModel.cs:347` | 合成しています… | **作っています…** |
| `ViewModels/TryViewModel.cs:366` | 合成が期限内に終わりませんでした。 | **時間がかかりすぎたので、やめました。文を短くしてお試しください。** |
| `ViewModels/TryViewModel.cs:393-396` | 所要 1420 ms／出力 3.30 秒／RTF 0.43／seed … | **3.3 秒の音を 1.4 秒で作りました。**（`RTF`・`seed`・`ms` は 詳細 の中へ） |
| `ViewModels/TryViewModel.cs:404` | 再生中（音量を −16 dBFS 相当に揃えています）。 | **再生しています。** |
| `ViewModels/TryViewModel.cs:443-444` | サーバが落ちました（exit N）。 | **読み上げの用意が止まりました。**＋〔もう一度動かす〕 |
| `ViewModels/TryViewModel.cs:467` | 保存する音がありません。 | **保存する音がありません。先に〔しゃべらせる〕を押してください。** |
| `ViewModels/TryViewModel.cs:474` | 保存しました（12.3 MiB）。 | **保存しました。** |
| `ViewModels/TryViewModel.cs:479,484` | 保存できませんでした：… | **保存できませんでした。**（＋〔ログを開く〕） |
| `ViewModels/VoicesViewModel.cs:93-95` | 一覧を読み直せませんでした／削除できませんでした／事前計算を始められませんでした：… | **一覧を読み直せませんでした。／消せませんでした。／下ごしらえを始められませんでした。**（＋〔ログを開く〕） |
| `ViewModels/VoicesViewModel.cs:193-195` | 参照ボイスは、権利者の許諾がある音声だけを使ってください。実在の人物の声を本人の許諾なく模倣することは、上流 Irodori-TTS の利用条件（Ethical Restrictions 1・No Impersonation）で禁じられています。 | **元にする音声は、使ってよいと許しを得た物だけにしてください。実在する人の声を、本人の許しなくまねることは、この音声合成の利用条件（Ethical Restrictions 1・No Impersonation）で禁じられています。**（**平易化はしても短縮しない**） |
| `ViewModels/VoicesViewModel.cs:203` | 参照 wav は 10〜30 秒を勧めます（…）。 | **10〜30 秒くらいの、その人だけがしゃべっている録音がよく合います（…）。** |
| `ViewModels/VoicesViewModel.cs:299-302` | 覚えていた N 名の事前計算を M 回試しても始められませんでした（「参照潜在を焼く」を押してください）。 | **N 人ぶんの下ごしらえを始められませんでした。〔この声を下ごしらえしておく〕を押してください。** |
| `ViewModels/VoicesViewModel.cs:365,379,396-399` | サーバの話者一覧が…／台帳だけで一覧を出しています。 | **声の一覧を読み込めませんでした。分かっている名前だけを出しています。** |
| `ViewModels/VoicesViewModel.cs:428,495,593` | 話者の登録系がまだ組み込まれていません（便 D・話者席の実装待ち）。 | **この操作はまだできません。** |
| `ViewModels/VoicesViewModel.cs:440` | 「◯」を追加しました（再起動は要りません）。 | **「◯」を追加しました。すぐ使えます。** |
| `ViewModels/VoicesViewModel.cs:453,458` | 参照 wav を写せませんでした：… | **音声ファイルを取り込めませんでした。** |
| `ViewModels/VoicesViewModel.cs:489` | この話者は消せません。 | **この声は消せません。** |
| `ViewModels/VoicesViewModel.cs:524-525` | 「◯」を削除しました。／「◯」は台帳にありませんでした（何も消していません）。 | **「◯」を消しました。／「◯」は見つかりませんでした（何も消していません）。** |
| `ViewModels/VoicesViewModel.cs:568-576` | 参照潜在の事前計算が走っています。終わってから削除してください。ほか | **いま下ごしらえの最中です。終わってから消してください。** |
| `ViewModels/VoicesViewModel.cs:612-617` | 同梱のプリセットを N 名入れ直しました。／入れ直すプリセットはありませんでした（…）。 | **最初から入っている声を N 人ぶん入れ直しました。／入れ直す声はありませんでした。** |
| `ViewModels/VoicesViewModel.cs:647-648` | 名を改めた N 名（旧→新）。 | **名前が変わった声が N 人います（旧→新）。** |
| `ViewModels/VoicesViewModel.cs:665-672` | この話者は試聴できません。／「◯」を試聴中（3.3 秒）。 | **この声は試聴できません。／「◯」を試聴しています。** |
| `ViewModels/VoicesViewModel.cs:706-761` | 焼き直しが要る話者はありません。／サーバが動いていないので、参照潜在の事前計算はできません。／…事前計算… | **やり直しが要る声はありません。／まだ準備ができていないので、下ごしらえはできません。／下ごしらえを始めました（N 件）。** |
| `ViewModels/VoicesViewModel.cs:798-803` | 話者台帳が読めませんでした：… | **声の一覧が読めませんでした。**（＋〔ログを開く〕） |
| `ViewModels/VoiceRow.cs:39-40` | 参照なし／サーバ側／プリセット／利用者 | **なし／このアプリの外／同梱／自分で追加** |
| `ViewModels/VoiceRow.cs:48` | 要・焼き直し／焼き済み／未 | **やり直しが要る／済み／未** |
| `ViewModels/VoiceRow.cs:79-80` | 「◯」は一覧に常在するので消せません。／「◯」は配布版の台帳にありません（サーバ側の走査で見えている檔です）。 | **「◯」はいつでも使える声なので消せません。／「◯」はこのアプリが管理していない声なので消せません。** |
| `ViewModels/VoiceRow.cs:109-119` | 「◯」は参照なしの話者なので試聴する音がありません。ほか | **「◯」には試聴できる音がありません。／「◯」の元の音声は wav ではないので試聴できません（追加としゃべらせるには使えます）。** |
| `ViewModels/SettingsViewModel.cs:97` | （device 連動・既定）, bf16, fp16, fp32 | **（おまかせ・既定）, bf16, fp16, FP32**（技術語は原語のまま＝詳細の中） |
| `ViewModels/SettingsViewModel.cs:232-233` | 上級者向け。既定は device 連動（GPU→bf16・CPU→fp32）です。／この変種は bf16 固定です（fp32 を載せるとサーバが起動前に止まります）。 | **おまかせだと、GPU では bf16、CPU では FP32 になります。／この動かし方は bf16 だけです。** |
| `ViewModels/SettingsViewModel.cs:272-273` | 既定 18088。塞がっていたら次を探さずに止まって告げます。／既定（18088）から変えています。本体（読み分けちゃん2）の接続先も同じ値にしてください。 | **ふつうは触りません（18088）。／ふつうと違う番号にしています。読み分けちゃん2 の側も同じ番号にしてください。** |
| `ViewModels/SettingsViewModel.cs:341-344` | 変種の既定（ON）に従います。…参照潜在を焼き、話者を切り替えるときの待ちが減ります。 | **おまかせ（いまは ON）。ON にすると、声を切り替えるときの待ちが減ります。** |
| `ViewModels/SettingsViewModel.cs:367-368,421-423,441-442` | GPU キャッシュ／待機の上限／取得キャッシュを消す（…） | 前 2 つは**削除**。3 つ目は **一時ファイルを消す（◯ GB）**／**一時ファイルはありません** |
| `ViewModels/SettingsViewModel.cs:517-573` | GPU の列挙系がまだ…／GPU を数えています…／N 台見つかりました（120 ms）。 | **まだできません。／グラフィックスを探しています…／N 台見つかりました。** |
| `ViewModels/SettingsViewModel.cs:582-600` | 暖機の段が読めません。／ポートは 1〜65535 です。／この配布物には変種「◯」の取得台帳がありません。 | 1 つ目は**削除**。**つなぎ口の番号は 1〜65535 です。／この動かし方には対応していません。** |
| `ViewModels/SettingsViewModel.cs:620` | 設定を保存しました。変種・GPU・ポートを変えたときは、サーバを起動し直すと効きます。 | **設定を保存しました。変えた設定は、次にアプリを開いたときから効きます。** |
| `ViewModels/SettingsViewModel.cs:644` | 変更を取り消しました。 | 据え置き |
| `ViewModels/SettingsViewModel.cs:693` | （勧め＝CUDA 12.6・自動では切り替えません） | **（このパソコンには CUDA 12.6 が合います）** |
| `ViewModels/FirstRunViewModel.cs:243-249` | 段の題 7 つ | §2 の 4 つに畳む（**お知らせ／これからすること／準備しています／使えます**・内部の 7 段は据え置き） |
| `ViewModels/FirstRunViewModel.cs:267-273` | もう一度／同意して次へ／この構成で取得を始める／発話テストへ／次へ | **もう一度／次へ／準備を始める／しゃべらせてみる／次へ**（`Done` の釦は「発話テストへ」→「**しゃべらせてみる**」） |
| `ViewModels/FirstRunViewModel.cs:350-353` | CPU は GPU の数百分の一の速さです…／ROCm 版です（Radeon の GPU 向け）…／NVIDIA の GPU で動きます。ドライバの版が下限に届いているか下の行を確かめてください。 | §2 段 2 の「決めた結果を告げる 1 行」に差し替え（詳細の中では原文の技術語のまま） |
| `ViewModels/FirstRunViewModel.cs:452` | 通知文を読み込めていないので同意できません（配布物を確かめてください）。 | **お知らせの全文が読めないので、先へ進めません。アプリを入れ直してください。** |
| `ViewModels/FirstRunViewModel.cs:457,465,515` | 通知に同意しました。／変種＝◯ を選びました。／初回取得が終わりました。 | **詳細へ**（畳みの中の記録・字は据え置きでよい／`Record` は檔にも残る） |
| `ViewModels/FirstRunViewModel.cs:584,589` | いま中断できる仕事はありません。／中断しました（続きから取り直せます）。 | **いまはやめられません。／やめました。次に開いたときは、続きから始めます。** |
| `ViewModels/FirstRunViewModel.cs:700-716` | 通知文（licenses/first-run-notices.md）が配布物に見つかりません。…／通知文が読めませんでした：… | **お知らせの全文が見つかりません。この状態でははじめの準備を始められません。アプリを入れ直してください。** |
| `ViewModels/FirstRunViewModel.cs:752,765-767` | 【空き容量が足りません】必要 …・いまの空き …（あと …）。 | §3-2 E-09 の 3 部品に差し替え |
| `ViewModels/FirstRunViewModel.cs:814-832` | 不明（取得台帳が読めません）／取得 …（実行系 …・モデル …・vc_redist …） | **約 5.3 GB をダウンロードします**（丸めた散文の定数）。内訳の実数は**詳細の中**に `GiB` のまま |
| `ViewModels/FirstRunViewModel.cs:982-985` | 実行系は揃っているので取得と展開は飛ばします（モデルだけ取り直します）。／実行系は揃っているので展開は飛ばします。 | **必要な部品は揃っています。声のデータだけをダウンロードします。／必要な部品は揃っています。** |
| `ViewModels/FirstRunViewModel.cs:1000,1083,1189,1251` | 取得系／vc_redist の導入系／展開系／モデルの取得系がまだ組み込まれていません（便 D…）。 | **まだできません。** |
| `ViewModels/FirstRunViewModel.cs:1021-1039` | 取得台帳が読めないので取得を始められません（…）。 | §3-2 E-03 の 3 部品に差し替え |
| `ViewModels/FirstRunViewModel.cs:1050,1215,1267` | 取得に失敗しました：…／展開に失敗しました：…／モデルの取得に失敗しました。 | §3-2 E-10 の 3 部品に差し替え |
| `ViewModels/FirstRunViewModel.cs:1054,1224` | 実行系を取得しました（N 件）。／展開しました（N 檔・… ）。 | **詳細へ**（畳みの中の記録） |
| `ViewModels/FirstRunViewModel.cs:1087,1118` | Visual C++ 再頒布可能パッケージを確かめています…／…を入れています… | **Microsoft の部品を確かめています…／Microsoft の部品を入れています…**（§2 段 3 の許可の予告と対で使う） |
| `ViewModels/FirstRunViewModel.cs:1105-1107` | Visual C++ 再頒布可能パッケージの導入を飛ばしました（利用者の選択）。torch の読み込みで msvcp140.dll が…手で入れてください。 | **Microsoft の部品を入れずに進みました。うまく動かないときは、はじめの準備をやり直してください。** |
| `ViewModels/FirstRunViewModel.cs:1146-1168` | 待機／接続／取得／検証／着地／取得済み／完了／失敗 ＋ 速度・残り | **詳細へ**（畳みの中）。表の 1 行は §2 段 3 の 4 文だけ |
| `ViewModels/FirstRunViewModel.cs:1301-1324` | サーバを起こしています…／サーバが起動しました。／サーバを起こせませんでした（状態タブの理由を見てください）。 | **動くか確かめています…／使えるようになりました。／**§3-2 E-12 の 3 部品 |
| `ViewModels/MainViewModel.cs:231-232` | 実行系／モデルが揃っていないため、取得からやり直します（不足＝…）。 | **必要な部品が足りないので、はじめの準備からやり直します。** |
| `ViewModels/MainViewModel.cs:319,424,435-436` | 変種の実行系がまだありません（展開の段をやり直してください）。ほか | **まだ準備が終わっていません。** ＋〔はじめの準備をする〕 |
| `ViewModels/MainViewModel.cs:177-181,497-957` | `AppendLog` に落ちる行すべて | **据え置き**（檔に残る記録＝画面には出さない。憲章 §5「ログの綴りは変えない」） |
| `ViewModels/AboutViewModel.cs:29-30` | これは非公式のランチャです。Aratako 氏および Irodori-TTS／Irodori-TTS-Server とは関係がありません。上流は無改変のまま commit で固定して使っています。 | §8 の断り 3 行に差し替え |
| `ViewModels/AboutViewModel.cs:34` | 合成した音声には SilentCipher の電子透かしが入ります（切る経路はありません）。 | §8 の「音に埋め込まれる印」に差し替え |
| `ViewModels/AboutViewModel.cs:39` | 版 v1.1.0 | **バージョン v2.0.0 － RTX（CUDA）** |
| `ViewModels/AboutViewModel.cs:44-45` | Irodori-TTS 8224daf／Irodori-TTS-Server …／開発ビルド（上流 pin は焼かれていません） | **詳細へ**（畳みの中・字は据え置き） |
| `ViewModels/MemoryEstimate.cs:95-106` | 参照なし（増えません）／潜在参照（…）／wav 参照（概算 +…） | **増えません／下ごしらえ済み（…）／元の音声から読む（およそ +…）**（詳細の中） |
| `Services/Gpu/VariantRecommendation.cs:132-207` | このドライバ（537.58）では CUDA 13.0 は動きません（下限 580.00）。別の変種を選んでください。ほか | §3-2 E-02 の 3 部品に差し替え（**ドライバの版の数字は残す**＝§9-2） |
| `Services/Gpu/VariantGate.cs:102-165` | この機体で GPU を見られません（…）。cpu の変種に切り替えてください。ほか 5 文 | §3-2 E-06／E-11 の 3 部品に差し替え。**画面に出す文とログに落とす文を分ける**（ログは据え置き） |
| `Services/Gpu/VariantGate.cs:361-363`（`Label`） | `"CUDA 版"` | **`"CUDA（版を指定しない既定）"`**（`:366` の助詞継ぎを通って断りの 1 行に出る。**版の名札は `ReleaseFlavors.FlavorLabel` の 1 箇所だけが綴る**＝同じ画面に 2 つの名を並べない） |
| `Services/Gpu/VariantGate.cs:382-387` | 設定で別の変種を選んでください／cpu の変種に切り替えてください | **設定の「詳細」で動かし方を変えてください** |
| `Services/Models/AcquisitionCheck.cs:69` | 上の「初回取得をやり直す」から取得してください。 | **〔はじめの準備をする〕を押してください。** |
| `Services/Models/AcquisitionCheck.cs:92-97,193-207` | 実行系（変種 …）／モデル N 檔（…） | **詳細へ**（畳みの中・字は据え置き） |
| `Services/Server/ServerStateMachine.cs:92-94` | ポート N は既に使われています。別のアプリを閉じるか、設定でポートを変えてください（自動では別のポートを探しません）。 | §3-2 E-05 の 3 部品に差し替え（**18088 の数字は残す**＝原則 5 の唯一の例外） |
| `Services/Server/ServerStateMachine.cs:147` | サーバが応答しなくなりました（プロセスは生きています）。停止してから起こし直してください。 | §3-2 E-08 の 3 部品に差し替え |
| `Services/Server/ServerStateMachine.cs:156` | `RuntimeLoadFailedPrefix`＝モデルの読込に失敗＝ | **据え置き**（ログと内部判定に使う綴り。画面へは §3-2 E-07 が出る） |
| `ViewModels/WarmupStagesText.cs:60-103` | 暖機の段は…／暖機で撃つ話者は 64 件までです。 | **削除**（設定の該当欄ごと消える＝§1-5） |
| `Contracts/ServerEnvironment.cs:71` | `DefaultVoiceId = "デフォルト"` | **据え置き**（契約に載る id＝1 文字も動かさない） |
| `Views/MainWindow.xaml.cs:48` | `v1.1.0／Irodori-TTS 8224daf…／開発ビルド` | **`v2.0.0`** だけ |
| `Views/MainWindow.xaml.cs:57-58` | 変種 CUDA 13.0（ドライバ 580 以上）／開発モード（…） | **削除**（開発ビルドのときだけ出す） |
| `Views/VoicesView.xaml.cs:44-45` | 参照する音声を選ぶ／音声（…）｜すべての檔（*.*） | **元にする音声ファイルを選ぶ／音声ファイル（…）｜すべてのファイル（*.*）** |
| `Views/VoicesView.xaml.cs:88-95` | 写した参照 wav と焼いた参照潜在も消えます。…「◯」を削除します。…／話者の削除 | **この声のために取り込んだ音声ファイルも消えます。最初から入っている声は〔最初から入っている声を入れ直す〕でいつでも戻せます。／「◯」を消します。よろしいですか。／声を消す** |
| `Views/TryView.xaml.cs:49-59` | 保存する音がありません。先に合成してください。／保存／合成した音声を保存する／WAV（*.wav） | **保存する音がありません。先に〔しゃべらせる〕を押してください。／保存／音声ファイルに保存する／WAV（*.wav）** |
| `Views/FirstRunWizard.xaml.cs:57-62` | 「はい」＝Visual C++ 再頒布可能パッケージを入れます（Microsoft の公式 URL から取得し、管理者の確認が 1 回出ます）。「いいえ」＝…msvcp140.dll が見つからないと出ます）。／Visual C++ 再頒布可能パッケージ | **Microsoft の部品を 1 つ入れます。Windows の許可の窓が 1 度出るので「はい」を押してください。入れないと、しゃべらせられないことがあります。〔入れる〕〔入れない〕／Windows の許可について** |
| `Views/FirstRunWizard.xaml.cs:80-81` | 取得が走っています。中断して閉じますか（続きから取り直せます）。／初回取得 | **準備の途中です。やめて閉じますか。次に開いたときは、続きから始めます。／はじめの準備** |

---

## §2 はじめの準備（ウィザード）の全文

**押す回数＝3**（次へ → 準備を始める → しゃべらせてみる）＋**チェック 1 つ**（初回のみ・決裁 Q3）。
**選ぶ問いは 1 つも無い**（決裁 Q1）。段番号は **1 / 3・2 / 3・3 / 3**、完了は番号なし。
窓題＝**はじめの準備 － RTX（CUDA）**（Radeon 機では **はじめの準備 － Radeon（ROCm）**）。

### 段 1 ／ 3 ＝ お知らせ

> **お知らせ**
>
> このアプリは、ほかの方が作ったプログラムと音声モデルを使って動きます。
> それらはこのアプリには入っていないので、**これからこのパソコンへダウンロードします**（約 5.3 GB・10 分ほど）。
> ほかの方が作った物を勝手に配って回らない方針だからです。
> それぞれに作者と利用条件があり、その全文はダウンロードのあと、このパソコンにも残ります。
>
> 〔**全文を見る**〕（新 id `FirstRunNoticesFullButton`・押すと畳みが開いて全文が出る／もう一度押すと畳む）
>
> ☐ **上の内容を読みました。はじめの準備で、これらをこのパソコンにダウンロードすることに同意します。**
> 　（`FirstRunAcceptCheck`・`CanAcceptNotices` の錠は据え置き＝全文が読めないときはチェックできない）
>
> 〔**次へ**〕　〔**やめる**〕

**全文が読めないとき**（差し替え・チェックも要約も出さない）＝
「**お知らせの全文が見つかりません。この状態でははじめの準備を始められません。アプリを入れ直してください。**」

**2 度目以降**＝同じ内容のお知らせには**二度と聞かない**（`acceptedNoticesSha256` が一致する回は段 1 を飛ばす）。

### 段 2 ／ 3 ＝ これからすること（**確かめるだけ・選ばせない**）

> **これからすること**
>
> このパソコンに合わせて、動かし方を選びました。**NVIDIA の GPU（GeForce RTX 3090）・CUDA 13.0 で動かします。**
> **これから約 5.3 GB をダウンロードします。10 分ほどかかります**（回線の速さによります）。
> 途中でやめられます。次に開いたときは、続きから始めます。
>
> ▸ **詳細（上級者向け）** — 動かし方を自分で選ぶ（CUDA 13.0／CUDA 12.6／ROCm／CPU）・落とす量の内訳
> 　（新 id `FirstRunAdvancedExpander`・既定は閉。中に `FirstRunVariantCombo`・`FirstRunVariantNameText`・
> 　`FirstRunVariantNoteText`・`FirstRunDriverText`・`FirstRunVariantBlockText`・`FirstRunSizeText` を**据え置きのまま**入れる）
>
> 〔**準備を始める**〕　〔**戻る**〕　〔**やめる**〕

**告げる 1 行の型**（機体で変わる）＝
- NVIDIA・CUDA 13.0＝「**NVIDIA の GPU（〈製品名〉）・CUDA 13.0 で動かします。**」
- NVIDIA・CUDA 12.6＝「**NVIDIA の GPU（〈製品名〉）・CUDA 12.6 で動かします**（グラフィックスドライバの版が 580.00 未満のためです）。」
- Radeon＝「**AMD の Radeon（〈製品名〉）・ROCm で動かします。**」
- GPU 無し＝「**このパソコンには対応する GPU が見つかりませんでした。とても遅い方法（CPU）で試すこともできますが、配信には使えません。**」
  （**CPU は、GPU がある機体では一切見せない**＝憲章 §7 既定）

### 段 3 ／ 3 ＝ 準備しています

> **準備しています**
>
> ████████░░░░░░░░　**42 %　あと 6 分ほど**
>
> （1 行だけ・いまの段に応じて 1 つ）
> ・**必要な部品をダウンロードしています。**
> ・**落とした物を組み立てています。**
> ・**声のデータをダウンロードしています。**
> ・**動くか確かめています。**
> ・**新しくなった分をダウンロードしています。**（更新の回だけ）
>
> **このままお待ちください。ネットにつないだままにしてください。ほかの作業をしていてかまいません。**
>
> （Microsoft の部品を入れる直前だけ 1 行）
> **Microsoft の部品を入れます。Windows の許可の窓が 1 度出るので「はい」を押してください。**
>
> ▸ **詳細（上級者向け）**（速さ・件数・残りバイト・`FirstRunTrailList` はこの中）
>
> 〔**やめる**〕

- 進捗は**1 本のバーで全体**を出す（段ごとに 0 へ戻さない）。
- 等幅フォントは外す（`Consolas, MS Gothic` の指定を落とす）。
- 失敗した回は §3-2 の 3 部品を本文に出し、釦を〔**もう一度**〕にする。

### 完了

> **使えます。**
>
> さっそく、ひとことしゃべらせてみましょう。
> 読み分けちゃん2 で使うときは、**このアプリを開いたままにしてください**（閉じると読み上げも止まります）。
>
> ▸ **詳細（上級者向け）**（`FirstRunDoneTrailList`）
>
> 〔**しゃべらせてみる**〕

---

## §3 状態の帯と、失敗の全文（3 部品）

### 3-1 帯の状態（名乗るのは 3 語だけ）

形＝`● 〈ひとこと〉` ＋ 必要なときだけ理由 1 行 ＋ 1 手の釦 ＋ 右端に常時〔**ログを開く**〕。

| いつ | 丸 | 帯の文 | 釦 |
|---|---|---|---|
| 開いた直後・準備が走っている | 灰 | **準備しています…** | なし |
| 声を読み込んでいる | 灰 | **準備しています…（1〜2 分かかることがあります）** | なし |
| 使える | 緑 | **使えます** | なし |
| 使える・声を先に準備している | 緑 | **使えます（声を準備しています）** | なし |
| 利用者が止めた | 灰 | **止まっています** | 〔**もう一度動かす**〕 |
| 新しい版が出ている | 緑 | **使えます**（右に薄字で **新しい版があります**） | 〔**新しい版を見る**〕 |
| 失敗 | 赤 | **止まりました。** ＋ §3-2 の理由 1 行 | §3-2 の 1 手 |

**帯の右端（連携の 1 行・事実だけ）**
- **読み分けちゃん2 から使えます**
- **まだ読み分けちゃん2 から呼ばれていません**
- **いま読み分けちゃん2 の読み上げに使われています**

**帯に出してはならないもの**＝記録の生の行／`variant=…`／`接続先 http://127.0.0.1:18088`／UUID／
「暖機」「参照潜在」「変種」の語。**技術語そのものは禁じていない**＝「CUDA 12.6 に切り替えます」
「ドライバは 580.00 以上が必要です」は帯に出してよい。

**〔ログを開く〕**＝在り処ごと開く。檔が無い回は「**まだ記録がありません。**」を出し、**押せない釦は出さない**。
脇に薄字「**うまくいかないときは、このファイルを添えて X の @yomiwakechan までお知らせください。**」

### 3-2 失敗の全 14 態（⑴何が起きたか ⑵なぜか ⑶次にやること）

| # | ⑴ 何が起きたか | ⑵ なぜか | ⑶ 次にやること |
|---|---|---|---|
| E-01 | **まだ準備が終わっていません。** | はじめの準備が最後まで済んでいません。 | 〔**はじめの準備をする**〕 |
| E-02a | **この GPU では、いまの動かし方が使えません。** | グラフィックスドライバが古いためです（いまの版 〈537.58〉・CUDA 13.0 には 580.00 以上が必要です）。CUDA 12.6 に切り替えて準備し直せます。 | 〔**CUDA 12.6 で準備しなおす**〕（押すと、動かすための一式を 5 GB 前後ダウンロードし直します。10 分ほどかかります） |
| E-02b | **この GPU では、いまの動かし方が使えません。** | グラフィックスドライバが古いためです（いまの版 〈537.58〉・必要なのは 〈580.00〉 以上です）。このパソコンで使える別の動かし方がありません。 | 〔**NVIDIA のドライバ更新ページを開く**〕 |
| E-03 | **アプリのファイルが壊れているようです。** | 必要なファイルの一覧が読めませんでした。 | 〔**入れ直す手順を見る**〕（`docs/guide.md` の「困ったときは」を開く） |
| E-04 | **動かすための一式が、いまのアプリに合っていません。** | 前に入れた部品と、いまのアプリの組み合わせが合いません。 | 〔**動かすための一式を入れ直す**〕 |
| E-05 | **ほかのソフトが、このアプリのつなぎ口（18088）を使っています。** | 同じ番号を先に使っているソフトがあると、読み上げの用意ができません。 | 〔**使っているソフトを調べる方法を見る**〕 |
| E-06 | **対応するグラフィックスが見つかりませんでした。** | NVIDIA も AMD の GPU も見つからないか、グラフィックスドライバが読めませんでした。 | 〔**ドライバの入れ方を見る**〕 |
| E-06b | **このパソコンのグラフィックスは、この版では使えません。** | このパソコンに入っているのは AMD の Radeon ですが、いま入れてあるのは RTX（CUDA）版です。 | 〔**Radeon（ROCm）版のページを開く**〕（逆の機体では NVIDIA のグラフィックスと RTX（CUDA）版を案内する） |
| E-07 | **声の読み込みに失敗しました。** | 声のデータが途中までしか入っていない可能性があります。 | 〔**はじめの準備をやり直す**〕 |
| E-08 | **反応がなくなりました。** | 読み上げの準備が、途中で固まったままになっています。 | 〔**もう一度動かす**〕 |
| E-09 | **ディスクの空きが足りません。** | 準備には空き **約 12 GB** が要ります。 | **要らないファイルを消してから**〔**もう一度**〕 |
| E-10 | **ダウンロードが途中で止まりました。** | ネットにつながらなくなったか、配布元が応答しませんでした。 | **ネットにつないでから**〔**もう一度**〕（**続きから始めます。最初からにはなりません。**） |
| E-11 | **Microsoft の部品を入れられませんでした。** | Windows の許可の窓で「いいえ」が押されたか、窓が閉じられました。 | 〔**もう一度**〕（次に出る許可の窓で「はい」を押してください） |
| E-12 | **準備に時間がかかりすぎました。** | 時間内に、読み上げの準備が終わりませんでした（ほかのソフトが重いときに起きやすくなります）。 | 〔**もう一度動かす**〕 |
| E-99 | **うまく動きませんでした。** | 理由が分かりませんでした。 | 〔**ログを開く**〕 |

**書き方の規則**＝⑴ は太字 1 文・**利用者の言葉**。⑵ は 1 行・**技術語を出してよい**（ドライバの版・CUDA の版・18088）。
⑶ は**1 つだけ**。**生の記録・番号つきの状態・スタックトレースは画面に出さない**（檔には残る）。

### 3-3 〔しゃべらせる〕が押せないときの 1 行（決裁 Q4）

| いつ | 出す文 |
|---|---|
| 読み分けちゃん2 が読み上げに使っている間 | **いま読み分けちゃん2 の読み上げに使われています。終わってからお試しください。**（釦は押せない） |
| まだ準備中 | **準備しています…**（釦の脇・釦は押せない） |
| ふだん（読み上げが来ていない） | **何も出さない**（橙の枠は非表示） |

---

## §4 設定 › 詳細（上級者向け）— **8 行**（上限 12 行）

見出し＝**詳細（上級者向け）**（新 id `SettingsAdvancedExpander`・既定は閉）
冒頭の 1 行＝**ここは、うまく動かないときに中を見るための欄です。ふだんは見なくてかまいません。**
**この表は `v2-spec.md` §2-5 と同じ物である**（片方だけ直さないこと）。

> **10 行 → 8 行に改めた**（v2.0 段 F の是正・2026-09-11・medium 17）。段 F がこの行の並びを閉じた席で、
> 当て込んだ姿がこちらである。**中身は 1 つも落としていない**＝畳みの外か、ふだんの設定の側へ動いただけ。
> - ⑼ **記録**＝〔ログを開く〕は**ふだんの設定の側**（`SettingsOpenLogButton`）に 1 つだけ置いた。
>   〔報告用のログを保存〕は〔このアプリについて〕に在る（`decisions.md` 131＝X の宛先の隣＝
>   「困ったら、この 1 檔を添えて送る」が 1 か所で完結する）。**同じ釦を 2 面に置かない。**
> - ⑽ **〔適用〕〔取り消し〕**＝**畳みの外**（`SettingsView.xaml` の `DockPanel.Dock="Bottom"`）。
>   ふだんの設定は 4 つとも畳みの**外**に在るので、保存の手が畳みの**中**にあると、
>   「詳細を開かないとふだんの設定を保存できない」という行き止まりになる。
>   前置きの 1 文（**変えた設定は、次にアプリを開いたときから効きます。**）も同じ場所に付く。
>
> したがって**畳みの中で数える行は 8 つ**である（`v2-spec.md` §2-5・§7 原則 7 の数も 8）。
> 上限 12 行（憲章 原則 7）には、どちらの数え方でも収まっている。

| # | 札 | 値・釦 |
|---|---|---|
| 1 | **使うグラフィックス** | `NVIDIA GeForce RTX 3090` ／〔見つけ直す〕 |
| 2 | **動かし方** | `自動（おすすめ）＝CUDA 13.0` ／ `CUDA 13.0` ／ `CUDA 12.6` ／ `ROCm` ／ `CPU`　※ **変えると、動かすための一式を 5 GB 前後ダウンロードし直します（10 分ほど）。** |
| 3 | **音質と速さ** | `bf16`（速い・既定）／`FP32`（重い）※ Radeon（ROCm）では出さない |
| 4 | **声の下ごしらえ** | 声を追加したときに、下ごしらえしておく（次から少し速くなります） |
| 5 | **読み上げの動作** | 〔いったん止める〕〔もう一度動かす〕〔はじめの準備をやり直す〕（id は `MainStopButton`／`MainStartButton`／`MainFirstRunButton` のまま） |
| 6 | **更新のとき、新しくなった分だけ取り直す** | チェック（既定 ON／新 id `SettingsDifferentialUpdateCheck`） |
| 7 | **ファイルの場所** | データ／アプリ ＋〔フォルダを開く〕　※ **版ごとに別**（RTX（CUDA）＝`%LOCALAPPDATA%\irodori-tts-ywk-cuda\`／Radeon（ROCm）＝`…\irodori-tts-ywk-radeon\`）。**場所は変えられません。** |
| 8 | **一時ファイル** | 〔消す（◯ GB）〕 |

**畳みの外**（数に入れない・上の断りを見よ）＝⒜ 〔ログを開く〕はふだんの設定の側（`SettingsOpenLogButton`）・
〔報告用のログを保存〕は〔このアプリについて〕（`decisions.md` 131） ⒝ **変えた設定は、次にアプリを開いた
ときから効きます。** ＋〔適用〕〔取り消し〕は畳みの下（`SettingsApplyButton`／`SettingsRevertButton`）。

**入らなかったので消した 7 件**（移さずに削除＝憲章 原則 7）＝GPU の UUID の行・GPU メモリの欄を出す設定・
準備運転の段（秒）・準備運転で使う声の一覧・GPU キャッシュの解放間隔・準備を待つ上限（秒）・**つなぎ口の行**。
**設定の鍵は残す**（`settings.json` は 1 つも欄を減らさない＝手で書けば効く）。
**「準備運転」の札は捨てる**＝起動時の先読みは **§1-5 のふだんの設定**「よく使う声を、開いたときに準備しておく」1 箇所だけで綴る
（同じ設定を 2 箇所に置かない）。
**「動かすための一式を入れ直す」は 詳しい状態の側**（`StatusRebuildRuntimeButton`・畳みの外）に 1 つだけ置く。
**つなぎ口の番号 18088 を出す面は 1 つだけ**＝埋まって起動できないときの帯（§3-2 E-05・憲章 原則 5 の唯一の例外）。
`SettingsPortBox`・`SettingsPortNoteText` は**要素と id を残すが、番号を常時は出さない**。

---

## §5 `site/index.html` の全文（v2）

**構造**＝見出し → 3 行のリード → **正直な断り枠（釦より前）** → 釦 2 つ → 使い方 3 手順（画像 3 枚）→
必要なもの → よくある質問 5 つ → 註記。**CSS と JS は据え置き**（`dl-cuda`／`dl-rocm` の id と資産名の末尾で拾う所）。
**このページに出さない**＝sha256・バイト数・`SHA256SUMS.txt`・`gfx1151`・`cu130`／`cu126`・つなぎ口の番号・
`install.md` への案内・送り先を名指しする 1 行。

### header

> irodori-TTS for 読み分けちゃん
> 　（アプリの名。見出しより小さく、頁の頭に 1 行だけ置く）
>
> # 文字を打つと、しゃべります。
>
> Windows のパソコンで動く、無料の日本語 読み上げ（音声合成）アプリ
>
> - むずかしい準備は要りません。**入れて、開いて、打つだけ**です。Python も要りません。
> - **12 人の声（13 種類）**が最初から入っています。自分の音声ファイルから声を増やすこともできます。
> 　（人数は 12・一覧の行数は 13＝`voices/presets.json` の `presets` が 13 件。おふとんP が「きざ」と
> 　「のーまるv2」の 2 種類あるため）
> - 配信ソフト **読み分けちゃん2** の読み上げにも使えます。

### notice（釦より **前**・枠つき）

> **はじめて開いたときに、動かすための一式（約 5.3 GB）をインターネットからダウンロードします。
> 10 分ほどかかります。**
>
> ほかの方が作ったプログラムや音声モデルを、勝手に配って回らない方針だからです。そのぶんを、
> はじめて開いたときにあなたのパソコンへダウンロードします。
> ネットにつないだままお待ちください。**一度すませば、次からはすぐ使えます。**

### buttons

> ### お使いのパソコンに合う方を選んでください
>
> **［ RTX（CUDA）をダウンロード ］**
> 　NVIDIA のグラフィックス（GeForce・RTX など）が入っているパソコンの方
>
> **［ Radeon（ROCm）をダウンロード ］**
> 　AMD の Radeon が入っているパソコンの方。動作を確かめられているのは Ryzen AI MAX+ 395 ／ Radeon 8060S の
> 　パソコンで、ほかの Radeon では試していません。
>
> 　最新版
> 　（**版の番号は書かない**＝JS が最新のリリースから `tag_name` を拾えたときだけ「最新版 v…」へ
> 　書き換わる。拾えないとき（JS を切っている・API に届かない・まだ公開していない）は番号を出さない）
>
> ▸ **どちらか分からないときは**
> 　キーボードの `Windows` キーを押しながら `X` を押し →「デバイス マネージャー」→「ディスプレイ アダプター」を開いてください。
> 　**NVIDIA** や **GeForce** と書いてあれば **RTX（CUDA）**、**Radeon** と書いてあれば **Radeon（ROCm）** です。
> 　それでも分からないときは **RTX（CUDA）** をお試しください。合わないときはアプリが教えます。

### howto

> ## 使い方（3 ステップ）
>
> **1. ダウンロードして、インストールする**
> 　落ちてきたファイルをダブルクリックして、「次へ」「インストール」「完了」を押すだけです。
> 　管理者のパスワードは要りません。
> 　`［画像 1：インストーラの完了画面］` → `site/img/step1-setup.png`
> 　`alt="インストーラの完了画面"`
>
> **2. アプリが開いたら、そのまま待つ**
> 　はじめの 1 回だけ、動かすための一式をダウンロードします（約 5.3 GB・10 分ほど）。
> 　途中で 1 度、Windows の許可の窓が出るので「はい」を押してください。
> 　「使えます。」と出たら終わりです。
> 　`［画像 2：はじめの準備の進み具合］` → `site/img/step2-setup.png`
> 　`alt="はじめの準備の進み具合"`
>
> **3. しゃべらせてみる**
> 　声を選び、文を打って、〔しゃべらせる〕を押します。1〜2 秒で鳴ります。
> 　読み分けちゃん2 で使うときは、**このアプリを開いたまま**にしてください。
> 　`［画像 3：しゃべらせているところ］` → `site/img/step3-talk.png`
> 　`alt="しゃべらせているところ"`
>
> *画像＝横 1100 px 以上（表示は 960 px 目安）・`<img>` に `loading="lazy"` と `width`／`height` を付ける。*

### require

> ## 必要なもの
>
> - **Windows 11 / 10（64 ビット）** のパソコン
> - **NVIDIA のグラフィックス（GeForce / RTX など）**、または **AMD の Radeon**
> 　（Radeon で確かめられているのは Ryzen AI MAX+ 395 ／ Radeon 8060S です）
> - **グラフィックスドライバ**が新しいこと。NVIDIA なら **580.00 以上**だと CUDA 13.0 で、
> 　**528.33 以上**なら CUDA 12.6 で動きます。古いままだと、アプリが理由と直し方を出します。
> - **空き容量 12 GB** ほど（準備のあいだだけ必要です。終わったあと、このアプリが使うのは **約 8 GB** です）
> - **インターネット接続**（はじめの準備のときだけ必要です）
>
> グラフィックスが無いパソコンでも動きますが、**とても遅い**ので配信には使えません。

### faq（`<details>` で畳む・5 つ）

> ## よくある質問
>
> **はじめの 1 回だけ時間がかかるのはなぜ？**
> 　このアプリには、ほかの方が作ったプログラムと音声モデルが入っていません。勝手に配って回らない方針だからです。
> 　そのぶんを、はじめて開いたときにあなたのパソコンへダウンロードします（約 5.3 GB・10 分ほど）。
> 　**2 回目からはすぐ使えます。**
>
> **RTX（CUDA）と Radeon（ROCm）は何が違うの？**
> 　GPU の計算のしくみが違うだけで、**できることも、入っている声も同じ**です。
> 　NVIDIA のグラフィックスは CUDA、AMD の Radeon は ROCm を使うので、入れる物が別になっています。
> 　まちがえて入れても、開いたときにアプリが教えます。そのときは、このページからもう一方をダウンロードして
> 　入れてください。まちがえた方は Windows の「**設定 → アプリ**」から消せます。
> 　**2 つは別のアプリです。**同じパソコンに両方入れられますが、設定も、追加した声も、ダウンロードした物も
> 　それぞれ別に持ち、**同時には開けません**（あとから開いた方が、つなぎ口が塞がっていると出ます）。
>
> **「WindowsによってPCが保護されました」と出ました**
> 　「**詳細情報**」を押してから「**実行**」を押してください。個人が作った無料のソフトで、
> 　Windows がまだ見慣れていないために出る画面です。
>
> **読み分けちゃん2 ではどう使うの？**
> 　このアプリを**開いたまま**にして、読み分けちゃん2 の設定でこのエンジンを有効にするだけです。
> 　あとは読み分けちゃん2 の側が自動で見つけます。声は、このアプリに出ている名前がそのまま出ます。
> 　**アプリを閉じると読み上げも止まります**ので、配信の間は開いたままにしてください。
> 　※ このエンジンに対応した版の読み分けちゃん2 が要ります。
>
> **消したいときは？**
> 　Windows の「**設定 → アプリ**」から、ふつうのアプリと同じように消せます。
> 　そのアプリが自分の場所に置いていた物（ダウンロードした一式・取り込んだ声・設定・記録）は
> 　**すべて一緒に消えます**。**何も聞かれません。**
> 　**声を追加するときに選んだ、あなたの音声ファイルには触れません。**そのまま残ります。
> 　両方の版を入れている場合、消えるのは消した方だけです。もう片方はそのまま使えます。

### footer

> このアプリは**非公式**です。もとになった音声合成 Irodori-TTS の作者 Aratako 氏とは関係がありません。
> **うまく動かないときも、Aratako 氏へは問い合わせないでください。**
> 出した音声には、人には聞こえない印が入ります。実在する人の声を、本人の許しなくまねる目的で使わないでください。
> うまく動かないときは、X の **@yomiwakechan**（https://x.com/yomiwakechan ）までお知らせください。

> **この位置に置く 1 行**（`decisions.md` 131）＝
> 「うまく動かないときは、X の **@yomiwakechan**（https://x.com/yomiwakechan ）までお知らせください。」
> **GitHub のリポジトリ（Issues）へは案内しない。**（いまの `site/index.html` の脚はリポジトリを指しているので、この 1 行に差し替える。）

---

## §6 `docs/guide.md` の全文（**150 行**・上限 150 行・アプリと一緒に入れる）

> **この檔は作成済み**＝`docs/guide.md`（本文は下のブロックと 1 字まで同じ）。片方だけ直さないこと。
> 上限を 120 行から **150 行**へ改めた＝⑴ 裁定 133 の 3 件を足したため（2 つの版は別のアプリで、設定も声も
> ダウンロードした物も別・同時には開けない／ファイルの場所は版ごと／消したときに消える物と、**触れない物**）
> ⑵ **画像 3 枚の刺し場所を「撮影待ち」と明記して置いた**（公式ページと同じ 3 枚＝`step1-setup`・`step2-setup`・
> `step3-talk`。RTX 機の席が v2.0 の画面で撮ったら、この 3 行を画像に差し替える）。
> `docs/install.md` は利用者に指さない（作る側の帳面＝憲章 原則 8）。
> **148 行 → 150 行**（v2.0 段 F の是正・2026-09-11・medium 16）＝`docs/radeon.md` も配布物から外したので、
> 利用者に要る 2 文（確かめたのは gfx1151 の 1 機種だけ・ほかは測っていない）を §1 の末尾に 1 行で畳んだ。
> **上限に達した**＝次にここへ足す席は、同じだけ削る所を先に決めること。

```markdown
# irodori-TTS for 読み分けちゃん 使い方

Windows のパソコンで、書いた文をその場でしゃべらせるアプリです。Python も、黒い画面も要りません。

## 1. 入れる

1. 公式ページから、お使いのパソコンに合う方を落とします。
   - **RTX（CUDA）**＝NVIDIA のグラフィックス（GeForce・RTX など）のパソコン
   - **Radeon（ROCm）**＝AMD の Radeon のパソコン
   - どちらか分からないときは、`Windows` キー ＋ `X` →「デバイス マネージャー」→
     「ディスプレイ アダプター」を開いて、NVIDIA と書いてあれば RTX（CUDA）です。
2. 落としたファイルをダブルクリックし、「次へ」「インストール」「完了」を押します。
   管理者のパスワードは要りません。
   ※「WindowsによってPCが保護されました」と出たら「詳細情報」→「実行」。

［**画像 1：インストーラの完了画面**］ — **撮影待ち**（v2.0 の画面で撮り直します）

**RTX（CUDA）と Radeon（ROCm）は別のアプリです。** 同じパソコンに両方入れられますが、
設定も、追加した声も、ダウンロードした物も、版ごとに別々です（RTX（CUDA）で追加した声は、Radeon（ROCm）には出ません）。
**同時に開くことはできません。** あとから開いた方は「ほかのソフトが、このアプリのつなぎ口を
使っています」と出ます。片方を閉じてから、もう片方を開いてください。

**Radeon（ROCm）で動くことを確かめたのは Ryzen AI MAX+ 395（Radeon 8060S）の 1 機種だけです。** ほかの Radeon で動くか・どのくらいの速さかは測っていません。

## 2. はじめの準備（1 回だけ・10 分ほど）

アプリが開くと、はじめの準備の画面が出ます。

1. お知らせを読み、チェックを入れて〔次へ〕。
2. 〔準備を始める〕を押し、ネットにつないだままお待ちください（約 5.3 GB・10 分ほど）。
3. 途中で 1 度、Microsoft の部品を入れるために Windows の許可の窓が出ます。「はい」を押してください
   （「いいえ」を押すと、しゃべらせられないことがあります）。
4. 「使えます。」と出たら終わりです。

［**画像 2：はじめの準備の進み具合**］ — **撮影待ち**（v2.0 の画面で撮り直します）

動かし方（CUDA 13.0 / CUDA 12.6 / ROCm / CPU）は**アプリが自動で選びます**。
自分で選び直したいときは、設定の「詳細（上級者向け）」で変えられます。
途中でやめても、次に開いたときは続きから始めます。最初からにはなりません。

## 3. しゃべらせる

声を選び、文を打って、〔しゃべらせる〕を押します。1〜2 秒で鳴ります。

［**画像 3：しゃべらせているところ**］ — **撮影待ち**（v2.0 の画面で撮り直します）

- **品質**＝「はやい」か「きれい」を選べます。
- **話し方の指示**＝「明るく」「落ち着いて」など、自由に書けます（空でもかまいません）。
- **速さ**＝1.0 がふつうです。
- 〔音声ファイルに保存…〕で wav として保存できます。

## 4. 読み分けちゃん2 から使う

このアプリを**開いたまま**にしておくと、読み分けちゃん2 の側でエンジンとして選べます。
こちら側で必要な操作はありません。読み分けちゃん2 が自動で見つけます。
声は、このアプリの「声」の画面の名前がそのまま出ます。

**アプリを閉じると読み上げも止まります。** 配信の間は開いたままにしてください。
配信中にこのアプリで〔しゃべらせる〕を押すと、「読み上げが終わるまでお待ちください」と出ます。
読み上げが終われば、そのまま使えます。

## 5. 声を増やす

「声」の画面 → 〔**声を追加する**〕を押し、元にする音声ファイル（wav）を選んで名前を付け、
〔**この声を追加する**〕を押します。
10〜30 秒くらいの、その人だけがしゃべっている録音がよく合います。
追加した声は、読み分けちゃん2 からも同じ名前で選べます。

選んだ音声ファイルは、アプリが**写しを取り込んで**使います。**あなたが選んだ元のファイルは動かしません。**

**実在する人の声を、本人の許しなくまねることは、この音声合成の利用条件で禁じられています。**
元にする音声は、使ってよいと許しを得た物だけにしてください。

## 6. 困ったときは（症状から引く）

### 準備の途中で止まった
窓を閉じて、もう一度開いてください。**続きから始めます。**
空きが足りないと出たときは、要らないファイルを消してから、もう一度お試しください
（準備には空き 12 GB ほどが要ります）。

### 「この GPU では、いまの動かし方が使えません」と出る
グラフィックスドライバが古いためです。画面に出ている釦を押すと、そのパソコンで動く方法に切り替えて
準備し直します。ドライバを新しくしてから開き直しても直ります
（NVIDIA は 580.00 以上で CUDA 13.0、528.33 以上で CUDA 12.6 が使えます）。

### 「グラフィックスが使えませんでした」と出る
いま選ばれている動かし方から、このパソコンのグラフィックスが見えていません。
設定の「詳細（上級者向け）」の「動かし方」で、別の動かし方に変えてお試しください
（変えると、動かすための一式を 5 GB 前後ダウンロードし直します）。
NVIDIA か AMD のページから最新のグラフィックスドライバを入れて開き直しても直ることがあります。

### 「このパソコンには対応する GPU が見つかりませんでした」と出る
NVIDIA のグラフィックスも AMD の Radeon も見つかりませんでした。
グラフィックスが無いパソコンでも動きますが、**とても遅い**ので配信には使えません。
グラフィックスがあるはずのときは、そのメーカーのページから最新のドライバを入れて開き直してください。

### 「このパソコンのグラフィックスは、この版では使えません」と出る
版を取り違えています（NVIDIA のパソコンに Radeon（ROCm）版を入れた／その逆）。
**ダウンロードが始まる前に閉じて**、公式ページからもう一方をダウンロードして入れ直してください。
取り違えた方は Windows の「設定 → アプリ」から消せます。

> **見出しは画面に出る文の逐語である**（是正・段 G・low 13）＝1 稿目は E-06 の
> 「対応するグラフィックスが見つかりませんでした」を見出しにしていたが、
> その綴りはコードのどこにも無く、§6 は**症状から引く**節なので引けない見出しになっていた。
> 上の 3 つは実物の綴り（`BandText.cs` の B1／`FirstRunViewModel.NoGpuDecisionLine`／
> 同 `WrongEditionNeedsCuda`／`WrongEditionNeedsRocm`）である。

### 「ほかのソフトが、このアプリのつなぎ口（18088）を使っています」と出る
まず、**もう一方の版（RTX（CUDA）／Radeon（ROCm））を同時に開いていないか**確かめてください。
2 つは別のアプリなので、同時には開けません。
それでも出るときは、どのソフトが使っているかを調べます。`Windows` キー ＋ `X` →「ターミナル」を開き、
`netstat -ano | findstr 18088` と打つと分かります。右端に出る番号を、タスク マネージャーの
「詳細」タブの「PID」の列で探すと、そのソフトの名前が出ます。
そのソフトを閉じてから、このアプリを開き直してください。
古い版のこのアプリが残って動いていることもあります。

### 読み分けちゃん2 が見つけてくれない
このアプリの窓が開いていて、上の帯が「**使えます**」になっているか確かめてください。
「準備しています…」の間はまだ使えません。
読み分けちゃん2 の側で、このエンジンを 1 度だけ選ぶ（有効にする）操作が要ります。
読み分けちゃん2 の設定で読み上げエンジンの一覧を開き、このアプリを選んでください。
※ このエンジンに対応した版の読み分けちゃん2 が要ります。

### 声が出ない・途中で切れる
品質を「はやい」にして、文を短くしてお試しください。
それでも直らないときは、いちど閉じてから開き直してください。

### 「アプリのファイルが壊れているようです」と出る
アプリを入れ直してください（上書きで入れ直せます）。**追加した声と設定は残ります。**
それでも直らないときは、いちど消してから入れ直してください。
※ 消すと、ダウンロードした物だけでなく、**追加した声と設定も一緒に消えます**（→ §9）。
入れ直したあとに、はじめの準備をもう一度やることになります（約 5.3 GB・10 分ほど）。

### それでも直らない
〔このアプリについて〕の〔**報告用のログを保存**〕を押すと、送るための記録が 1 つのファイルにまとまります。
そのファイルを添えて、X の **@yomiwakechan**（https://x.com/yomiwakechan ）までお知らせください。
（画面の〔ログを開く〕でも、記録のファイルの場所を開けます。）

## 7. 新しい版に上げる

上書きで入ります。設定と、自分で追加した声はそのまま残ります。
新しくなった分だけを、次に開いたときに取り直します。5.3 GB をもう一度落とし直すことはありません。

## 8. ファイルの場所

ふだんは開く必要はありません。設定の「詳細（上級者向け）」の「ファイルの場所」から
〔フォルダを開く〕で開けます。版ごとに別の場所です。

- RTX（CUDA）＝`%LOCALAPPDATA%\irodori-tts-ywk-cuda\`
- Radeon（ROCm）＝`%LOCALAPPDATA%\irodori-tts-ywk-radeon\`

ダウンロードした一式・取り込んだ声・設定・記録は、この中に入っています。
場所は変えられません。

## 9. 消す

Windows の「設定 → アプリ」から消せます。
消した方（RTX（CUDA）か Radeon（ROCm））が自分の場所に置いていた物（ダウンロードした一式・取り込んだ声・設定・記録）は
**すべて一緒に消えます**。**何も聞かれません。**
**声を追加するときに選んだ、あなたの音声ファイルには触れません。** そのまま残ります。
両方の版を入れている場合、消えるのは消した方だけです。もう片方はそのまま使えます。

## 10. このアプリについて

非公式のアプリです。もとになった音声合成 Irodori-TTS の作者（Aratako 氏）とは関係がありません。
うまく動かないときも、Aratako 氏へは問い合わせないでください。
出した音声には、AI が作った音であるという目印（人には聞こえません）が入ります。外す方法はありません。
うまく動かないときは、X の **@yomiwakechan**（https://x.com/yomiwakechan ）までお知らせください。
```

---

## §7 リリース文

### 7-1 ひな型（`docs/release-notes/vX.Y.Z.md`・**14 行**・上限 15 行）

```markdown
> 非公式のソフトです。もとになった Irodori-TTS の作者（Aratako 氏）とは関係がありません。

**文字を打つと、しゃべります。** 読み分けちゃん2 の読み上げにも使えます。

**この版で変わったこと**
- （利用者に見える変化を 1 行ずつ・最大 5 行・ふつうの言葉で）

**落とす物**
- **RTX（CUDA）** — NVIDIA の GeForce / RTX などのパソコンの方
- **Radeon（ROCm）** — AMD の Radeon のパソコンの方（確かめられているのは Ryzen AI MAX+ 395 ／ Radeon 8060S）

**はじめて開いたときに、動かすための一式（約 5.3 GB）をダウンロードします**（10 分ほど）。
必要なもの＝Windows 11 / 10（64 ビット）・空き 12 GB ほど・はじめの準備のときだけネット接続。
はじめての方は**公式ページの「使い方」**をご覧ください＝https://mugonkun.github.io/irodori-tts-for-yomiwakechan/
困ったときは X の **@yomiwakechan** までお知らせください。
```

**書かないこと**＝sha256・バイト数・`SHA256SUMS.txt`・動かし方ごとの内訳の表・RTF・実測値・試験の本数・
番号つきの判断・元実装の commit・つなぎ口の番号・送り先を名指しする 1 行。
**書いてよいこと**＝`CUDA`／`ROCm`／`NVIDIA`／`Radeon`／`GeForce`／`RTX`、および
「判っている制約」でのドライバの下限（CUDA 13.0 には 580.00 以上）。

### 7-2 `docs/release-notes/v2.0.0.md`（**作成済み**＝下のブロックと 1 字まで同じ）

> 資産の檔名（`irodori-tts-ywk-setup-v2.0.0-cuda.exe`／`…-radeon.exe`）は**落とす物の見分け**に要るので出す。
> **sha256・バイト数・`SHA256SUMS.txt` は出さない**（`decisions.md` 125）。
> 「変わったこと」の 5 行目（削除）は `decisions.md` 132／133 の当て込みで、**ひな型の上限を 5 行に改めた**。

```markdown
> 非公式のソフトです。もとになった Irodori-TTS の作者（Aratako 氏）とは関係がありません。

**文字を打つと、しゃべります。** 読み分けちゃん2 の読み上げにも使えます。

**この版で変わったこと**
- **開いたら、そのまま使えるようになりました。**「起動」を押す必要がなくなり、上の帯が
  「準備しています…」から「使えます」に変わるだけになりました。
- **はじめの準備から、選ぶ場面が無くなりました。** お使いのグラフィックスに合う動かし方を
  アプリが決め、結果を 1 行で告げます（自分で選び直す口は、設定の「詳細」にあります）。
- **画面をふつうの言葉に書き直しました。** しゃべらせる画面・声の画面・設定の 3 つが中心になり、
  数字と記録は「詳細」に畳みました。うまくいかないときは、何が起きたか・なぜか・次にどうするかを 3 行で出します。
- **新しい版に上げたとき、増えた声や部品だけを取り直すようになりました**（全部の落とし直しはしません）。
- **消したときに、このアプリが置いていた物もすべて一緒に消えるようになりました。** 何も聞かれません。
  声を追加するときに選んだ、あなたの音声ファイルには触れません。

**落とす物**
- **RTX（CUDA）** — NVIDIA の GeForce / RTX などのパソコンの方
  （`irodori-tts-ywk-setup-v2.0.0-cuda.exe`）
- **Radeon（ROCm）** — AMD の Radeon のパソコンの方（確かめられているのは Ryzen AI MAX+ 395 ／ Radeon 8060S）
  （`irodori-tts-ywk-setup-v2.0.0-radeon.exe`）

**はじめて開いたときに、動かすための一式（約 5.3 GB）をダウンロードします**（10 分ほど）。
必要なもの＝Windows 11 / 10（64 ビット）・空き 12 GB ほど・はじめの準備のときだけネット接続。
はじめての方は**公式ページの「使い方」**をご覧ください＝https://mugonkun.github.io/irodori-tts-for-yomiwakechan/
困ったときは X の **@yomiwakechan** までお知らせください。

**判っている制約**
- CUDA 13.0 にはグラフィックスドライバ 580.00 以上が要ります（528.33 以上なら CUDA 12.6 で動きます）。
- Radeon（ROCm）は Ryzen AI MAX+ 395 ／ Radeon 8060S でしか確かめていません。
- RTX（CUDA）と Radeon（ROCm）は別のアプリです。同じパソコンに両方入れられますが、設定も、追加した声も、
  ダウンロードした物も別で、同時には開けません。
- 読み分けちゃん2 で使う間は、このアプリを開いたままにしてください。
```

> ※ 上の「変わったこと」4 行目（差分の取り直し）は **v2.0 の必須要件**として憲章 §4-24 に載っている。
> 実装が入らなかった場合は、この 1 行を落として 3 行で出す。

---

## §8 〔このアプリについて〕の全文

> # irodori-TTS for 読み分けちゃん － RTX（CUDA）
> 　（Radeon 機では `… － Radeon（ROCm）`）
>
> ---
>
> **これは非公式のアプリです。**
> もとになった音声合成 Irodori-TTS の作者（Aratako 氏）とは関係がありません。
> **うまく動かないときも、Aratako 氏へは問い合わせないでください。**
>
> ---
>
> **バージョン**
> v2.0.0 － RTX（CUDA）
>
> **音に埋め込まれる印**
> 出した音声には、**AI が作った音であるという目印**が入ります。人には聞こえません。**外す方法はありません。**
>
> **声の元にする音声について**
> 元にする音声は、使ってよいと許しを得た物だけにしてください。
> 実在する人の声を、本人の許しなくまねることは、この音声合成の利用条件
> （Ethical Restrictions 1・No Impersonation）で禁じられています。
>
> **ライセンス**
> このアプリと一緒に入っているライセンス文の場所＝〈路〉
> 動かすための一式と声のデータは、このアプリには入っていません。はじめの準備のときに、
> 公式の配布元からこのパソコンへダウンロードします。それぞれのライセンス文も一緒に入ります。
>
> ▸ **詳細（上級者向け）**（ライセンスの一覧・もとになった実装の版）
>
> 〔**使い方を見る**〕　〔**報告用のログを保存**〕

**〔報告用のログを保存〕を押したあとの 1 行**＝
「**記録をまとめました。**〈路〉　うまくいかないときは、このファイルを添えて X の **@yomiwakechan** までお知らせください。」
（〔フォルダを開く〕を脇に置く。**GitHub の Issues へは案内しない**＝`decisions.md` 131。）

---

## §9 語の検分

### 9-1 この檔の利用者向けの文（§2〜§8）に **1 語も無い**ことを確かめた語

変種・実行系・取得台帳・台帳・潜在・参照潜在・焼く・焼き印・暖機・サーバ・ポート・接続先・
エンドポイント・裁定・`decisions.md`・wrapper・上流・pin・commit・門・便・配布版・配布物・配布樹・
データ樹・檔（利用者向けの文では「ファイル」）・話者（「声」）・試し撃ち・取り寄せる・初回取得・
展開・RTF（詳細の外）・GiB（詳細の外）・sha256（Release の外）・平時のつなぎ口の番号、
および「原文のまま引く」を指す硬い言い方（所有者の指示 2026-09-10＝「**原文**」「**そのまま**」と書く）。

※ この §1 の対照表と §9 は**作る側が読む欄**なので、旧語を「いま」の側に引用している。
　**利用者に届く面（§2〜§8 の引用ブロックの中身）には 1 語も出していない。**

### 9-2 出してよい技術語と、出した場所

| 語 | 出した場所 |
|---|---|
| CUDA・ROCm | 版の名（RTX（CUDA）／Radeon（ROCm））・§2 段 2 の告げる 1 行・§3-2 E-02・§4-2・§5 FAQ・§7 |
| cu126／cu130 | **どこにも出していない**（CUDA 12.6／13.0 と書けば足りる。詳細の中では原語を出してよい） |
| GPU・VRAM・NVIDIA・AMD・Radeon・GeForce・RTX | §2 段 2・§3-2・§4・§5・§6・§7 |
| ドライバとその版（580.00・528.33） | §3-2 E-02・§4・§5 必要なもの・§6-6・§7 判っている制約 |
| bf16・FP32 | §4 の 3 行目（設定 › 詳細）だけ |
| Windows・wav・Python | どこでも |
| 18088 | **§3-2 E-05 と §6 の同じ症状の節の 2 箇所だけ**（つなぎ口が埋まったときの唯一の手がかり＝原則 5 の例外。**設定 › 詳細にも 詳しい状態にも公式ページにも出さない**） |
| SHA256 | **どこにも出していない**（Release の本文と記録の中だけ） |
| `%LOCALAPPDATA%\irodori-tts-ywk-cuda\`／`…-radeon\`（**版ごとの置き場**） | **§6 の「8. ファイルの場所」と §4-7（設定 › 詳細）の 2 面だけ**。2 段の検分を通る＝⑴ `%LOCALAPPDATA%` は世間の語で他所の解説が出る ⑵「自分の物がどこに在るか・消したら何が消えるか」という利用者の判断に直結する。**「データ樹」「配布樹」という語は使わない**（`decisions.md` 133） |

### 9-3 開いている点（決めが要る／実装が要る）

1. **報告の受け皿は決まった**＝X の `@yomiwakechan`（`decisions.md` 131）。§5 footer・§6 の「それでも直らない」・
   §7 のリリース文・§8 の 4 面に同じ 1 行を置いた。**GitHub の Issues は受け皿にしない。**
2. **`docs/guide.md` の中の 3 つの案内先**（〔入れ直す手順を見る〕E-03・〔使っているソフトを調べる方法を見る〕E-05・
   〔ドライバの入れ方を見る〕E-06）は、いま `guide.md` §6 の見出しへ飛ばす形にしてある。
   アプリから開く先を「公式ページの同じ見出し」にするか「同梱の `guide.md`」にするかは実装席の判断。
3. **§7-2 の「変わったこと」4 行目**（更新時に差分だけ取り直す）は憲章 §4-24 の v2.0 必須要件だが、
   実装が入らなければ**この 1 行を落とす**。嘘を書かない。**同じ歯止めが 3 箇所に掛かる**＝
   §3-1 の帯の「新しい版があります」の行・§2 段 3 の「新しくなった分をダウンロードしています。」・
   §6 の「7. 新しい版に上げる」の 2 行目（`v2-spec.md`:707・:779・:826 の 3 箇所も同じ）。
4. **裁定 133（置き場と削除）を当て込んだ**＝⑴ **2 つの版は別のアプリ**（設定も声もダウンロードした物も共有せず、
   同時には開けない）＝§5 FAQ・§6-1・§6-9 ⑵ **ファイルの場所は版ごと**＝§4-7・§6-8 ⑶ **削除はその版の置き場を
   丸ごと消し、問わない。置き場の外の元の wav には触れない**＝§5 FAQ・§6-9。
   `site/index.html` の FAQ（§5）にも同じ 2 件（別のアプリ・消したときの範囲）が要る＝**着地頁の席が当て込む**。
   実装の落とし所＝`v2-spec.md` §11-8／`v2-plan.md` 段 E-3。
5. **画像 3 枚（`site/img/step1-setup.png`・`step2-setup.png`・`step3-talk.png`）は未撮影。**
   v2.0 の画面で撮り直すまで、公式ページに古い画面を載せない。
