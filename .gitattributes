# 既定＝テキストは自動判定（グローバル core.autocrlf=true と同じ挙動）
* text=auto
*.sh   text eol=lf
*.ps1  text eol=crlf
*.cmd  text eol=crlf
*.bat  text eol=crlf
*.iss  text eol=crlf
# research/ は調査便の写し＝バイト列をそのまま保存（改行の正規化をしない）
research/** -text
